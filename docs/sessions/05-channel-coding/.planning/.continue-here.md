---
context: default
phase: null
status: between-phases
last_updated: "2026-06-06T23:27:57.217Z"
milestone_progress: "83% — phases 1,2,3,5 done; phase 4 (Polar Lab) and phase 6 (QA) pending"
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: SC-DECODER-BUG — Cell 17 of lab.ipynb (SC recursive decoder, disabled with TODO comment) has a kron convention bug; BER_SC = 0.5 at all SNRs. Fix before any Phase 4 figure work.
- [ ] CONSTRAINT: FIGURE-CORRECTION-UNCOMMITTED — polar-butterfly-n4.png was regenerated this session with visual fixes (label overlap, external legend) but NOT committed. The version in the repo (665a1c5) still has the old broken layout. Must commit corrected figure + update lab.ipynb cell 16 before anything else.

**Do not proceed past the first task until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Fabricated figure interpretation | Writing captions that infer behavior from theory without running the code | blocking | Run the cell, read output, then write caption |
| Wrong kron indexing | G_N = kron(G_{N/2}, F) uses adjacent pairs (2j, 2j+1), not half-offset (j, j+N/2) | blocking | Verify SC decoder with noiseless test before any BER figure |

<current_state>
Session (Jun 6 2026) was entirely educational: walked through Polar code encoding butterfly N=4 interactively, added a worked example and figure to §4.1. The figure (polar-butterfly-n4.png) was regenerated at end of session to fix visual issues (overlapping labels, legend inside canvas) but the corrected version was NOT committed — the repo has the broken version from commit 665a1c5.

Two files are uncommitted:
1. `figures/polar-butterfly-n4.png` — corrected layout (must commit)
2. `index.md` — minor blank-line formatting (benign stash residue, commit or discard)

Lab.ipynb cell 16 (N=4 butterfly generator) also needs updating to match the corrected figure code.
</current_state>

<completed_work>

**This session (Jun 6, afternoon):**
- Explained channel W, LLR relationship, and n in N=2^n
- Walked through N=4 encoding butterfly interactively (u=[0,0,1,0] → x=[1,0,1,0])
- Walked through N=4 SC decoding interactively (same values, showed LLR amplification 1.2→8.0)
- Added `??? example "Ejemplo: encoding butterfly N=4 paso a paso"` to §4.1 → commit 3478ea8
- Added polar-butterfly-n4.png to §4.1 admonition + lab.ipynb cell 16 → commit 665a1c5
- Regenerated figure with visual fixes (NOT YET COMMITTED)

**Earlier this session (Jun 6, morning):**
- Fixed Figure 3 iteration-10 caption (removed unverified LLR overflow claim) → 066fb8b
- Paused and created handoff → 7eeef92
</completed_work>

<remaining_work>

**Immediate (before anything else):**
1. Update lab.ipynb cell 16 with corrected N=4 butterfly code (label separation, external legend)
2. Commit: figures/polar-butterfly-n4.png + lab.ipynb + index.md

**Next (Phase 4 — Polar Lab):**
3. gsd-quick fix-sc-decoder — fix kron convention in cell 17 (was 16), noiseless test
4. Plan 04-01: polar BER figure (SC vs SCL-8 vs uncoded)
5. Plan 04-02: figure blocks §4.1/§4.2 in index.md

**Then (Phase 6 — QA):**
6. MkDocs strict final pass, verify all figures, tag v1.0
</remaining_work>

<decisions_made>

- polar-butterfly-n4.png uses: salmon=#F08080 (frozen), blue=#4C9BE8 (info), green=#5DB05D (x=0), red=#C44 (x=1)
- u_i and (f) labels placed on separate lines (u_i above, (f) below) to avoid overlap
- Legend placed below figure using fig.legend() + subplots_adjust(bottom=0.12)
- node_x range [0.22, 0.78] (wider margins than original [0.12, 0.88])
- Example uses N=4 (not N=8 like Figure 5) — declared explicitly in the admonition title
</decisions_made>

<blockers>

- SC-DECODER-BUG: lab.ipynb cell 17 disabled, BER_SC=0.5 everywhere. Blocks Phase 4.
- FIGURE-CORRECTION-UNCOMMITTED: corrected polar-butterfly-n4.png in working tree, not in git.
- Deleted tracked files: figures/ofdm_rx_core.png and ofdm_tx_core.png — check if referenced in index.md before committing deletion.
</blockers>

## Required Reading (in order)

1. `lab.ipynb` cell 16 — read current (broken) N=4 butterfly code before replacing with corrected version
2. `lab.ipynb` cell 17 — read SC decoder TODO comment to understand exact kron bug
3. `.planning/STATE.md` — phase progress

## Infrastructure State

- MkDocs strict build: PASS as of commit d104185
- figures/polar-butterfly-n4.png: corrected version in working tree (uncommitted)
- No running servers

<context>
The session was almost entirely interactive/educational — explaining Polar codes to the student (W as channel concept, LLR relationship, butterfly structure, N=4 encoding then decoding). The main deliverable was the worked example + figure added to §4.1. The figure needed visual corrections that were done at end of session but not committed due to context limits.
</context>

<next_action>
Start with: read lab.ipynb cell 16, replace its source with the corrected polar-butterfly-n4.png generation code (the fixed version with separate u_i/(f) labels and external legend), then commit figures/polar-butterfly-n4.png + lab.ipynb + index.md as a single fix commit.
</next_action>
