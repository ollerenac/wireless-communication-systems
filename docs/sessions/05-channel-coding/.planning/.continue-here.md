---
context: default
phase: null
task: null
total_tasks: null
status: between-phases
last_updated: "2026-06-06T10:23:00.000Z"
milestone_progress: "83% — phases 1,2,3,5 done; phase 4 (Polar Lab) and phase 6 (QA) pending"
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: SC-DECODER-BUG — Cell 16 of lab.ipynb has a disabled SC decoder with a kron convention bug (combines (j, j+N/2) instead of adjacent pairs (2j, 2j+1)); BER_SC = 0.5 at all SNRs — fix this first before any polar BER figure work

**Do not proceed with Phase 4 until the box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Fabricated figure interpretation | Describing histogram bars as "LLR overflow" or "did not converge" without running the code to verify — two caption rewrites were needed to remove these claims | blocking | Before writing any caption, run the notebook cell and read the actual output; never infer convergence state from visual appearance alone |
| Unverified quantitative claims | Writing "less than 2 dB from Shannon limit" or "9 errors visible" without counting/computing | advisory | Anchor every numeric claim to a specific notebook cell output or figure coordinate |

<current_state>
Paused between phases. The session (Jun 6 2026 05:xx GMT-5) was entirely spent on figure caption corrections and educational Q&A about LDPC BP mechanics. No new lab code was written. The last substantive code commit was the ofdm-ldpc-ber.png integrator (Phase 5, complete). Phase 4 (Polar Lab + BER figures) has not been started because Cell 16 in lab.ipynb has a disabled SC decoder.

The index.md is in a good state: §3.1, §3.3, §4.1, §4.2, §2 and all figure captions have been polished. Figure 3 (bp-messages.png) and Figure 4 (ldpc-ber-waterfall.png) captions are now accurate.
</current_state>

<completed_work>

**This session (Jun 6):**
- Figure 3 iteration-10 caption: removed unverified LLR overflow claim → `066fb8b`

**Recent sessions (Jun 5):**
- Clarified Figure 3 and Figure 4 captions (LLR direction, waterfall threshold vs Shannon limit) → `b444dfb`, `e74e2c6`
- Expanded §3.1 Tanner graph with explicit H matrix, parity equations, LLR inputs, BP termination → `1f56da9`
- Clarified §2 coding trade-off (derive Ec = Rc·Eb from energy conservation) → `ccf9f60`
- Quick task: clarify §4.1 (Z(W) intuition, N=4 mini-example) and §4.2 (worked SC decoding, SCL beam-search analogy) → `410bf89`
- Quick tasks: remove Mermaid Tanner diagram, center check nodes in Tanner graph → `d37498e`, `6bb2204`

**Earlier sessions:**
- Phase 1: Index Polish (hooks, lab section, figure blocks, factual corrections)
- Phase 2: Shannon capacity + waterfall figures
- Phase 3: LDPC lab (Tanner graph, BP decoder, bp-messages.png, ldpc-ber-waterfall.png)
- Phase 5: OFDM+FEC integrator (ofdm-ldpc-ber.png, LAB-03)
</completed_work>

<remaining_work>

**Immediate next (Phase 4 — Polar Lab + Figuras):**
1. Fix Cell 16 SC decoder — kron convention bug (adjacent pairs, not half-offset pairs)
2. Verify BER_SC < 0.1 at SNR=4dB with a noiseless test
3. Plan 04-01: generate polar BER figure (SC vs SCL-8 vs uncoded) — save as `figures/polar-ber.png`
4. Plan 04-02: add `<figure markdown="span">` blocks for FIG-06 (polar-butterfly), FIG-07 (polar-polarization), new polar-ber figure in §4.1/§4.2 of index.md

**Then (Phase 6 — QA & Publicación):**
- MkDocs strict build final pass
- Check all figure references resolve
- Verify lab.ipynb runs end-to-end without errors
- Commit + tag v1.0
</remaining_work>

<decisions_made>

- All-zeros codeword is the simulation convention; negative LLR = bit error
- BP convergence is determined by H·ĉ=0 early termination, not LLR magnitude alone
- SC decoder disabled rather than left with wrong output to avoid misleading BER figures
- Phase 5 (integrador) was completed before Phase 4 (polar) because the integrator only needs LDPC, not Polar
- explanations.ipynb (scratch notebook, 4 cells, Jun 4) is safe to delete or ignore
</decisions_made>

<blockers>

- SC-DECODER-BUG: Cell 16 lab.ipynb — kron convention mismatch, BER_SC = 0.5 everywhere. Fix before Phase 4.
- Deleted tracked files: `figures/ofdm_rx_core.png` and `figures/ofdm_tx_core.png` are staged for deletion but not committed. Verify they are not referenced in index.md before committing the deletion.
</blockers>

## Required Reading (in order)

1. `lab.ipynb` Cell 16 — read the disabled SC decoder comment to understand the exact bug before fixing
2. `.planning/STATE.md` — current phase progress and key decisions
3. `index.md` §4.1 and §4.2 — the polar sections that the Phase 4 figures must support

## Critical Anti-Patterns (do NOT repeat these)

- FABRICATED-CAPTIONS: Do not describe what figures *should* show based on theory — run the cell, read the output, then write the caption. Two rewrites (b444dfb, 066fb8b) were needed to remove unverified claims.
- WRONG-KRON-INDEXING: For G_N = kron(G_{N/2}, F), the butterfly combines pairs (2j, 2j+1), not (j, j+N/2). The latter is for the Hadamard (bit-reversal) convention.

## Infrastructure State

- MkDocs strict build: PASS as of commit `066fb8b`
- lab.ipynb: Cells 0–15 and 17–21 run correctly; Cell 16 disabled
- All figures in `figures/` except ofdm_rx_core.png and ofdm_tx_core.png (deleted, tracked)
- No running servers

<context>
The session was educational: the user asked about the LDPC BP histogram figures and we discovered that two captions had fabricated claims (LLR overflow, iteration-10 behavior, "2 dB from Shannon"). All three were corrected. The index.md is now accurate and well-structured. The only remaining work is the Polar BER figures (needs SC decoder fix) and final QA. The SC decoder bug has been known since 2026-05-29 and has blocked Phase 4 since then.
</context>

<next_action>
Start with: `gsd-quick fix-sc-decoder` — fix Cell 16 of lab.ipynb by correcting the kron butterfly indexing from half-offset (j, j+N/2) to adjacent-pair (2j, 2j+1), then run a noiseless test (SNR=100dB, send all-zeros, verify BER_SC = 0).
</next_action>
