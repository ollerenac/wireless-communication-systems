---
phase: quick-260606-9gm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/sessions/05-channel-coding/index.md]
autonomous: true
requirements: [QUICK-260606-9gm]
must_haves:
  truths:
    - "Un lector que abre §4.1 puede expandir un ??? example colapsado que muestra el encoding butterfly N=4 paso a paso"
    - "El ejemplo muestra el cálculo XOR de las dos etapas y la codeword resultante x = [1, 0, 1, 0]"
    - "El ejemplo incluye una tabla de influencia que explica por qué u0 es el canal sintético más débil"
    - "El index.md sigue siendo MkDocs-Material válido (admonition ??? example, sintaxis de tabla en pipe)"
  artifacts:
    - path: "docs/sessions/05-channel-coding/index.md"
      provides: "Ejemplo colapsable encoding butterfly N=4 en §4.1"
      contains: "Ejemplo: encoding butterfly N=4 paso a paso"
  key_links:
    - from: "index.md §4.1 figura 5 (polar-butterfly)"
      to: "??? example encoding butterfly N=4"
      via: "inserción inmediatamente tras </figure> y antes de '¿Cómo medir si un canal sintético'"
      pattern: "Ejemplo: encoding butterfly"
---

<objective>
Añadir un ejemplo colapsable (`??? example`) a §4.1 de `index.md` que muestra el encoding butterfly de un código Polar N=4 paso a paso: cálculo XOR de las dos etapas, codeword resultante, y una tabla de influencia que explica por qué u₀ es el canal sintético más débil.

Purpose: Conectar la transformación butterfly abstracta (Figura 5, N=8) con un cálculo numérico concreto y pequeño (N=4) que el lector puede seguir a mano, motivando intuitivamente el parámetro de Bhattacharyya que se introduce inmediatamente después.
Output: `index.md` con un nuevo bloque `??? example` insertado entre la Figura 5 y la explicación del parámetro de Bhattacharyya.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<insertion_anchor>
Insertar el nuevo bloque `??? example` en index.md inmediatamente DESPUÉS de la línea de cierre `</figure>` del bloque de la Figura 5 (polar-butterfly.png, líneas ~325-330) y ANTES de la línea que comienza con:

  `**¿Cómo medir si un canal sintético es "bueno" o "malo"?**`

Estructura actual del punto de inserción (verificada):

```
  </figcaption>
</figure>
                       <-- NUEVO BLOQUE ??? example AQUÍ (separado por línea en blanco arriba y abajo)
**¿Cómo medir si un canal sintético es "bueno" o "malo"?** El **parámetro de Bhattacharyya**...
```

Nota de consistencia narrativa: el texto circundante (línea ~323) usa N=8 para describir la Figura 5. El nuevo ejemplo usa N=4 deliberadamente (caso más pequeño, seguible a mano). NO modificar la narrativa N=8 existente; el ejemplo es autocontenido y declara explícitamente "Código Polar N=4" en su escenario.
</insertion_anchor>

<formatting_reference>
Estilo de admonition existente en este index.md (usar como plantilla de indentación):
- `??? example` colapsado por defecto (como el ejemplo de BP en §3.2 y el mini-ejemplo de §4.1)
- Título en la misma línea que `??? example`, entre comillas
- Cuerpo indentado con 4 espacios
- Bloques de código indentado mantienen su propia fence ``` dentro de la indentación de 4 espacios
- Tablas en pipe markdown estándar, indentadas 4 espacios
- Math inline con `$...$`, símbolos de subíndice usando `u_0` etc. o caracteres Unicode (u₀) — mantener consistencia con el contenido verbatim provisto
</formatting_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insertar el bloque ??? example encoding butterfly N=4 en §4.1</name>
  <files>docs/sessions/05-channel-coding/index.md</files>
  <action>
Insertar un nuevo bloque de admonition `??? example "Ejemplo: encoding butterfly N=4 paso a paso"` (colapsado por defecto) en §4.1, inmediatamente después del `</figure>` de la Figura 5 (polar-butterfly.png) y antes de la línea `**¿Cómo medir si un canal sintético es "bueno" o "malo"?**`. Dejar una línea en blanco antes y después del bloque.

El contenido del admonition (todo el cuerpo indentado 4 espacios para que MkDocs-Material lo reconozca) debe contener, en español con términos técnicos en inglés:

1. **Escenario:** declarar Código Polar N=4, tasa 1/2; bits congelados {u₀, u₁}, bits de información {u₂, u₃}; vector de entrada u = [u₀, u₁, u₂, u₃] = [0, 0, 1, 0] con etiquetas frozen/frozen/info/info. Presentar el vector dentro de un bloque de código fenced (```) indentado.

2. **Etapa 1 — combinar pares adyacentes (0,1) y (2,3):** mostrar w₀ = u₀ ⊕ u₁ = 0 ⊕ 0 = 0 ; w₁ = u₁ = 0 ; w₂ = u₂ ⊕ u₃ = 1 ⊕ 0 = 1 ; w₃ = u₃ = 0 ; valores intermedios w = [0, 0, 1, 0]. Usar dos espacios al final de cada línea de cálculo para forzar salto de línea en Markdown, como en el contenido verbatim.

3. **Etapa 2 — combinar pares intercalados (0,2) y (1,3):** mostrar x₀ = w₀ ⊕ w₂ = 0 ⊕ 1 = 1 ; x₂ = w₂ = 1 ; x₁ = w₁ ⊕ w₃ = 0 ⊕ 0 = 0 ; x₃ = w₃ = 0 ; Codeword en negrita x = [1, 0, 1, 0].

4. **¿Por qué u₀ es el canal sintético más débil?** seguido de una tabla en pipe markdown con columnas: Bit | Aparece en | Señales que lo transportan, con filas u₀ → x₀ → 1 ; u₁ → x₀, x₁ → 2 ; u₂ → x₀, x₂ → 2 ; u₃ → x₀, x₁, x₂, x₃ → 4 (los conteos en negrita).

5. Párrafo de cierre explicando que u₃ se distribuye por las 4 posiciones del codeword (cuatro observaciones ruidosas + info de û₀, û₁, û₂ para recuperarlo) mientras u₀ solo llega a x₀ y el SC lo decodifica primero sin información de cancelación; concluir que eso es exactamente lo que mide el parámetro de Bhattacharyya Z(W): la dificultad de recuperar un bit sin información de contexto.

Usar el contenido verbatim provisto en el contexto del planner como fuente exacta del texto. NO modificar ninguna otra parte de index.md (la narrativa N=8 de la Figura 5 permanece intacta).
  </action>
  <verify>
    <automated>cd /home/researcher/Teaching/uni/2026/wireless-communication-systems/docs/sessions/05-channel-coding && grep -q 'Ejemplo: encoding butterfly N=4 paso a paso' index.md && grep -q 'x = \[1, 0, 1, 0\]' index.md && grep -A2 '</figure>' index.md | grep -q '??? example' && echo OK</automated>
  </verify>
  <done>El bloque `??? example` aparece en §4.1 entre la Figura 5 y la explicación de Bhattacharyya; contiene las dos etapas XOR, la codeword x = [1, 0, 1, 0], la tabla de influencia y el párrafo de cierre. El resto de index.md sin cambios.</done>
</task>

</tasks>

<verification>
- `grep -n 'Ejemplo: encoding butterfly N=4'` devuelve una línea dentro del rango de §4.1 (líneas ~330-332).
- El bloque está colapsado por defecto (`??? example`, no `!!! example`).
- La tabla de influencia tiene 4 filas (u₀..u₃) con conteos 1, 2, 2, 4.
- El bloque de cierre `</figure>` de la Figura 5 sigue intacto y precede al nuevo ejemplo.
- La línea `**¿Cómo medir si un canal sintético...` sigue presente, ahora después del nuevo ejemplo.
</verification>

<success_criteria>
- §4.1 de index.md contiene un `??? example` colapsable con el encoding butterfly N=4 paso a paso.
- El ejemplo es MkDocs-Material válido (indentación 4 espacios, fence de código y tabla pipe correctas).
- Contenido en español con términos técnicos en inglés, coherente con el estilo de admonitions existentes (BP §3.2, mini-ejemplo §4.1).
- Ningún otro contenido de index.md modificado.
</success_criteria>

<output>
Create `.planning/quick/260606-9gm-add-encoding-butterfly-example-to-4-1-in/260606-9gm-SUMMARY.md` when done
</output>
