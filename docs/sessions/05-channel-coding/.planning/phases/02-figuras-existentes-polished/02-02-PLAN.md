# Plan 02-02: FIG-03 — Waterfall curves analíticas + comentario index.md

**Phase:** 2 — figuras-existentes-polished
**Requirements:** FIG-03, LAB-05 (parte Ej6)
**Estimated effort:** 35 min

## Objective
Reemplazar completamente el código del Ejercicio 6 (Cell 13) por curvas waterfall analíticas multi-tasa — LDPC (r=1/2, 2/3, 3/4) + Polar (r=1/2, 3/4) + BPSK sin código — con threshold markers, guardando `figures/waterfall-curves.png` publicable; y actualizar el comentario de celda en `index.md` línea 245.

## Context
La Cell 13 actual ejecuta una simulación Monte Carlo del LDPC (8,4) que tarda ~10 s, produce ruido y solo muestra una curva codificada. D-04/D-05/D-11 ordenan descartarla por completo y reemplazarla con curvas analíticas Q-desplazadas: cero tiempo de ejecución, 6 curvas limpias, separación visual garantizada. La simulación BP real con waterfall visible llega en la Fase 3 (LAB-01) — esta figura es la versión pedagógica para el index.md.

Todos los valores (cliff_dB, alpha, colores) provienen de `02-RESEARCH.md` (sección "Verified Values for FIG-03"), donde se verificó que el coding gain a BER=1e-5 resulta 7.8 dB (r=1/2) a 5.4 dB (r=3/4), consistente con la figcaption del index.md que afirma "4-8 dB".

**Depende de:** Cell 1 ya actualizada por el plan 02-01 (`os.makedirs`, `figure.figsize`). La función `Q(x)` ya existe en Cell 1.

## Tasks

### Task 1: Reemplazar Cell 13 — Ej6 waterfall analítico (6 curvas)
**File:** `lab.ipynb` — celda índice **13** (la del código actual de `simulate_ldpc_bler` / Ej6)
**Action:** Borrar TODO el contenido de la celda (D-11: eliminar `awgn_llr`, `simulate_ldpc_bler`, `bp_ldpc_llr`, `bler_uncoded` y el bloque de plot Monte Carlo) y escribir desde cero la versión analítica. No tocar la celda markdown 12 (D-10: Ej6 se mantiene en su lugar con su número).

Implementación (copiar el patrón de RESEARCH.md "FIG-03: Cell 12-13 completa"):

1. Rango y baseline:
   - `EbN0_dB = np.linspace(-2, 12, 500)`; `EbN0_lin = 10**(EbN0_dB/10)`.
   - Baseline BPSK sin código: `ber_bpsk = Q(np.sqrt(2 * EbN0_lin))`.
2. Función auxiliar waterfall analítico:
   - `def ber_code(EbN0_dB_arr, cliff_dB, alpha): return Q(alpha * (EbN0_dB_arr - cliff_dB))`.
3. Lista `CODES` de 5 tuplas `(nombre, cliff_dB, alpha, color, ls)` con los valores verificados (RESEARCH.md tabla "Parámetros de las curvas analíticas"):

   | nombre | cliff_dB | alpha | color | ls |
   |--------|----------|-------|-------|----|
   | `'LDPC $r_c=1/2$'` | 0.68 | 4.0 | `'steelblue'` | `'-'` |
   | `'LDPC $r_c=2/3$'` | 1.95 | 4.0 | `'dodgerblue'` | `'--'` |
   | `'LDPC $r_c=3/4$'` | 3.09 | 4.0 | `'royalblue'` | `':'` |
   | `'Polar $r_c=1/2$'` | 1.68 | 2.5 | `'darkorange'` | `'-'` |
   | `'Polar $r_c=3/4$'` | 3.59 | 2.5 | `'orange'` | `'--'` |

4. `fig, ax = plt.subplots(figsize=(10, 5))` (D-07, explícito).
5. Plot baseline: `ax.semilogy(EbN0_dB, ber_bpsk, 'k-', lw=2, label='BPSK sin código')`.
6. Bucle sobre `CODES`: calcular `ber = np.maximum(ber_code(EbN0_dB, cliff, alpha), 1e-7)` (floor 1e-7, Pitfall 3) y `ax.semilogy(EbN0_dB, ber, color=color, lw=1.8, ls=ls, label=name)`.
7. **Threshold markers (D-06) — solo 3 líneas, una por tasa** (Pitfall 4: LDPC y Polar comparten threshold de Shannon a igual r, no dibujar 5 líneas solapadas). Bucle sobre `[(0.5, '1/2'), (2/3, '2/3'), (0.75, '3/4')]`:
   - `thr_dB = 10*np.log10((2**r_val - 1)/r_val)`.
   - `ax.axvline(thr_dB, color='gray', ls=':', lw=0.8, alpha=0.7)`.
   - Usar posiciones Y distintas para cada etiqueta (las tres líneas están a <0.5 dB, las etiquetas se solaparían con la misma Y): `y_pos = [5e-1, 1.5e-1, 5e-2]` — primer threshold más arriba, cada siguiente una décima abajo.
   - `ax.text(thr_dB + 0.05, y_pos[i], f'$C(r={label})$', fontsize=7, color='gray', rotation=90, va='top')` donde `i` es el índice de la iteración (0, 1, 2).
8. Ejes y leyenda: `ax.set_xlabel('$E_b/N_0$ (dB)')`, `ax.set_ylabel('BER')`, `ax.set_title('Curvas waterfall — LDPC, Polar y BPSK sin código (canal AWGN)')`, `ax.set_xlim(-2, 10)`, `ax.set_ylim(1e-6, 1.1)`, `ax.legend(fontsize=9, loc='lower left')`.
9. Guardado (D-08): `plt.tight_layout()` → `plt.savefig('figures/waterfall-curves.png', dpi=150, bbox_inches='tight')` → `plt.show()`.

NO reutilizar ninguna función de la celda antigua (D-11). NO usar `H_ldpc` ni Monte Carlo. NO usar `figsize=(8, 5)` (Pitfall 2).

**Verification:**
- [ ] La celda 13 ya no contiene `simulate_ldpc_bler`, `bp_ldpc_llr`, ni `np.random` (Monte Carlo eliminado)
- [ ] La celda ejecuta sin error y sin depender de celdas de LDPC previas (autocontenida salvo `Q` de Cell 1)
- [ ] La figura muestra exactamente 6 curvas (BPSK negra + 3 LDPC azules + 2 Polar naranjas) y 3 líneas de threshold verticales
- [ ] `figures/waterfall-curves.png` existe y pesa > 5 KB tras ejecutar
- [ ] `figsize=(10, 5)` en `plt.subplots`

### Task 2: Actualizar comentario de celda en index.md
**File:** `index.md` — línea 245
**Action:** Reemplazar el comentario placeholder del bloque `<figure>` de `waterfall-curves.png` para reflejar que la figura ya está generada.

- Actual (línea 245): `  <!-- será generada por lab.ipynb — Fase 2 -->`
- Target: `  <!-- generada por celda 13 de lab.ipynb -->`

(La celda de código del Ej6 es la celda índice 13 — confirmado por inspección del notebook; índice estable porque D-10 mantiene el Ej6 en su lugar sin reordenar.)

No modificar nada más del bloque `<figure>` ni de la figcaption (la narrativa "4-8 dB" ya es correcta y coincide con los valores verificados).

**Verification:**
- [ ] La línea 245 de index.md dice `<!-- generada por celda 13 de lab.ipynb -->`
- [ ] El resto del bloque `<figure>` (alt, ruta, figcaption) permanece sin cambios
- [ ] `grep -n "será generada por lab.ipynb" index.md` no devuelve resultados

### Task 3: Corregir LAB-05 en REQUIREMENTS.md (D-12)
**File:** `.planning/REQUIREMENTS.md` — línea 30
**Action:** Corregir la referencia incorrecta al número de ejercicio.

- Actual (línea 30): `- [ ] **LAB-05**: Ejercicio 1 (Shannon) y Ejercicio 5 (waterfall) actualizados...`
- Target: `- [ ] **LAB-05**: Ejercicio 1 (Shannon) y Ejercicio 6 (waterfall) actualizados...`

Solo cambiar `Ejercicio 5` → `Ejercicio 6`. No modificar el resto de la línea.

**Verification:**
- [ ] `grep -q "Ejercicio 6 (waterfall)" .planning/REQUIREMENTS.md && echo PASS`
- [ ] `grep -c "Ejercicio 5 (waterfall)" .planning/REQUIREMENTS.md` devuelve `0`

## Success Criteria
- [ ] **FIG-03:** `waterfall-curves.png` muestra ≥3 tasas LDPC + ≥2 tasas Polar + BPSK sin código (6 curvas) con threshold markers visibles
- [ ] **D-04:** curvas analíticas (Q-desplazada en dominio dB), sin Monte Carlo
- [ ] **D-05:** exactamente las 6 curvas especificadas (LDPC 1/2, 2/3, 3/4 + Polar 1/2, 3/4 + BPSK uncoded)
- [ ] **D-06:** 3 líneas verticales de threshold de Shannon (una por tasa) con etiqueta `C(r=...)`
- [ ] **D-07/D-08:** `figsize=(10, 5)`, guardada `dpi=150, bbox_inches='tight'`
- [ ] **D-10/D-11:** Ej6 actualizado en su lugar, código Monte Carlo antiguo eliminado por completo
- [ ] **D-12:** REQUIREMENTS.md línea 30 dice "Ejercicio 6 (waterfall)" — no "Ejercicio 5"
- [ ] **LAB-05 (Ej6):** la figura se genera por la celda del notebook y se almacena en `figures/`
- [ ] index.md línea 245 actualizada (comentario "generada por celda 13")

## Dependencies
- Plan 02-01 ejecutado (Cell 1 actualizada con `os.makedirs` y `figure.figsize`). La función `Q(x)` de Cell 1 es prerequisito para `ber_bpsk` y `ber_code`.
- Si se ejecuta de forma aislada, basta con que Cell 1 se haya ejecutado primero en la sesión del kernel.

## Post-execution
- Ejecutar Cell 1 → Cell 13 y verificar visualmente: 6 curvas separadas, waterfall cliffs claros, BPSK con caída suave, 3 thresholds legibles.
- Confirmar `test -f figures/waterfall-curves.png` y tamaño > 5 KB.
- Verificación de integración (recomendada antes de cerrar la fase): `jupyter nbconvert --to notebook --execute lab.ipynb --ExecutePreprocessor.timeout=120 --output /tmp/test-04.ipynb` debe completar sin error (cubre LAB-05 end-to-end de Cells 1-13).
- Commit sugerido: `feat(02-02): waterfall-curves.png analítico multi-tasa + comentario index.md (FIG-03, LAB-05)`. Incluir `lab.ipynb`, `figures/waterfall-curves.png`, `index.md`.
- Commit sugerido para Task 3 en el mismo commit que Tasks 1-2: `feat(02-02): waterfall-curves.png analítico multi-tasa + corrección D-12 + comentario index (FIG-03, LAB-05)`.
- NO actualizar STATE.md ni crear SUMMARY (lo hace el flujo de ejecución).
