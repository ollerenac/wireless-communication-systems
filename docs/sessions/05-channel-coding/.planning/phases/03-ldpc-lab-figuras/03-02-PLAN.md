---
phase: 03-ldpc-lab-figuras
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - lab.ipynb
  - index.md
autonomous: true
requirements:
  - FIG-05
  - FIG-08
  - LAB-01

must_haves:
  truths:
    - "bp_awgn converge correctamente: a Eb/N0=5 dB con tasa 1/2, retorna en <15 iteraciones"
    - "figures/bp-messages.png existe con 3 histogramas de LLR para iteraciones 1, 3, 10"
    - "figures/ldpc-ber-waterfall.png muestra waterfall cliff visible — BER cae ≥3 décadas en ≤4 dB para tasa 1/2"
    - "index.md §3.2 referencia tanto bp-messages.png como ldpc-ber-waterfall.png con bloques <figure markdown='span'>"
    - "Las nuevas celdas están en posiciones 8, 9, 10 del notebook (después de la Cell 7 modificada)"
  artifacts:
    - path: "lab.ipynb"
      provides: "3 nuevas celdas: Cell 8 (Gallager+BP decoder), Cell 9 (bp-messages.png), Cell 10 (Monte Carlo+waterfall)"
      contains: "gallager_ldpc"
    - path: "figures/bp-messages.png"
      provides: "Histogramas de LLR en iteraciones 1, 3, 10"
      min_size_bytes: 50000
    - path: "figures/ldpc-ber-waterfall.png"
      provides: "Curvas BER Monte Carlo para tasa 1/2 y 3/4 + BPSK sin código"
      min_size_bytes: 50000
    - path: "index.md"
      provides: "Dos nuevos bloques <figure> en §3.2 para bp-messages.png y ldpc-ber-waterfall.png"
      contains: "bp-messages.png"
  key_links:
    - from: "lab.ipynb Cell 8"
      to: "lab.ipynb Cell 9 y Cell 10"
      via: "variables H12, H34, var_nbrs12, chk_nbrs12, var_nbrs34, chk_nbrs34, Rc12, Rc34, bp_awgn, bp_awgn_track"
    - from: "lab.ipynb Cell 10"
      to: "figures/ldpc-ber-waterfall.png"
      via: "plt.savefig('figures/ldpc-ber-waterfall.png', dpi=150, bbox_inches='tight')"
    - from: "index.md §3.2 línea 166"
      to: "figures/bp-messages.png y figures/ldpc-ber-waterfall.png"
      via: "<figure markdown='span'> blocks insertados"
---

<objective>
Este plan implementa el decodificador BP sum-product completo (LAB-01) sobre el código Gallager n=240, genera las figuras bp-messages.png (FIG-05) y ldpc-ber-waterfall.png (FIG-08), e inserta los bloques de figura en index.md §3.2.

Purpose: El núcleo pedagógico de la Fase 3 — el decodificador BP realista sobre un código de longitud de producción (n=240) con Monte Carlo BER. Sin este plan, la sesión no tiene demostración empírica del waterfall cliff ni de la evolución de creencias del algoritmo BP.

Output:
- 3 nuevas celdas en lab.ipynb (índices 8, 9, 10 tras la inserción)
- figures/bp-messages.png — convergencia de LLRs en iteraciones 1, 3, 10
- figures/ldpc-ber-waterfall.png — curvas BER Monte Carlo para r=1/2 y r=3/4
- index.md §3.2 — dos nuevos bloques <figure> con figcaptions en español
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-ldpc-lab-figuras/RESEARCH.md
@.planning/phases/03-ldpc-lab-figuras/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Insertar 3 nuevas celdas en lab.ipynb (Cell 8, 9, 10)</name>
  <files>lab.ipynb</files>
  <action>
Leer lab.ipynb completo. El notebook tiene 15 celdas (índices 0–14) después del Plan 03-01. Insertar 3 nuevas celdas entre el índice actual 7 (Cell 7 modificada) y el índice actual 8 (celda markdown de Ejercicio 4 — Polar). Tras la inserción el notebook tendrá 18 celdas (0–17).

**Nota sobre conteo de celdas:** RESEARCH.md indica "19 total cells (0–18)" en su tabla de layout — esto es un error en RESEARCH.md. El cálculo correcto es 15 (existentes) + 3 (nuevas) = 18 celdas (índices 0–17). El assert de verificación usa 18, que es el valor autoritativo.

**Estructura de inserción:**
- Nueva celda en posición 8 → "Cell 7B": construcción Gallager + bp_awgn + bp_awgn_track
- Nueva celda en posición 9 → "Cell 7C": bp-messages.png
- Nueva celda en posición 10 → "Cell 7D": Monte Carlo BER + ldpc-ber-waterfall.png

Leer el JSON de lab.ipynb, localizar el array `"cells"`, y splicearlo:
`cells = cells[:8] + [new_cell_8, new_cell_9, new_cell_10] + cells[8:]`

Las nuevas celdas son de tipo `"code"` con `"cell_type": "code"`, `"outputs": []`, `"execution_count": null`.

---

**CELDA 8 — Gallager LDPC + BP decoder (Cell 7B)**

`"source"` de la celda (lista de strings con `\n` al final de cada línea excepto la última):

```python
# ── Ejercicio 3 (cont.) — LDPC realista: código Gallager n=240 ───────────
# Código de longitud n=240, tasa ~1/2 y ~3/4, grado de variable d_v=3
# Construido con la matriz de Gallager (apilamiento de submatrices permutadas)

def gallager_ldpc(n, d_v, d_c, seed=2024):
    """
    Matriz de paridad LDPC regular de Gallager.
    Requiere: n * d_v % d_c == 0  Y  (n * d_v // d_c) % d_v == 0
    Devuelve H de forma (n*d_v//d_c, n)
    """
    rng = np.random.default_rng(seed)
    m = n * d_v // d_c
    sub_m = m // d_v          # filas por submatriz
    # Primera submatriz: d_c columnas consecutivas por fila
    H0 = np.zeros((sub_m, n), dtype=int)
    for i in range(sub_m):
        for j in range(i * d_c, (i+1) * d_c):
            H0[i, j % n] = 1
    # Apilar d_v submatrices, cada una es una permutación de columnas de H0
    rows = [H0]
    for _ in range(d_v - 1):
        perm = rng.permutation(n)
        rows.append(H0[:, perm])
    return np.vstack(rows)

def gf2_rank(M):
    """Rango de M en GF(2) por eliminación de Gauss."""
    M = M.copy() % 2
    rows, cols = M.shape
    pivot_row = 0
    for col in range(cols):
        if pivot_row >= rows:
            break
        pivot = np.where(M[pivot_row:, col] == 1)[0]
        if len(pivot) == 0:
            continue
        pivot += pivot_row
        M[[pivot_row, pivot[0]]] = M[[pivot[0], pivot_row]]
        for r in range(rows):
            if r != pivot_row and M[r, col] == 1:
                M[r] ^= M[pivot_row]
        pivot_row += 1
    return pivot_row

# Construir los dos códigos
H12 = gallager_ldpc(240, d_v=3, d_c=6,  seed=2024)   # tasa ~1/2
H34 = gallager_ldpc(240, d_v=3, d_c=12, seed=2024)   # tasa ~3/4

k12 = 240 - gf2_rank(H12)   # ≈ 122
k34 = 240 - gf2_rank(H34)   # ≈ 182
Rc12, Rc34 = k12 / 240, k34 / 240
print(f"H12: {H12.shape}, k={k12}, tasa={Rc12:.3f}")
print(f"H34: {H34.shape}, k={k34}, tasa={Rc34:.3f}")

# Tablas de vecinos (calcular una vez)
var_nbrs12 = [np.where(H12[:, j] == 1)[0] for j in range(240)]
chk_nbrs12 = [np.where(H12[i, :] == 1)[0] for i in range(H12.shape[0])]
var_nbrs34 = [np.where(H34[:, j] == 1)[0] for j in range(240)]
chk_nbrs34 = [np.where(H34[i, :] == 1)[0] for i in range(H34.shape[0])]

def bp_awgn(H, llr_ch, var_nbrs, chk_nbrs, max_iter=30):
    """
    Decodificador BP sum-product para BPSK AWGN.
    H:         matriz de paridad (m x n)
    llr_ch:    LLRs del canal, shape (n,) = 2*y/sigma^2
    var_nbrs:  lista longitud n; var_nbrs[j] = índices de check nodes adyacentes a v_j
    chk_nbrs:  lista longitud m; chk_nbrs[i] = índices de variable nodes adyacentes a c_i
    Devuelve:  (c_hat, n_iters_used)
    """
    m, n = H.shape
    # Inicializar mensajes v→c con LLR del canal
    L_vc = np.zeros((m, n))
    for i in range(m):
        L_vc[i, chk_nbrs[i]] = llr_ch[chk_nbrs[i]]
    L_cv = np.zeros((m, n))

    for it in range(max_iter):
        # ── Actualización c→v (regla tanh / sum-product) ─────────────────
        for i in range(m):
            nbrs = chk_nbrs[i]
            tanh_vals = np.tanh(np.clip(L_vc[i, nbrs] / 2, -20, 20))
            prod_all = np.prod(tanh_vals)
            for idx, j in enumerate(nbrs):
                # excluir v_j del producto
                t_excl = prod_all / (tanh_vals[idx] + 1e-300)
                t_excl = np.clip(t_excl, -1 + 1e-12, 1 - 1e-12)
                L_cv[i, j] = 2 * np.arctanh(t_excl)

        # ── Actualización v→c ─────────────────────────────────────────────
        for j in range(n):
            nbrs = var_nbrs[j]
            total = llr_ch[j] + np.sum(L_cv[nbrs, j])
            L_vc[nbrs, j] = total - L_cv[nbrs, j]

        # ── Decisión dura y verificación de síndrome ──────────────────────
        L_total = np.array([
            llr_ch[j] + np.sum(L_cv[var_nbrs[j], j]) for j in range(n)
        ])
        c_hat = (L_total < 0).astype(int)
        if np.all(H @ c_hat % 2 == 0):
            return c_hat, it + 1   # convergió

    return c_hat, max_iter   # no convergió

def bp_awgn_track(H, llr_ch, var_nbrs, chk_nbrs, max_iter=30, track_iters=(1, 3, 10)):
    """Como bp_awgn pero registra L_total en las iteraciones especificadas."""
    m, n = H.shape
    L_vc = np.zeros((m, n))
    for i in range(m):
        L_vc[i, chk_nbrs[i]] = llr_ch[chk_nbrs[i]]
    L_cv = np.zeros((m, n))
    history = {}

    for it in range(max_iter):
        for i in range(m):
            nbrs = chk_nbrs[i]
            tanh_vals = np.tanh(np.clip(L_vc[i, nbrs] / 2, -20, 20))
            prod_all = np.prod(tanh_vals)
            for idx, j in enumerate(nbrs):
                t_excl = prod_all / (tanh_vals[idx] + 1e-300)
                t_excl = np.clip(t_excl, -1 + 1e-12, 1 - 1e-12)
                L_cv[i, j] = 2 * np.arctanh(t_excl)
        for j in range(n):
            nbrs = var_nbrs[j]
            total = llr_ch[j] + np.sum(L_cv[nbrs, j])
            L_vc[nbrs, j] = total - L_cv[nbrs, j]
        L_total = np.array([llr_ch[j] + np.sum(L_cv[var_nbrs[j], j]) for j in range(n)])
        if (it + 1) in track_iters:
            history[it + 1] = L_total.copy()
        c_hat = (L_total < 0).astype(int)
        if np.all(H @ c_hat % 2 == 0):
            for k in track_iters:
                if k not in history:
                    history[k] = L_total.copy()
            return c_hat, it + 1, history

    for k in track_iters:
        if k not in history:
            history[k] = L_total.copy()
    return c_hat, max_iter, history

# Verificación rápida: convergencia a 5 dB
rng_test = np.random.default_rng(0)
EbN0_test = 5.0
sigma2_test = 1.0 / (2 * Rc12 * 10**(EbN0_test/10))
y_test = np.ones(240) + np.sqrt(sigma2_test) * rng_test.standard_normal(240)
_, iters_test = bp_awgn(H12, 2*y_test/sigma2_test, var_nbrs12, chk_nbrs12)
print(f"Verificación BP a {EbN0_test} dB: convergió en {iters_test} iteraciones (esperado <15)")
assert iters_test < 15, f"BP tardó {iters_test} iter — revisar sigma²"
```

---

**CELDA 9 — bp-messages.png (Cell 7C)**

```python
# ── bp-messages.png — FIG-05: evolución de LLR marginales ────────────────
rng_fig = np.random.default_rng(137)   # semilla fija para figura reproducible
EbN0_track_dB = 2.5
EbN0_track_lin = 10**(EbN0_track_dB / 10)
sigma2_track = 1.0 / (2 * Rc12 * EbN0_track_lin)
sigma_track = np.sqrt(sigma2_track)

c_track = np.zeros(240, dtype=int)
bpsk_track = np.ones(240)
y_track = bpsk_track + sigma_track * rng_fig.standard_normal(240)
llr_track = 2 * y_track / sigma2_track

_, _, history = bp_awgn_track(H12, llr_track, var_nbrs12, chk_nbrs12,
                               max_iter=30, track_iters=(1, 3, 10))

fig, axes = plt.subplots(1, 3, figsize=(12, 4), sharey=True)  # excepción al estándar (10,5): ancho extra necesario para 3 paneles horizontales
bins = np.linspace(-25, 25, 50)
for ax, it_num in zip(axes, [1, 3, 10]):
    L = history[it_num]
    ax.hist(L, bins=bins, color='steelblue', edgecolor='white', linewidth=0.3)
    ax.axvline(0, color='darkorange', ls='--', lw=1.5, label='Umbral de decisión')
    ax.set_title(f'Iteración {it_num}', fontsize=11)
    ax.set_xlabel('LLR marginal $\\lambda_v^{(\\mathrm{total})}$')
    n_errors = np.sum((L < 0) != c_track)
    ax.text(0.97, 0.97, f'{n_errors} errores', transform=ax.transAxes,
            ha='right', va='top', fontsize=9, color='firebrick')
axes[0].set_ylabel('Número de bits')
axes[1].set_title(f'Iteración 3  (Eb/N₀ = {EbN0_track_dB} dB)')
fig.suptitle('Convergencia de BP: evolución de LLR marginales — código LDPC n=240, r=1/2',
             fontsize=11, y=1.01)
plt.tight_layout()
plt.savefig('figures/bp-messages.png', dpi=150, bbox_inches='tight')
plt.show()
```

---

**CELDA 10 — Monte Carlo BER + ldpc-ber-waterfall.png (Cell 7D)**

```python
# ── ldpc-ber-waterfall.png — FIG-08: curvas BER Monte Carlo ──────────────
# NOTA: esta celda tarda ~40 segundos en total (n_blocks=200, n=240)

rng_mc = np.random.default_rng(42)

def run_mc_ber(H, var_nbrs, chk_nbrs, Rc, EbN0_dB_arr, n_blocks=200, max_iter=30):
    n = H.shape[1]
    floor = 1.0 / (n_blocks * n)
    c_test = np.zeros(n, dtype=int)
    bpsk = np.ones(n)
    BER = []
    for EbN0_dB in EbN0_dB_arr:
        EbN0_lin = 10**(EbN0_dB / 10)
        sigma2 = 1.0 / (2 * Rc * EbN0_lin)
        sigma = np.sqrt(sigma2)
        bit_errors = 0
        for _ in range(n_blocks):
            y = bpsk + sigma * rng_mc.standard_normal(n)
            c_hat, _ = bp_awgn(H, 2*y/sigma2, var_nbrs, chk_nbrs, max_iter)
            bit_errors += np.sum(c_hat != c_test)
        BER.append(max(bit_errors / (n_blocks * n), floor))
    return np.array(BER)

EbN0_12 = np.arange(0.0, 5.5, 0.5)
EbN0_34 = np.arange(2.5, 8.0, 0.5)

print("Simulando curva BER, tasa 1/2 (puede tardar ~25 s)...")
BER12 = run_mc_ber(H12, var_nbrs12, chk_nbrs12, Rc12, EbN0_12, n_blocks=200)
print("Simulando curva BER, tasa 3/4 (puede tardar ~20 s)...")
BER34 = run_mc_ber(H34, var_nbrs34, chk_nbrs34, Rc34, EbN0_34, n_blocks=200)

# Verificar waterfall visible
ratio = BER12[0] / BER12[-1]
print(f"BER tasa 1/2: {BER12[0]:.2e} → {BER12[-1]:.2e}, ratio={ratio:.0f}x")
assert ratio > 1000, f"Waterfall no visible: ratio={ratio:.0f} (esperado >1000)"

# ── Figura ────────────────────────────────────────────────────────────────
EbN0_plot = np.linspace(-1, 9, 300)
BER_bpsk = 0.5 * erfc(np.sqrt(10**(EbN0_plot/10)))

fig, ax = plt.subplots(figsize=(10, 5))
ax.semilogy(EbN0_plot, BER_bpsk, 'k-', lw=2, label='BPSK sin código')
ax.semilogy(EbN0_12, BER12, 'o-', color='steelblue', lw=2, ms=5,
            label=f'LDPC $r_c\\approx{Rc12:.2f}$ (n=240, Monte Carlo)')
ax.semilogy(EbN0_34, BER34, 's--', color='darkorange', lw=2, ms=5,
            label=f'LDPC $r_c\\approx{Rc34:.2f}$ (n=240, Monte Carlo)')

# Límites de Shannon por tasa
for r, color in [(Rc12, 'steelblue'), (Rc34, 'darkorange')]:
    ebn0_sh = 10*np.log10((2**r - 1)/r)
    ax.axvline(ebn0_sh, color=color, ls=':', lw=0.9, alpha=0.6)
    ax.text(ebn0_sh + 0.05, 2e-1, f'$C(r={r:.2f})$', fontsize=8,
            color=color, rotation=90, va='top')

ax.set_xlabel('$E_b/N_0$ (dB)')
ax.set_ylabel('BER')
ax.set_title('Curvas waterfall LDPC — Monte Carlo (n=240, BP sum-product)')
ax.set_xlim(-1, 9)
ax.set_ylim(1e-5, 1.1)
ax.legend(fontsize=9, loc='lower left')
plt.tight_layout()
plt.savefig('figures/ldpc-ber-waterfall.png', dpi=150, bbox_inches='tight')
plt.show()
```

---

**Método de edición del notebook:**
Leer lab.ipynb como JSON. En el array `"cells"`, insertar las 3 nuevas celdas entre cells[7] y cells[8] (la celda markdown de Ejercicio 4). Cada nueva celda tiene la estructura:

```json
{
  "cell_type": "code",
  "execution_count": null,
  "id": "cell-7b",
  "metadata": {},
  "outputs": [],
  "source": [
    "... líneas de código ..."
  ]
}
```

IDs sugeridos: "cell-7b", "cell-7c", "cell-7d". Usar Write para guardar el JSON completo. Verificar que `len(nb['cells']) == 18` tras la inserción.

**CRÍTICO — sigma formula:** En run_mc_ber usar `sigma2 = 1.0 / (2 * Rc * EbN0_lin)` — NO `sigma2 = Rc / (2 * EbN0_lin)`. La fórmula incorrecta da ruido ~Rc² veces menor, produciendo un waterfall artificialmente 6 dB optimista. Ver RESEARCH.md §Common Pitfalls, Pitfall 1.

**CRÍTICO — no eliminar simulate_ldpc_bler:** Esa función no existe en el notebook actual (fue eliminada en Phase 2). No hay que preocuparse por ella. Solo usar run_mc_ber definida en esta celda.
  </action>
  <verify>
    <automated>cd /home/researcher/Teaching/uni/2026/wireless-communication-systems/docs/sessions/04-channel-coding && python3 -c "
import json
with open('lab.ipynb') as f:
    nb = json.load(f)
assert len(nb['cells']) == 18, f'Esperado 18 celdas, hay {len(nb[\"cells\"])}'
src8 = ''.join(nb['cells'][8]['source'])
src9 = ''.join(nb['cells'][9]['source'])
src10 = ''.join(nb['cells'][10]['source'])
assert 'gallager_ldpc' in src8, 'gallager_ldpc no está en cell 8'
assert 'bp_awgn' in src8, 'bp_awgn no está en cell 8'
assert 'bp-messages.png' in src9, 'bp-messages.png no está en cell 9'
assert 'ldpc-ber-waterfall.png' in src10, 'waterfall no está en cell 10'
assert '1.0 / (2 * Rc' in src10, 'sigma formula incorrecta en cell 10'
print('Estructura de celdas OK')
"
    </automated>
  </verify>
  <done>lab.ipynb tiene 18 celdas. Cell 8 contiene gallager_ldpc y bp_awgn. Cell 9 genera bp-messages.png. Cell 10 usa sigma2=1/(2*Rc*EbN0) y genera ldpc-ber-waterfall.png. La verificación Python no lanza AssertionError.</done>
</task>

<task type="auto">
  <name>Tarea 2: Ejecutar celdas 8–10 y actualizar index.md §3.2</name>
  <files>index.md</files>
  <action>
**Parte A — Ejecutar celdas 8, 9 y 10 del notebook:**

Las tres nuevas celdas dependen del estado del kernel (Cell 1 imports, Cell 8 define funciones usadas en 9 y 10). Ejecutar en secuencia correcta. Método preferido:

```bash
cd /home/researcher/Teaching/uni/2026/wireless-communication-systems/docs/sessions/04-channel-coding
jupyter nbconvert --to notebook --execute --inplace \
  --ExecutePreprocessor.kernel_name=python3 \
  --ExecutePreprocessor.timeout=300 \
  lab.ipynb
```

El timeout de 300 s es necesario porque la simulación Monte Carlo tarda ~40 s. Si la ejecución falla en celdas de Polar (Cell 11+), ignorar — las nuevas figuras ya estarán generadas porque vienen antes.

Si jupyter nbconvert no está disponible, ejecutar como script standalone extrayendo el código de las 3 celdas y las dependencias (Cell 1 + Cell 8). El script debe:
1. Importar numpy, matplotlib, scipy.special.erfc
2. Definir gallager_ldpc, gf2_rank, bp_awgn, bp_awgn_track, run_mc_ber
3. Ejecutar las lógicas de Cell 9 (bp-messages) y Cell 10 (waterfall) con el mismo código exacto

Verificar que ambas figuras existen y tienen tamaño razonable (>50 KB cada una) antes de continuar con Parte B.

**Parte B — Actualizar index.md §3.2 (insertar 2 bloques `<figure>`):**

Leer index.md. Las inserciones van en §3.2 (Belief Propagation), después de la línea que termina con "cae precipitosamente." (actualmente línea 166, que termina con el texto "...y la BER cae precipitosamente.") y antes del párrafo "La pregunta natural es:" (línea 168).

Insertar el siguiente bloque completo entre esas dos líneas. El bloque incluye bp-messages.png Y ldpc-ber-waterfall.png, separados por una línea en blanco:

```markdown

<figure markdown="span">
  ![Evolución de LLR en belief propagation](figures/bp-messages.png)
  <!-- generada por celda 9 de lab.ipynb -->
  <figcaption markdown="1">**Figura 4.** Evolución de los LLR marginales $\lambda_v^{(\text{total})}$ durante el algoritmo de belief propagation sobre un código LDPC con $n=240$, $r_c\approx1/2$, a $E_b/N_0=2{,}5\ \text{dB}$ (zona de transición del waterfall).
  Iteración 1: los LLR reflejan principalmente la información del canal, concentrados cerca de cero. Iteración 3: la propagación de mensajes entre vecinos comienza a polarizar las creencias. Iteración 10: las creencias convergen hacia valores de gran magnitud (bits con alta certeza), con muy pocos bits en la zona de incertidumbre ($|\lambda|<2$).
  </figcaption>
</figure>

<figure markdown="span">
  ![Curvas BER Monte Carlo LDPC n=240](figures/ldpc-ber-waterfall.png)
  <!-- generada por celda 10 de lab.ipynb -->
  <figcaption markdown="1">**Figura 5.** Curvas BER Monte Carlo para el código LDPC de $n=240$ bits con tasas $r_c\approx1/2$ (azul) y $r_c\approx3/4$ (naranja), comparadas con BPSK sin código (negro). Simulación BP sum-product con 200 bloques por punto de SNR; las líneas verticales punteadas marcan el límite teórico de Shannon para cada tasa.
  La "cascada" (*waterfall cliff*) es visible: la BER cae más de 3 décadas en menos de 2 dB por encima del umbral de decodificación, a diferencia de la caída gradual de BPSK sin código.
  </figcaption>
</figure>

```

Usar Edit tool para insertar este bloque. El punto de inserción exacto es: después del final de la línea 166 (que termina con "...y la BER cae precipitosamente.") y antes de la línea con "La pregunta natural es:".

**Verificar compatibilidad MkDocs-Material:** Los bloques `<figure markdown="span">` con `<figcaption markdown="1">` son la sintaxis estándar usada en las otras figuras del archivo (ver líneas 131–137 para referencia de formato). No usar sintaxis alternativa.
  </action>
  <verify>
    <automated>cd /home/researcher/Teaching/uni/2026/wireless-communication-systems/docs/sessions/04-channel-coding && python3 -c "
import os
for fname, min_size in [('bp-messages.png', 50000), ('ldpc-ber-waterfall.png', 50000)]:
    sz = os.path.getsize(f'figures/{fname}')
    assert sz > min_size, f'{fname}: {sz} bytes (esperado >{min_size})'
    print(f'{fname}: {sz} bytes OK')
" && python3 -c "
txt = open('index.md').read()
assert 'bp-messages.png' in txt, 'bp-messages.png no está en index.md'
assert 'ldpc-ber-waterfall.png' in txt, 'ldpc-ber-waterfall.png no está en index.md'
assert 'celda 9 de lab.ipynb' in txt, 'comentario celda 9 no encontrado'
assert 'celda 10 de lab.ipynb' in txt, 'comentario celda 10 no encontrado'
assert txt.count('<figure markdown=\"span\">') >= 4, 'faltan bloques figure'
print('index.md OK')
"
    </automated>
  </verify>
  <done>figures/bp-messages.png y figures/ldpc-ber-waterfall.png existen con >50 KB cada una. index.md contiene los dos nuevos bloques `<figure markdown="span">` con referencias a celdas 9 y 10 del notebook, insertados en §3.2 antes de "La pregunta natural es:".</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 03-02 completo: decodificador BP sum-product sobre código Gallager n=240, figuras bp-messages.png y ldpc-ber-waterfall.png generadas, index.md §3.2 actualizado con dos nuevos bloques de figura.
  </what-built>
  <how-to-verify>
    1. Abrir figures/ldpc-ber-waterfall.png — debe mostrar:
       - Curva BPSK sin código (negra, caída gradual)
       - Curva LDPC r≈0.51 (azul con círculos), con caída pronunciada entre 1.5 y 4 dB
       - Curva LDPC r≈0.76 (naranja con cuadrados), con caída entre 3 y 6 dB
       - Dos líneas verticales punteadas (límites de Shannon por tasa)
       - La BER de la curva azul debe caer de ~0.1 a ~1e-4 en el rango visible (waterfall cliff)

    2. Abrir figures/bp-messages.png — debe mostrar:
       - 3 histogramas de barras azul acero en una fila (Iteración 1, 3, 10)
       - Iteración 1: histograma centrado cerca de cero, forma unimodal
       - Iteración 10: histograma con barras en los extremos (bimodal o muy polarizado)
       - Línea vertical naranja punteada (umbral de decisión en 0) en cada subplot
       - Texto "N errores" en esquina superior derecha de cada subplot

    3. Abrir index.md y buscar §3.2 — debe contener ambos bloques `<figure markdown="span">` con bp-messages.png y ldpc-ber-waterfall.png, ubicados después de la descripción del algoritmo BP y antes de "La pregunta natural es:".

    4. Ejecutar en terminal desde el directorio del proyecto:
       ```bash
       python3 -c "
       import json, os
       nb = json.load(open('lab.ipynb'))
       print(f'Total celdas: {len(nb[\"cells\"])}')  # debe ser 18
       src8 = ''.join(nb['cells'][8]['source'])
       print('gallager_ldpc en cell 8:', 'gallager_ldpc' in src8)
       print('bp-messages.png:', os.path.getsize('figures/bp-messages.png'), 'bytes')
       print('ldpc-ber-waterfall.png:', os.path.getsize('figures/ldpc-ber-waterfall.png'), 'bytes')
       "
       ```
  </how-to-verify>
  <resume-signal>Escribe "aprobado" si las figuras son correctas. Describe cualquier problema visual (p. ej. "waterfall no visible", "histograma plano", "figuras vacías").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notebook execution → figures/ | Código Python genera figuras locales; sin input externo no confiable |
| Monte Carlo RNG | Semilla fija (rng_mc=42, rng_fig=137) — resultados reproducibles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | sigma² formula en run_mc_ber | mitigate | Usar `1/(2*Rc*EbN0_lin)` explícito y assert en Cell 10; el assert `ratio > 1000` detecta la fórmula incorrecta que produce waterfall falso |
| T-03-04 | Denial of Service | Monte Carlo timeout (>300 s) | mitigate | n_blocks=200 verificado a ~41 s total; timeout explícito de 300 s en nbconvert |
| T-03-05 | Information Disclosure | NaN/Inf en bp_awgn por arctanh sin clip | mitigate | np.clip antes de arctanh (a ±(1-1e-12)) y antes de tanh (a ±20); verificado sin NaN en simulación |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No hay instalaciones de paquetes; numpy+matplotlib+scipy ya disponibles en Cell 1 |
</threat_model>

<verification>
Verificación global de Phase 3 Plan 02:

```bash
cd /home/researcher/Teaching/uni/2026/wireless-communication-systems/docs/sessions/04-channel-coding

# 1. Estructura del notebook (18 celdas)
python3 -c "
import json
nb = json.load(open('lab.ipynb'))
assert len(nb['cells']) == 18, f'{len(nb[\"cells\"])} celdas (esperado 18)'
print(f'Notebook: {len(nb[\"cells\"])} celdas OK')
"

# 2. Figuras existentes y no triviales
python3 -c "
import os
for f, min_b in [('bp-messages.png', 50000), ('ldpc-ber-waterfall.png', 50000), ('tanner-graph.png', 30000)]:
    sz = os.path.getsize(f'figures/{f}')
    assert sz > min_b, f'{f}: {sz} bytes'
    print(f'{f}: {sz} bytes OK')
"

# 3. index.md contiene los 4 bloques de figura de Phase 3
python3 -c "
txt = open('index.md').read()
for s in ['tanner-graph.png', 'bp-messages.png', 'ldpc-ber-waterfall.png',
          'celda 7 de lab.ipynb', 'celda 9 de lab.ipynb', 'celda 10 de lab.ipynb']:
    assert s in txt, f'Falta: {s}'
    print(f'Encontrado: {s}')
assert 'Esta figura se generará en la Fase 3' not in txt
print('index.md OK')
"

# 4. Fórmula sigma² correcta en las nuevas celdas
python3 -c "
import json
nb = json.load(open('lab.ipynb'))
src10 = ''.join(nb['cells'][10]['source'])
assert '1.0 / (2 * Rc' in src10, 'sigma formula incorrecta'
print('sigma formula OK')
"

# 5. Criterio LAB-01: convergencia <15 iters a 5 dB (se valida por output de Cell 8)
python3 -c "
import json
nb = json.load(open('lab.ipynb'))
# Verificar que Cell 8 tiene el assert
src8 = ''.join(nb['cells'][8]['source'])
assert 'assert iters_test < 15' in src8, 'assert de convergencia no encontrado'
print('Assert LAB-01 OK')
"
```
</verification>

<success_criteria>
1. LAB-01: bp_awgn converge en <15 iteraciones a Eb/N0=5 dB con código tasa 1/2 (verificado por assert en Cell 8)
2. FIG-05: figures/bp-messages.png existe con >50 KB — 3 histogramas de LLR para iteraciones 1, 3, 10, mostrando polarización progresiva de las creencias
3. FIG-08: figures/ldpc-ber-waterfall.png existe con >50 KB — curvas BER para r≈1/2 y r≈3/4, waterfall cliff visible (ratio BER_min/BER_max > 1000 para tasa 1/2)
4. index.md §3.2 contiene dos nuevos bloques `<figure markdown="span">` referenciando bp-messages.png (celda 9) y ldpc-ber-waterfall.png (celda 10), ubicados antes de "La pregunta natural es:"
5. lab.ipynb tiene 18 celdas totales; las celdas de Polar (antiguo índice 8+) siguen en posiciones correctas sin modificaciones
</success_criteria>

<output>
Crear `.planning/phases/03-ldpc-lab-figuras/03-02-SUMMARY.md` al terminar con:
- Tamaños finales de figures/bp-messages.png y figures/ldpc-ber-waterfall.png
- BER12[0] y BER12[-1] (primer y último punto de la curva tasa 1/2) para verificar waterfall
- Número de iteraciones del test de convergencia a 5 dB
- Líneas de index.md modificadas (números de línea antes y después)
- Confirmación visual del checkpoint (descripción del waterfall cliff observado)
</output>
