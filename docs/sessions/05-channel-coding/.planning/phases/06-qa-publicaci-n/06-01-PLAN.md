---
phase: 06-qa-publicaci-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/sessions/05-channel-coding/lab.ipynb
  - docs/sessions/05-channel-coding/figures/waterfall-curves.png
  - docs/sessions/05-channel-coding/figures/polar-polarization.png
  - docs/sessions/05-channel-coding/figures/ofdm-ldpc-ber.png
autonomous: true
requirements: [LAB-04]

must_haves:
  truths:
    - "jupyter nbconvert --to notebook --execute lab.ipynb completes with exit code 0 (no cell raises)"
    - "Every code cell has a non-null execution_count after the clean run"
    - "No code cell has an output of type 'error' after the clean run"
    - "A code cell generates figures/waterfall-curves.png (no longer an orphan PNG)"
    - "polar-polarization.png (FIG-07) is still generated by the notebook"
  artifacts:
    - path: "docs/sessions/05-channel-coding/lab.ipynb"
      provides: "Notebook that executes top-to-bottom without errors"
      contains: "savefig('figures/waterfall-curves.png'"
    - path: "docs/sessions/05-channel-coding/figures/waterfall-curves.png"
      provides: "Comparative LDPC/Polar/BPSK waterfall, now notebook-generated"
    - path: "docs/sessions/05-channel-coding/figures/polar-polarization.png"
      provides: "FIG-07 polarization histogram, still generated post-fix"
  key_links:
    - from: "lab.ipynb cell 17"
      to: "polar-polarization.png"
      via: "savefig before any NotImplementedError path"
      pattern: "polar-polarization\\.png"
    - from: "lab.ipynb (new waterfall cell)"
      to: "waterfall-curves.png"
      via: "savefig"
      pattern: "waterfall-curves\\.png"
---

<objective>
Hacer que `lab.ipynb` ejecute de principio a fin sin errores con `jupyter nbconvert --to notebook --execute` (requisito LAB-04), eliminando el único crash real (la celda 17 invoca decodificadores Polar SC/SCL deshabilitados que lanzan `NotImplementedError`) y cerrando el hueco de la figura huérfana `waterfall-curves.png`, que el `index.md` referencia pero ninguna celda genera.

Purpose: El notebook es ground truth de todas las figuras publicadas. Si no ejecuta limpio, las figuras no son reproducibles y la sesión no es publicable.
Output: `lab.ipynb` ejecutable end-to-end (21 celdas, todas con execution_count y sin outputs de error), `waterfall-curves.png` regenerada desde una celda real, `polar-polarization.png` y `ofdm-ldpc-ber.png` regeneradas por la corrida limpia.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/sessions/05-channel-coding/CLAUDE.md

<notebook_state>
<!-- Estado verificado del notebook (jun 2026). 21 celdas. -->
<!-- nbconvert --execute FALLA actualmente en la celda 17 (índice 17). -->

Celdas de código y su estado:
[1]  import numpy/scipy/matplotlib + Q(), os.makedirs('figures') — OK
[3]  Ej1 Shannon → savefig('figures/shannon-capacity.png') — OK
[5]  Hamming(7,4) H, síndrome — OK
[7]  LDPC(8,4) → savefig('figures/tanner-graph.png') — OK
[8]  Ej3 LDPC Gallager n=240: define H12, var_nbrs12, chk_nbrs12, Rc12, bp_awgn — OK
[9]  → savefig('figures/bp-messages.png') (FIG-05) — OK
[10] → savefig('figures/ldpc-ber-waterfall.png') (FIG-08) — OK
[12] bhattacharyya_tree(Z0,N), N=8, plot histograma BEC — OK
[14] f_func, g_func, sc_decode_n4 (N=4 explícito), demo SC — OK
[15] build_polar_G(N), polar_encode, G64, Z_polar, info_idx, frozen_set;
     draw_butterfly → savefig('figures/polar-butterfly.png') (FIG-06) — OK
[16] sc_decode_polar / scl_decode_polar = raise NotImplementedError
     (marcada "[DESHABILITADO TEMPORALMENTE]", bug kron, scope Fase 4 diferida) — define stubs
[17] Bloque A: genera savefig('figures/polar-polarization.png') (FIG-07) — OK por sí solo
     Bloque B (MC BER SC vs SCL): llama run_polar_mc(..., sc_decode_polar, ...)
     y (..., scl_decode_polar, ...) → DISPARA NotImplementedError → CRASH nbconvert
[19] Ej6 Integrador OFDM+LDPC: reusa ofdm_tx/apply_channel/ofdm_rx_no_channel/zf_equalizer,
     usa H12/var_nbrs12/chk_nbrs12/Rc12/bp_awgn de celda 8 →
     savefig('figures/ofdm-ldpc-ber.png') (FIG-09) — OK (nunca ejecutada en el .ipynb guardado)

PNG huérfana: figures/waterfall-curves.png existe en disco (Fase 2) pero NINGUNA
celda la genera. index.md la referencia con comentario "generada por celda 13"
(celda 13 es markdown). Cierre de este hueco = parte de LAB-05/LAB-04.
</notebook_state>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eliminar el crash de la celda 17 (decodificadores Polar SC/SCL deshabilitados)</name>
  <files>docs/sessions/05-channel-coding/lab.ipynb</files>
  <read_first>
    - docs/sessions/05-channel-coding/lab.ipynb (leer source completo de celdas índice 16 y 17 antes de tocar nada — confirmar que `sc_decode_polar`/`scl_decode_polar` siguen siendo `raise NotImplementedError` y que la sección "MC BER SC vs SCL" los invoca vía `run_polar_mc`)
  </read_first>
  <action>
    En la celda de código de índice 17 (la que empieza con el comentario `# -- polar-polarization.png (FIG-07) + MC BER: SC vs SCL-8`), CONSERVAR íntegro el Bloque A — desde el inicio de la celda hasta justo después de la línea `print(f'FIG-07 guardada. Z<0.05: ...')` inclusive — que genera y guarda `figures/polar-polarization.png`.

    ELIMINAR de esa misma celda todo el Bloque B "MC BER SC vs SCL-8 (figura auxiliar, no guardada)": las líneas desde `rng_polar = np.random.default_rng(99)` hasta el final de la celda (la definición de `run_polar_mc`, las llamadas `run_polar_mc(..., sc_decode_polar, ...)` y `run_polar_mc(..., scl_fn, ...)`, las variables `BER_SC`/`BER_SCL`/`EbN0_dB_arr`/`BER_bpsk`, y el bloque de `fig, ax = plt.subplots(...)` con `semilogy` de SC/SCL).

    Razón concreta: ese Bloque B invoca `sc_decode_polar` y `scl_decode_polar`, que la celda 16 define como `raise NotImplementedError('SC decoder deshabilitado — ver TODO')`. El decodificador SC/SCL recursivo N=64 era scope de la Fase 4 (LAB-02), que está DIFERIDA. No reimplementar el decodificador en esta fase de QA — eso reintroduciría el bug de convención `kron` documentado en la celda 16 y está fuera del mandato LAB-04. La figura de polarización (FIG-07) no depende del decodificador y debe seguir generándose.

    Tras editar, añadir al final del Bloque A conservado una sola línea de print informativa: `print('SC/SCL N=64 (comparativa BER) diferido — ver Fase 4 / LAB-02')` para que quede traza explícita de la omisión deliberada.

    NO tocar la celda 16: sus stubs `NotImplementedError` pueden permanecer porque, tras este cambio, ninguna celda los invoca durante la ejecución (un `def` que lanza al ser llamado no falla al definirse).
  </action>
  <verify>
    <automated>python3 -c "import json; nb=json.load(open('docs/sessions/05-channel-coding/lab.ipynb')); src=''.join(nb['cells'][17]['source']); assert 'polar-polarization.png' in src, 'FIG-07 perdida'; assert 'run_polar_mc' not in src, 'Bloque B MC BER no eliminado'; assert 'sc_decode_polar' not in src and 'scl_decode_polar' not in src, 'aun invoca decoders deshabilitados'; print('OK cell17 trimmed')"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import json; nb=json.load(open('docs/sessions/05-channel-coding/lab.ipynb')); print('run_polar_mc' in ''.join(nb['cells'][17]['source']))"` imprime `False`
    - `grep -c "polar-polarization.png" docs/sessions/05-channel-coding/lab.ipynb` devuelve un valor ≥ 1 (la celda 17 aún guarda FIG-07)
    - La celda de índice 16 permanece sin cambios (sigue conteniendo `raise NotImplementedError`)
    - El source de la celda 17 ya no contiene los tokens `sc_decode_polar`, `scl_decode_polar`, `BER_SC`, `BER_SCL`
  </acceptance_criteria>
  <done>La celda 17 genera FIG-07 y ya no invoca ningún decodificador deshabilitado; la causa raíz del crash de nbconvert queda eliminada.</done>
</task>

<task type="auto">
  <name>Task 2: Añadir celda generadora de waterfall-curves.png (figura huérfana)</name>
  <files>docs/sessions/05-channel-coding/lab.ipynb</files>
  <read_first>
    - docs/sessions/05-channel-coding/lab.ipynb (leer celda 8 para conocer las variables reutilizables `H12`, `var_nbrs12`, `chk_nbrs12`, `Rc12`, `bp_awgn`; leer celda 10 para replicar el estilo de las curvas BER Monte Carlo LDPC; leer celda 1 para `Q`/`erfc` y rcParams)
    - docs/sessions/05-channel-coding/index.md (líneas 266-272: figcaption de la Figura "waterfall-curves" — la celda generada debe producir exactamente las curvas que la leyenda describe: BPSK sin código en negro, LDPC en azul, Polar en naranja, eje Eb/N0, BER semilog)
  </read_first>
  <action>
    Insertar UNA nueva celda de código en el notebook inmediatamente DESPUÉS de la celda de índice 17 (la del FIG-07 ya recortada) y ANTES de la celda markdown de índice 18. Esta celda debe generar `figures/waterfall-curves.png` — actualmente una PNG huérfana en disco que `index.md` referencia pero ninguna celda produce.

    La celda debe: (1) reutilizar `bp_awgn`, `H12`, `var_nbrs12`, `chk_nbrs12`, `Rc12` ya definidos en la celda 8 para simular Monte Carlo BER del LDPC n=240 a tasa r_c≈1/2 (mismo patrón que la celda 10, ~150-200 bloques por punto de SNR para mantener el tiempo de ejecución bajo); (2) trazar también la curva analítica BPSK sin código usando `Q(np.sqrt(2*EbN0_lin))` o `0.5*erfc(np.sqrt(EbN0_lin))`; (3) añadir una curva Polar de referencia — dado que el decodificador SC/SCL está diferido, usar una curva analítica/aproximada de referencia Polar (por ejemplo, la cota BPSK desplazada por la ganancia de codificación nominal, o reutilizar `Z_polar`/`info_idx` de la celda 15 para una cota de unión P_b ≤ Σ Z sobre Eb/N0) claramente etiquetada como cota teórica, no como simulación SC; (4) colores consistentes con el resto del notebook: BPSK negro, LDPC azul (`steelblue`), Polar naranja (`darkorange`); (5) ejes `$E_b/N_0$ (dB)` vs `BER` en semilogy con `set_ylim(1e-5, 1.1)`; (6) `plt.savefig('figures/waterfall-curves.png', dpi=150, bbox_inches='tight')` seguido de `plt.show()`.

    El comentario de cabecera de la celda debe identificarla: `# -- waterfall-curves.png (FIG-03 / LAB-05): comparativa LDPC vs Polar vs BPSK sin codigo --`.

    NO modificar `index.md` en este plan (eso es el plan 06-02). NO reentrenar/reimplementar el decodificador SC: la curva Polar es una referencia teórica etiquetada, coherente con el alcance de QA.
  </action>
  <verify>
    <automated>python3 -c "import json; nb=json.load(open('docs/sessions/05-channel-coding/lab.ipynb')); srcs=[''.join(c['source']) for c in nb['cells'] if c['cell_type']=='code']; assert any(\"savefig('figures/waterfall-curves.png'\" in s for s in srcs), 'ninguna celda genera waterfall-curves.png'; print('OK waterfall cell present')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "savefig('figures/waterfall-curves.png'" docs/sessions/05-channel-coding/lab.ipynb` devuelve exactamente 1
    - La nueva celda aparece en posición de índice 18 (entre la antigua celda 17 de FIG-07 y la markdown que era índice 18), verificable con `python3 -c "import json; nb=json.load(open('docs/sessions/05-channel-coding/lab.ipynb')); print(nb['cells'][18]['cell_type'])"` → `code`
    - El source de la nueva celda contiene los tokens `bp_awgn`, `steelblue`, `darkorange`, `semilogy` y `set_ylim`
    - La nueva celda NO contiene `sc_decode_polar` ni `scl_decode_polar`
  </acceptance_criteria>
  <done>Existe una celda de código que genera `waterfall-curves.png`; la PNG deja de ser huérfana y queda trazada a una celda real (cierra el hueco LAB-05 que bloquea IDX-04).</done>
</task>

<task type="auto">
  <name>Task 3: Ejecución limpia end-to-end con nbconvert (LAB-04)</name>
  <files>docs/sessions/05-channel-coding/lab.ipynb</files>
  <read_first>
    - docs/sessions/05-channel-coding/lab.ipynb (estado tras Tasks 1-2)
  </read_first>
  <action>
    Ejecutar el notebook completo in-place desde el directorio `docs/sessions/05-channel-coding/` con:
    `jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.timeout=600 lab.ipynb`

    Esto re-ejecuta las 21+1 celdas en orden, normaliza los `execution_count` a una secuencia limpia, regenera todas las figuras (incluidas `polar-polarization.png`, `ofdm-ldpc-ber.png` y la nueva `waterfall-curves.png`) y persiste outputs frescos en el .ipynb.

    Si nbconvert reporta un error en alguna celda: leer el traceback de esa celda, corregir la causa concreta en el source de la celda (variable indefinida, import faltante, ruta), y re-ejecutar. Errores esperados a vigilar: nombres de variable cruzados entre celdas (p.ej. `H12`/`Rc12`/`bp_awgn` de celda 8 usados en celdas 19 y en la nueva celda waterfall) — confirmar que el orden de ejecución los define antes de usarlos. NO silenciar errores con try/except: corregir la causa.

    Verificar el directorio de trabajo: el notebook usa rutas relativas `figures/...`, por lo que nbconvert DEBE correr con cwd = `docs/sessions/05-channel-coding/` para que las PNG caigan en el `figures/` de esta sesión.
  </action>
  <verify>
    <automated>cd docs/sessions/05-channel-coding && jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.timeout=600 lab.ipynb && python3 -c "import json; nb=json.load(open('lab.ipynb')); codes=[c for c in nb['cells'] if c['cell_type']=='code']; assert all(c.get('execution_count') is not None for c in codes), 'celda sin execution_count'; assert not any(o.get('output_type')=='error' for c in codes for o in c.get('outputs',[])), 'output de error presente'; print('OK execute clean:', len(codes), 'code cells')"</automated>
  </verify>
  <acceptance_criteria>
    - El comando `jupyter nbconvert --to notebook --execute --inplace` termina con exit code 0
    - Toda celda de código tiene `execution_count` no nulo (verificado por el assert del comando)
    - Ninguna celda de código tiene un output con `output_type == 'error'`
    - Tras la corrida, existen y tienen mtime reciente: `figures/waterfall-curves.png`, `figures/polar-polarization.png`, `figures/polar-butterfly.png`, `figures/ofdm-ldpc-ber.png`, `figures/ldpc-ber-waterfall.png`, `figures/bp-messages.png`, `figures/tanner-graph.png`, `figures/shannon-capacity.png` (verificable con `ls -la docs/sessions/05-channel-coding/figures/`)
  </acceptance_criteria>
  <done>`jupyter nbconvert --execute lab.ipynb` completa sin errores; todas las figuras generadas por el notebook quedan regeneradas y el notebook persiste con outputs limpios. LAB-04 satisfecho.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ejecución de notebook → filesystem | nbconvert ejecuta código Python arbitrario del notebook y escribe PNGs en `figures/` |
| entorno Python local → notebook | numpy 1.26.4 / scipy 1.15.3 / matplotlib 3.10.8 deben coincidir con las APIs que usa el notebook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | nbconvert escribe en `figures/` | mitigate | cwd fijado a `docs/sessions/05-channel-coding/`; solo se sobrescriben PNGs ya versionadas, ninguna ruta fuera del árbol de la sesión |
| T-06-02 | Denial of Service | celda con loop Monte Carlo (waterfall, Ej6) | mitigate | `ExecutePreprocessor.timeout=600` y conteo de bloques moderado (~150-200) acotan el tiempo de ejecución |
| T-06-03 | Repudiation | figura sin celda generadora (waterfall-curves.png) | mitigate | Task 2 añade celda explícita; toda PNG queda trazable a una celda del ground truth |
| T-06-SC | Tampering | instalación de paquetes npm/pip/cargo | accept | no se instalan paquetes en esta fase; numpy/scipy/matplotlib ya presentes y verificados en el entorno |
</threat_model>

<verification>
- `jupyter nbconvert --to notebook --execute --inplace lab.ipynb` → exit 0, sin celdas en error
- Toda celda de código con `execution_count` no nulo y sin outputs de error
- `figures/waterfall-curves.png` regenerada por una celda real del notebook
- `figures/polar-polarization.png` y `figures/ofdm-ldpc-ber.png` regeneradas por la corrida limpia
</verification>

<success_criteria>
El notebook ejecuta de principio a fin sin errores ni excepciones, todas las figuras que el notebook declara generar existen en `figures/` y son reproducibles, y la PNG `waterfall-curves.png` deja de ser huérfana. El decodificador Polar SC/SCL diferido (Fase 4) no bloquea la ejecución.
</success_criteria>

<output>
Create `.planning/phases/06-qa-publicaci-n/06-01-SUMMARY.md` when done
</output>
