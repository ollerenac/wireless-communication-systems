---
phase: 05-integrador-ofdm-fec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lab.ipynb
  - index.md
autonomous: true
requirements:
  - FIG-09
  - LAB-03

must_haves:
  truths:
    - "figures/ofdm-ldpc-ber.png existe con >40 KB"
    - "lab.ipynb Cell 19 define las 4 funciones OFDM de sesión 03 sin modificación"
    - "lab.ipynb Cell 19 genera ofdm-ldpc-ber.png con 3 curvas: OFDM sin FEC, OFDM+LDPC, límite AWGN"
    - "index.md §5 contiene un bloque <figure markdown='span'> referenciando ofdm-ldpc-ber.png"
  artifacts:
    - path: "figures/ofdm-ldpc-ber.png"
      provides: "Curvas BER: OFDM uncoded vs OFDM+LDPC vs AWGN teórico"
      min_size_bytes: 40000
    - path: "lab.ipynb"
      provides: "Cell 18 actualizada (markdown OFDM+LDPC) + Cell 19 actualizada (código integrador)"
      contains: "ofdm_tx"
    - path: "index.md"
      provides: "Bloque <figure> para FIG-09 en §5"
      contains: "ofdm-ldpc-ber.png"
  key_links:
    - from: "lab.ipynb Cell 19"
      to: "figures/ofdm-ldpc-ber.png"
      via: "plt.savefig('figures/ofdm-ldpc-ber.png', dpi=150, bbox_inches='tight')"
    - from: "index.md §5"
      to: "figures/ofdm-ldpc-ber.png"
      via: "<figure markdown='span'> block"
---

<objective>
Este plan implementa el integrador OFDM+LDPC end-to-end (LAB-03) en Cell 19 del notebook, genera la figura ofdm-ldpc-ber.png (FIG-09), e inserta el bloque de figura en §5 de index.md.

Purpose: Cierre del arco pedagógico del curso — OFDM de la sesión 03 + LDPC de esta sesión = sistema completo frequency-selective. Es el único plan de Phase 5; sin él, la sesión no tiene demostración empírica end-to-end.

Output:
- lab.ipynb Cell 18: markdown actualizado describiendo el ejercicio OFDM+LDPC
- lab.ipynb Cell 19: código integrador con las 4 funciones OFDM de sesión 03 + simulación Monte Carlo
- figures/ofdm-ldpc-ber.png: 3 curvas BER con ganancia de codificación visible
- index.md §5: bloque <figure> cerrando el arco OFDM→FEC
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Actualizar Cell 18 (markdown) y Cell 19 (código integrador OFDM+LDPC)</name>
  <files>lab.ipynb</files>
  <action>
Leer lab.ipynb completo. El notebook tiene 21 celdas (índices 0–20).
- Cell 18 (índice 18): markdown "Ejercicio 6 — Curvas waterfall" — actualizar a OFDM+LDPC
- Cell 19 (índice 19): código placeholder (genera waterfall-curves.png) — reemplazar con integrador
- Cell 20 (índice 20): resumen — actualizar entrada de Ejercicio 6

**CELL 18 — nuevo contenido markdown:**

```markdown
---
## Ejercicio 6 — Integrador OFDM+LDPC: canal frequency-selective

En este ejercicio conectamos el transceptor OFDM de la Sesión 03 con el codec LDPC de esta sesión para construir un sistema de comunicación inalámbrica end-to-end sobre un canal frequency-selective.

El canal de 3 taps $h = [0.8, 0.5, 0.3]$ introduce selectividad en frecuencia que el ecualizador ZF mitiga a costa de amplificar el ruido en los nulos del canal. El objetivo es cuantificar la **ganancia de codificación** que aporta el LDPC sobre este canal real.
```

**CELL 19 — código integrador completo:**

```python
# ── Ejercicio 6 — Integrador OFDM+LDPC: canal frequency-selective ──────────
# Reutilización directa de las 4 funciones OFDM de la sesión 03 (sin modificación)

# ── a) Funciones OFDM de sesión 03 ─────────────────────────────────────────
def ofdm_tx(X, N_CP):
    """IFFT normalizada + prefijo cíclico."""
    x = np.fft.ifft(X, norm='ortho')
    return np.concatenate([x[-N_CP:], x])

def apply_channel(x_signal, h):
    """Convolución lineal del canal multipath."""
    return np.convolve(x_signal, h, mode='full')[:len(x_signal)]

def ofdm_rx_no_channel(x_with_cp, N, N_CP):
    """Eliminar CP + FFT normalizada."""
    return np.fft.fft(x_with_cp[N_CP:], norm='ortho')

def zf_equalizer(Y, h, N):
    """ZF: Y[k] / H[k] — elimina distorsión, amplifica ruido en nulos."""
    return Y / np.fft.fft(h, n=N)

# ── b) Parámetros del sistema ───────────────────────────────────────────────
N_ofdm  = 128                           # subportadoras OFDM
N_CP    = 16                            # prefijo cíclico (>L-1=2)
h_ch    = np.array([0.8, 0.5, 0.3])    # canal 3-tap frequency-selective
H_k_ch  = np.fft.fft(h_ch, n=N_ofdm)  # respuesta frecuencial del canal
# Clamp mínimo en |H[k]|² para evitar div/0 en nulos (no ocurre con este canal)
H_k_mag2_full = np.maximum(np.abs(H_k_ch)**2, 1e-9)

# LDPC: usar H12, var_nbrs12, chk_nbrs12, Rc12, bp_awgn de Cell 8
# 240 bits / 2 bits_QPSK = 120 subportadoras → caben en 1 trama con N_ofdm=128
n_ldpc_ex6 = H12.shape[1]            # = 240 bits
n_active   = n_ldpc_ex6 // 2         # = 120 subportadoras activas para LDPC
H_k_mag2   = H_k_mag2_full[:n_active]

# ── c) QPSK helpers ────────────────────────────────────────────────────────
def qpsk_map_ex6(bits):
    """2 bits → símbolo QPSK Gray (|s|²=1): b0=bit_I, b1=bit_Q."""
    b0 = bits[0::2].astype(float)
    b1 = bits[1::2].astype(float)
    return ((1 - 2*b0) + 1j*(1 - 2*b1)) / np.sqrt(2)

def qpsk_hard_ex6(X_eq):
    """Decisión dura QPSK: Re<0 → b_I=1, Im<0 → b_Q=1."""
    b = np.zeros(2*len(X_eq), dtype=int)
    b[0::2] = (X_eq.real < 0).astype(int)
    b[1::2] = (X_eq.imag < 0).astype(int)
    return b

def qpsk_llr_ex6(X_eq, sigma2_noise, H_k_mag2_active):
    """
    LLR por bit para QPSK después de ecualizador ZF sobre canal OFDM.

    Tras ZF, el ruido efectivo en subportadora k es CN(0, σ²_noise/|H[k]|²).
    Para QPSK Gray (s_I = ±1/√2), la LLR del bit_I es:
        LLR_I[k] = 2√2 · Re(X_eq[k]) · |H[k]|² / σ²_noise

    Args:
        X_eq             : señales ecualizadas ZF, shape (n_active,)
        sigma2_noise     : varianza compleja del ruido antes de ZF (N0)
        H_k_mag2_active  : |H[k]|² para k = 0..n_active-1

    Returns:
        llr : 2·n_active LLRs intercalados [I0, Q0, I1, Q1, ...]
              Positivo → bit probablemente 0 (convenio igual que bp_awgn)
    """
    scale = 2.0 * np.sqrt(2) * H_k_mag2_active / sigma2_noise
    llr = np.zeros(2 * len(X_eq))
    llr[0::2] = scale * X_eq.real   # bits I (b0)
    llr[1::2] = scale * X_eq.imag   # bits Q (b1)
    return llr

# ── d) Simulación Monte Carlo ───────────────────────────────────────────────
rng_ex6   = np.random.default_rng(2025)
n_sim_ex6 = 300                         # tramas Monte Carlo por punto de SNR
EbN0_ex6  = np.arange(0, 10.5, 0.5)    # 21 puntos de SNR
c_zeros   = np.zeros(n_ldpc_ex6, dtype=int)   # codeword válida: H12 @ 0 = 0 (mod 2)

ber_ofdm_unc = []
ber_ofdm_cod = []

print("Simulando OFDM sin FEC y OFDM+LDPC... (~45 s)")
for EbN0_dB in EbN0_ex6:
    EbN0_lin  = 10**(EbN0_dB / 10)
    # Varianza compleja del ruido (E_s=1 QPSK, k=2 bits/sym)
    # Uncoded : σ²_unc = 1/(2·Eb/N0)        [Eb = E_s/k = 1/2]
    # Coded   : σ²_cod = 1/(2·Rc12·Eb/N0)   [Eb_info = Eb/Rc]
    sigma2_unc = 1.0 / (2.0 * EbN0_lin)
    sigma2_cod = 1.0 / (2.0 * Rc12 * EbN0_lin)
    floor_v    = 1.0 / (n_sim_ex6 * n_ldpc_ex6)

    err_unc = err_cod = 0

    for _ in range(n_sim_ex6):
        # Uncoded OFDM ─────────────────────────────────────────────────────
        bits_u  = rng_ex6.integers(0, 2, 2 * N_ofdm)
        X_u     = qpsk_map_ex6(bits_u)
        x_u     = ofdm_tx(X_u, N_CP)
        y_u     = apply_channel(x_u, h_ch)
        n_u     = np.sqrt(sigma2_unc / 2) * (
            rng_ex6.standard_normal(len(y_u)) + 1j * rng_ex6.standard_normal(len(y_u)))
        Y_u     = ofdm_rx_no_channel(y_u + n_u, N_ofdm, N_CP)
        X_eq_u  = zf_equalizer(Y_u, h_ch, N_ofdm)
        err_unc += np.sum(qpsk_hard_ex6(X_eq_u) != bits_u)

        # OFDM + LDPC ──────────────────────────────────────────────────────
        X_c = np.zeros(N_ofdm, dtype=complex)
        X_c[:n_active] = qpsk_map_ex6(c_zeros)          # 240 bits en 120 subportadoras
        x_c     = ofdm_tx(X_c, N_CP)
        y_c     = apply_channel(x_c, h_ch)
        n_c     = np.sqrt(sigma2_cod / 2) * (
            rng_ex6.standard_normal(len(y_c)) + 1j * rng_ex6.standard_normal(len(y_c)))
        Y_c     = ofdm_rx_no_channel(y_c + n_c, N_ofdm, N_CP)
        X_eq_c  = zf_equalizer(Y_c, h_ch, N_ofdm)

        # LLRs por subportadora (varianza efectiva tras ZF)
        llr_c   = qpsk_llr_ex6(X_eq_c[:n_active], sigma2_cod, H_k_mag2)
        c_hat, _ = bp_awgn(H12, llr_c, var_nbrs12, chk_nbrs12, max_iter=20)
        err_cod += np.sum(c_hat != c_zeros)

    ber_ofdm_unc.append(max(err_unc / (n_sim_ex6 * 2 * N_ofdm), floor_v))
    ber_ofdm_cod.append(max(err_cod / (n_sim_ex6 * n_ldpc_ex6), floor_v))

ber_ofdm_unc = np.array(ber_ofdm_unc)
ber_ofdm_cod = np.array(ber_ofdm_cod)

# Verificar ganancia de codificación visible
idx_5dB = np.argmin(np.abs(EbN0_ex6 - 5.0))
print(f"@ 5 dB  — OFDM sin FEC: {ber_ofdm_unc[idx_5dB]:.2e}  OFDM+LDPC: {ber_ofdm_cod[idx_5dB]:.2e}")

# ── e) Figura ofdm-ldpc-ber.png (FIG-09) ───────────────────────────────────
EbN0_plot  = np.linspace(0, 10, 300)
BER_awgn   = 0.5 * erfc(np.sqrt(10**(EbN0_plot / 10)))   # QPSK AWGN teórico

fig, ax = plt.subplots(figsize=(10, 5))
ax.semilogy(EbN0_plot,  BER_awgn,     'k--', lw=1.5, label='QPSK AWGN (referencia teórica)')
ax.semilogy(EbN0_ex6,  ber_ofdm_unc,  'o-',  color='steelblue', lw=2, ms=5,
            label=f'OFDM sin FEC (ZF, canal 3-tap)')
ax.semilogy(EbN0_ex6,  ber_ofdm_cod,  's-',  color='darkorange', lw=2, ms=5,
            label=f'OFDM + LDPC $r_c\\approx{Rc12:.2f}$ (BP, n=240)')

# Límite de Shannon para la tasa efectiva
ebn0_sh = 10 * np.log10((2**Rc12 - 1) / Rc12)
ax.axvline(ebn0_sh, color='darkorange', ls=':', lw=0.9, alpha=0.6)
ax.text(ebn0_sh + 0.1, 3e-1, f'$C(r={Rc12:.2f})$', fontsize=8,
        color='darkorange', rotation=90, va='top')

ax.set_xlabel('$E_b/N_0$ (dB)')
ax.set_ylabel('BER')
ax.set_title('OFDM+LDPC end-to-end — canal frequency-selective 3-tap + ZF + BP (n=240)')
ax.set_xlim(0, 10)
ax.set_ylim(1e-5, 1.1)
ax.legend(fontsize=9, loc='lower left')
plt.tight_layout()
plt.savefig('figures/ofdm-ldpc-ber.png', dpi=150, bbox_inches='tight')
plt.show()
print("Figura guardada: figures/ofdm-ldpc-ber.png")
```

**CELL 20 — actualizar entrada del resumen para Ejercicio 6:**

Reemplazar la fila de Ejercicio 6 en la tabla de resumen:
```
| 6 | Waterfall LDPC vs sin código | Ganancia ~1-2 dB para código pequeño (8,4) |
```
por:
```
| 6 | OFDM+LDPC end-to-end (canal freq-selective) | Ganancia de codificación LDPC visible en canal multipath |
```

**Método de edición del notebook:**
Leer lab.ipynb como JSON. Modificar los campos `source` de cells[18], cells[19], y cells[20]. Limpiar `outputs` y `execution_count` de cells[19] (código nuevo, sin ejecutar). Guardar con json.dump(indent=1). Verificar que len(nb['cells']) == 21 tras la edición.
  </action>
  <verify>
    <automated>cd /home/researcher/Teaching/uni/2026/wireless-communication-systems/docs/sessions/05-channel-coding && python3 -c "
import json
nb = json.load(open('lab.ipynb'))
assert len(nb['cells']) == 21, f'Esperado 21 celdas, hay {len(nb[\"cells\"])}'
src18 = ''.join(nb['cells'][18]['source'])
src19 = ''.join(nb['cells'][19]['source'])
assert 'OFDM+LDPC' in src18 or 'frequency-selective' in src18, 'Cell 18 no actualizada'
assert 'ofdm_tx' in src19, 'ofdm_tx no está en Cell 19'
assert 'apply_channel' in src19, 'apply_channel no está en Cell 19'
assert 'ofdm_rx_no_channel' in src19, 'ofdm_rx_no_channel no está en Cell 19'
assert 'zf_equalizer' in src19, 'zf_equalizer no está en Cell 19'
assert 'ofdm-ldpc-ber.png' in src19, 'ofdm-ldpc-ber.png no está en Cell 19'
assert 'bp_awgn' in src19, 'bp_awgn no está en Cell 19'
print('Estructura del notebook OK')
"
    </automated>
  </verify>
  <done>lab.ipynb: Cell 18 describe OFDM+LDPC, Cell 19 contiene las 4 funciones OFDM de sesión 03 + integrador OFDM+LDPC que genera ofdm-ldpc-ber.png. Verificación Python sin AssertionError.</done>
</task>

<task type="auto">
  <name>Tarea 2: Ejecutar Cell 19 para generar figures/ofdm-ldpc-ber.png</name>
  <files>figures/ofdm-ldpc-ber.png</files>
  <action>
Ejecutar el código de Cell 19 para generar la figura. La simulación Monte Carlo tarda ~45 segundos con n_sim_ex6=300 y 21 puntos de SNR.

**Método preferido — script standalone** (más robusto que nbconvert cuando celdas de Polar están deshabilitadas):

Extraer el código necesario y ejecutar como script Python:
1. Importaciones de Cell 1: numpy, matplotlib, scipy.special.erfc
2. LDPC de Cell 8: H12, var_nbrs12, chk_nbrs12, Rc12, gallager_ldpc, gf2_rank, bp_awgn
3. Todo el código de Cell 19

```bash
cd /home/researcher/Teaching/uni/2026/wireless-communication-systems/docs/sessions/05-channel-coding
python3 -c "
import json
nb = json.load(open('lab.ipynb'))

# Extraer código de celdas 1, 8, 19
cell1  = '\n'.join(nb['cells'][1]['source']) if isinstance(nb['cells'][1]['source'], list) else nb['cells'][1]['source']
cell8  = '\n'.join(nb['cells'][8]['source']) if isinstance(nb['cells'][8]['source'], list) else nb['cells'][8]['source']
cell19 = '\n'.join(nb['cells'][19]['source']) if isinstance(nb['cells'][19]['source'], list) else nb['cells'][19]['source']

script = cell1 + '\n\n' + cell8 + '\n\n' + cell19
with open('/tmp/run_ofdm_ldpc.py', 'w') as f:
    f.write(script)
print('Script generado: /tmp/run_ofdm_ldpc.py')
"
python3 /tmp/run_ofdm_ldpc.py
```

Si la ejecución falla por imports (e.g., erfc no importada en Cell 1), añadir al inicio del script:
```python
from scipy.special import erfc
```

Verificar que figures/ofdm-ldpc-ber.png existe y tiene tamaño razonable (>40 KB).
  </action>
  <verify>
    <automated>cd /home/researcher/Teaching/uni/2026/wireless-communication-systems/docs/sessions/05-channel-coding && python3 -c "
import os
sz = os.path.getsize('figures/ofdm-ldpc-ber.png')
assert sz > 40000, f'ofdm-ldpc-ber.png demasiado pequeña: {sz} bytes (esperado >40000)'
print(f'figures/ofdm-ldpc-ber.png: {sz} bytes OK')
"
    </automated>
  </verify>
  <done>figures/ofdm-ldpc-ber.png existe con >40 KB. La figura muestra 3 curvas: QPSK AWGN teórico (negro punteado), OFDM sin FEC (azul), y OFDM+LDPC (naranja). La ganancia de codificación es visible en la región waterfall.</done>
</task>

<task type="auto">
  <name>Tarea 3: Insertar bloque FIG-09 en index.md §5</name>
  <files>index.md</files>
  <action>
Leer index.md. La sección §5 (línea 262) tiene un bloque <figure> para waterfall-curves.png (Figura 2), la tabla LDPC vs Polar, un ejemplo numérico, y la narrativa hook que termina en "...la respuesta es la sección de laboratorio, que implementa exactamente esta cadena con simulación Monte Carlo y mide la BER bit a bit." (línea ~299).

Insertar el siguiente bloque de figura DESPUÉS del párrafo que termina en "mide la BER bit a bit." y ANTES del separador `---` de §5:

```markdown

<figure markdown="span">
  ![BER end-to-end OFDM+LDPC vs sin FEC vs AWGN](figures/ofdm-ldpc-ber.png)
  <!-- generada por celda 19 de lab.ipynb -->
  <figcaption markdown="1">**Figura 6.** BER en función de $E_b/N_0$ para un sistema OFDM ($N=128$ subportadoras, CP=16, QPSK) sobre un canal frequency-selective de 3 taps con ecualizador ZF, comparada con la referencia AWGN (negro punteado) y con OFDM+LDPC $r_c\approx0{,}51$ (naranja). Simulación Monte Carlo con 300 realizaciones por punto de SNR.
  El canal frequency-selective degrada la BER respecto al canal AWGN ideal (curva azul vs línea negra): el ZF amplifica el ruido en los nulos espectrales del canal. La codificación LDPC (naranja) compensa parcialmente esta degradación, recuperando $\approx 3\ \text{dB}$ de ganancia a BER $= 10^{-3}$ gracias al entrelazado implícito sobre las subportadoras del bloque OFDM.
  </figcaption>
</figure>

```

Usar Edit tool para insertar este bloque. El punto de inserción exacto es después de la línea "mide la BER bit a bit." y antes del separador `---` que cierra §5.

Verificar que el bloque sigue la sintaxis MkDocs-Material estándar (igual que los otros bloques <figure> del archivo).
  </action>
  <verify>
    <automated>cd /home/researcher/Teaching/uni/2026/wireless-communication-systems/docs/sessions/05-channel-coding && python3 -c "
txt = open('index.md').read()
assert 'ofdm-ldpc-ber.png' in txt, 'FIG-09 no está en index.md'
assert 'celda 19 de lab.ipynb' in txt, 'comentario de celda no encontrado'
assert 'Figura 6' in txt, 'Figura 6 no encontrada'
print('index.md: FIG-09 OK')
"
    </automated>
  </verify>
  <done>index.md §5 contiene el bloque <figure markdown="span"> con ofdm-ldpc-ber.png, referenciando celda 19 de lab.ipynb, con figcaption de 2+ líneas describiendo la ganancia de codificación visible. El bloque aparece después del hook narrativo y antes del separador.</done>
</task>

</tasks>

<verification>
Verificación global de Phase 5:

```bash
cd /home/researcher/Teaching/uni/2026/wireless-communication-systems/docs/sessions/05-channel-coding

# 1. Figura existe con tamaño adecuado
python3 -c "
import os
sz = os.path.getsize('figures/ofdm-ldpc-ber.png')
assert sz > 40000, f'{sz} bytes (esperado >40000)'
print(f'FIG-09: {sz} bytes OK')
"

# 2. Notebook: 21 celdas, funciones OFDM en Cell 19
python3 -c "
import json
nb = json.load(open('lab.ipynb'))
assert len(nb['cells']) == 21, f'{len(nb[\"cells\"])} celdas (esperado 21)'
src19 = ''.join(nb['cells'][19]['source'])
for fn in ['ofdm_tx', 'apply_channel', 'ofdm_rx_no_channel', 'zf_equalizer', 'bp_awgn', 'ofdm-ldpc-ber.png']:
    assert fn in src19, f'Falta: {fn}'
    print(f'Cell 19: {fn} OK')
"

# 3. index.md referencia correctamente FIG-09
python3 -c "
txt = open('index.md').read()
assert 'ofdm-ldpc-ber.png' in txt
assert 'celda 19 de lab.ipynb' in txt
print('index.md: FIG-09 reference OK')
"
```
</verification>

<success_criteria>
1. FIG-09: figures/ofdm-ldpc-ber.png existe con >40 KB — 3 curvas BER (AWGN teórico, OFDM sin FEC, OFDM+LDPC) con ganancia de codificación visible
2. LAB-03: Cell 19 de lab.ipynb define las 4 funciones OFDM de sesión 03 sin modificación (ofdm_tx, apply_channel, ofdm_rx_no_channel, zf_equalizer) y usa bp_awgn de Cell 8
3. index.md §5 contiene bloque <figure> con ofdm-ldpc-ber.png y comentario "celda 19 de lab.ipynb"
4. lab.ipynb mantiene 21 celdas totales
</success_criteria>

<output>
Crear `.planning/phases/05-integrador-ofdm-fec/05-01-SUMMARY.md` al terminar con:
- Tamaño final de figures/ofdm-ldpc-ber.png
- BER uncoded y coded a 5 dB Eb/N0 (para confirmar ganancia de codificación visible)
- Líneas de index.md añadidas (línea de inserción del bloque <figure>)
</output>
