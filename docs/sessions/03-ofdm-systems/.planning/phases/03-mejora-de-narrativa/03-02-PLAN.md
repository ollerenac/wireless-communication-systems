---
phase: 03-mejora-de-narrativa
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - index.md
autonomous: true
requirements:
  - NARR-02
tags:
  - narrative
  - introduction
  - markdown

must_haves:
  truths:
    - "El párrafo de la Introducción (línea ~23) menciona explícitamente que la Sesión 02 asumió canal AWGN plano (un coeficiente escalar) y que aquí el canal es frequency-selective"
    - "La inserción no rompe la frase final existente sobre cadena IFFT/CP/FFT/ecualización"
    - "La progresión Sesión 01 → Sesión 02 → OFDM se lee sin saltos lógicos al leer en secuencia"
  artifacts:
    - path: "index.md"
      provides: "Párrafo introductorio con brecha Sesión 02 → OFDM explícita"
      contains: "AWGN plano"
  key_links:
    - from: "Frase sobre Sesión 02 y SNR proporcional a (M-1)"
      to: "Frase '¿qué ocurre cuando aplicamos una única portadora M-QAM'"
      via: "frase puente D-10 sobre canal AWGN plano vs frequency-selective"
      pattern: "AWGN plano.*coeficiente escalar"
---

<objective>
Implementa NARR-02 (D-10, D-11, D-12): insertar una frase puente en el párrafo de la Introducción (línea ~23) que haga explícita la brecha entre el supuesto de Sesión 02 (canal AWGN plano, un coeficiente escalar) y el problema de OFDM (canal frequency-selective sin un único coeficiente que corrija todo el espectro). Intervención mínima — no reescribir el párrafo.

Purpose: Cerrar el salto lógico que actualmente existe entre "SNR proporcional a (M-1)" y "Pero hay un problema que no resolvimos". El lector debe entender exactamente qué supuesto de Sesión 02 deja de cumplirse en este contexto.
Output: `index.md` con una frase adicional en el párrafo de la Introducción (línea ~23), que articula la brecha en estilo declarativo conciso.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mejora-de-narrativa/03-CONTEXT.md
@.planning/phases/03-mejora-de-narrativa/03-RESEARCH.md
@.planning/phases/03-mejora-de-narrativa/03-PATTERNS.md

<interfaces>
<!-- Texto exacto del párrafo a modificar — línea 23 de index.md (verificado por grep) -->

Estado actual del párrafo:
```
Las Sesiones 01 y 02 construyeron los dos pilares del problema de transmisión digital:
el canal inalámbrico y la modulación. La Sesión 01 mostró que los canales de banda
ancha son frequency-selective — distintas frecuencias experimentan ganancias distintas,
y los ecos producen ISI cuando el período de símbolo es menor que el delay spread.
La Sesión 02 mostró que para transmitir $k$ bits por símbolo con M-QAM se necesita un
SNR proporcional a $(M-1)$. Pero hay un problema que no resolvimos: ¿qué ocurre cuando
aplicamos una única portadora M-QAM de alta tasa sobre un canal frequency-selective?
```

Punto exacto de inserción: después del punto que cierra `SNR proporcional a $(M-1)$.` y antes de `Pero hay un problema que no resolvimos`.

Formulación D-10 (CONTEXT.md):
```
La Sesión 02 resolvió el canal AWGN plano — un coeficiente escalar de canal que el
receptor puede invertir directamente. Aquí el canal es frequency-selective — no existe
un único coeficiente que corrija todo el espectro.
```

D-11: NO agregar párrafo de preview de secciones — la última frase de la intro (línea 68: "Esta sesión construye esa cadena completa: símbolos QAM → IFFT → CP → canal → FFT → ecualización de un tap.") ya hace de preview.

D-12: intervención mínima — solo el parche de D-10.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insertar frase puente D-10 en el párrafo de la Introducción</name>
  <files>index.md</files>
  <read_first>
    - index.md (línea 23 — el párrafo entero; usar `grep -n "Pero hay un problema que no resolvimos" index.md` para localizar si la línea se desplazó)
    - .planning/phases/03-mejora-de-narrativa/03-CONTEXT.md (D-10 formulación verbatim, D-11 no agregar preview, D-12 intervención mínima)
    - .planning/phases/03-mejora-de-narrativa/03-RESEARCH.md (sección "NARR-02: Introducción — Gap a rellenar", confirma ubicación y formato)
    - .planning/phases/03-mejora-de-narrativa/03-PATTERNS.md (PATTERN 6 — texto exacto a insertar y resultado esperado del párrafo)
  </read_first>
  <action>
Insertar la frase puente D-10 dentro del párrafo de la Introducción (línea ~23 de `index.md`), en el punto exacto entre `SNR proporcional a $(M-1)$.` y `Pero hay un problema que no resolvimos`.

Texto a insertar (D-10, verbatim de CONTEXT.md):
```
La Sesión 02 resolvió el canal AWGN plano — un coeficiente escalar de canal que el receptor puede invertir directamente. Aquí el canal es frequency-selective — no existe un único coeficiente que corrija todo el espectro.
```

La inserción es **dentro del mismo párrafo** (no agregar línea en blanco ni nuevo párrafo). El párrafo entero queda como un único bloque con la nueva frase intercalada. Mantener exactamente la misma puntuación: la frase nueva empieza tras un espacio (porque sigue a `$(M-1)$.`) y termina en `corrija todo el espectro.` con un punto y un espacio antes de `Pero hay un problema…`.

Verificar usando `grep -n "Pero hay un problema que no resolvimos" index.md` que el contexto contextual sigue siendo el de la Introducción (no algún otro lugar del documento donde aparezca la misma frase).

NO modifiques la última frase del párrafo ni la frase final de la Introducción (línea 68 sobre cadena IFFT/CP/FFT). NO agregues un párrafo de preview adicional (D-11). NO reescribas otras frases del párrafo (D-12, intervención mínima).
  </action>
  <verify>
    <automated>grep -c "AWGN plano" index.md</automated>
    <!-- esperado: ≥ 1 -->
    <automated>grep -c "coeficiente escalar de canal" index.md</automated>
    <!-- esperado: 1 -->
    <automated>grep -c "no existe un único coeficiente que corrija todo el espectro" index.md</automated>
    <!-- esperado: 1 -->
    <automated>grep -c "Pero hay un problema que no resolvimos" index.md</automated>
    <!-- esperado: 1 (frase original conservada) -->
    <automated>awk '/Las Sesiones 01 y 02 construyeron/{found=1} found && /Pero hay un problema/{print NR; exit}' index.md</automated>
    <!-- esperado: ≤ ~30 — la frase debe seguir en el mismo párrafo, próxima al inicio del documento -->
  </verify>
  <acceptance_criteria>
    - `grep "AWGN plano" index.md` retorna ≥ 1 línea con el contexto "La Sesión 02 resolvió el canal AWGN plano".
    - `grep -c "coeficiente escalar de canal" index.md` retorna 1.
    - `grep -c "no existe un único coeficiente que corrija todo el espectro" index.md` retorna 1.
    - `grep -c "Pero hay un problema que no resolvimos" index.md` retorna 1 (frase original conservada exactamente una vez).
    - La frase D-10 está **antes** de "Pero hay un problema": `awk '/AWGN plano/{a=NR} /Pero hay un problema/{b=NR; print (a<b ? "OK" : "WRONG")}' index.md` retorna `OK`.
    - La frase de preview existente (línea 68) sigue intacta: `grep -c "símbolos QAM → IFFT → CP → canal → FFT" index.md` retorna 1.
    - No se introdujeron párrafos nuevos en la Introducción: `awk '/^# Sistemas OFDM/,/^## /' index.md | grep -cE "^[A-Z]" | head -1` retorna un conteo razonable (no anomalías de párrafos extra).
  </acceptance_criteria>
  <done>
    El párrafo de la Introducción contiene la frase puente D-10 con el contenido verbatim de CONTEXT.md, insertada entre la frase de SNR proporcional a (M-1) y la frase "Pero hay un problema que no resolvimos". El resto del párrafo y la frase de preview de la línea 68 quedan sin modificar.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown → MkDocs build | Inserción de texto plano dentro de un párrafo existente: riesgo de markup es mínimo. El único punto delicado es no romper el LaTeX inline `$(M-1)$` adyacente al punto de inserción |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | LaTeX inline `$(M-1)$.` | mitigate | Insertar la frase D-10 después del punto final tras `$(M-1)$.`, sin tocar el `$...$` ni el punto que cierra la frase original; verificación `grep "SNR proporcional a \$(M-1)\\$" index.md` confirma que el LaTeX queda intacto |
| T-03-06 | Tampering | Frase original "Pero hay un problema que no resolvimos" | mitigate | La frase original no se modifica, solo precede a la nueva inserción; verificación `grep -c "Pero hay un problema que no resolvimos" index.md` retorna exactamente 1 |
</threat_model>

<verification>
- `grep -c "AWGN plano" index.md` retorna ≥ 1.
- `grep -c "coeficiente escalar de canal" index.md` retorna 1.
- `grep -c "Pero hay un problema que no resolvimos" index.md` retorna 1.
- La frase D-10 está **antes** de "Pero hay un problema" en el archivo: verificable con `awk` (ver acceptance criteria).
- `grep -c "símbolos QAM → IFFT → CP → canal → FFT" index.md` retorna 1 (frase de preview intacta).
</verification>

<success_criteria>
- Brecha Sesión 02 → OFDM explícita en la Introducción (D-10).
- Sin nuevos párrafos en la Introducción (D-11, D-12).
- LaTeX inline `$(M-1)$` y frase de preview de línea 68 intactos.
- `index.md` sigue siendo válido MkDocs-Material.
</success_criteria>

<output>
Crear `.planning/phases/03-mejora-de-narrativa/03-02-SUMMARY.md` al terminar con:
- Línea exacta donde quedó la inserción (probablemente sigue siendo línea 23 si el párrafo no se rompió)
- Conteo de verificación de las grep
- Confirmación de que la frase de preview de línea 68 no se modificó
</output>
