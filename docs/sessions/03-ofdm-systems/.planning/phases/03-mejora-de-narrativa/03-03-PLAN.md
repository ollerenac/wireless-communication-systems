---
phase: 03-mejora-de-narrativa
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - index.md
autonomous: true
requirements:
  - NARR-03
tags:
  - narrative
  - synthesis
  - figure-caption
  - markdown

must_haves:
  truths:
    - "Cada una de las 5 dimensiones de §7 Síntesis termina con una referencia parenthetical a la(s) sección(es) donde se desarrolló"
    - "El caption de Figura 3 describe los tres paneles (factor α[k] izquierda, ZF centro, MMSE derecha) en lugar de dos"
    - "El alt-text de Figura 3 enumera los tres paneles"
  artifacts:
    - path: "index.md"
      provides: "5 cross-refs en §7 + caption corregido de Figura 3 (3 paneles)"
      contains: "(§2 y §4)"
  key_links:
    - from: "§7 Síntesis Dimensión 1"
      to: "§2 (ortogonalidad) y §4 (cadena OFDM)"
      via: "parenthetical `(§2 y §4)`"
      pattern: "Doppler cause ICI \\(§2 y §4\\)"
    - from: "Figura 3 caption"
      to: "código real del Cell 18 (plt.subplots(1, 3))"
      via: "descripción de 3 paneles: factor α[k], constelación ZF, constelación MMSE"
      pattern: "tres paneles|izquierda.*centro.*derecha"
---

<objective>
Implementa NARR-03 (D-13, D-14) y D-15: agregar referencias cruzadas parenthetical al final de la "Implicación de diseño" de cada una de las 5 dimensiones de §7 Síntesis, y reemplazar el caption + alt-text de Figura 3 para describir los 3 paneles reales (factor α[k] / constelación ZF / constelación MMSE) en lugar de los 2 paneles incorrectos del estado actual.

Purpose: §7 Síntesis es la conclusión global del documento; sin cross-refs el lector no puede regresar a la sección donde se desarrolló cada concepto. La Figura 3 actualmente tiene un caption que miente sobre la cantidad de paneles, lo cual desorienta al estudiante que mira la imagen real.
Output: `index.md` con 5 parentheticals en §7 (líneas ~1078–1086) y un caption + alt-text de Figura 3 (líneas ~935–938) que describe 3 paneles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mejora-de-narrativa/03-CONTEXT.md
@.planning/phases/03-mejora-de-narrativa/03-RESEARCH.md
@.planning/phases/03-mejora-de-narrativa/03-PATTERNS.md

<interfaces>
<!-- Estado actual de cada dimensión §7 (verbatim, verificado por grep) -->
<!-- Las líneas pueden haberse desplazado tras Plan 01 + 02; usar grep "Dimensión [0-9]" para localizar -->

| Línea | Dimensión | Fin de oración actual | Referencia D-14 |
|-------|-----------|----------------------|-----------------|
| ~1078 | D1 (FSF → N canales flat) | `...que el Doppler cause ICI.` | `(§2 y §4)` |
| ~1080 | D2 (CP precio de circularidad) | `...5G NR balancea esto con numerologías.` | `(§3)` |
| ~1082 | D3 (FFT eficiente) | `...maximizar la eficiencia de la FFT radix-2.` | `(§2)` |
| ~1084 | D4 (ecualización + estimación) | `...Sesión 08 desarrolla los estimadores LS y MMSE.` | `(§4.5, §4.6, §4.7)` |
| ~1086 | D5 (PAPR) | `...a costa de perder la ecualización de un tap pura.` | `(§7)` |

Formato D-13: parenthetical `(§X)` antes del punto final de la oración.

Figura 3 — estado actual (líneas ~935–938):
```html
<figure markdown="span">
  ![Constelaciones QAM tras ecualización ZF (izquierda) vs MMSE (derecha)](figures/mmse-vs-zf-constellation.png)
  <figcaption markdown="1">**Figura 3.** Dispersión de la constelación QAM tras ecualización en un canal selectivo en frecuencia: ZF (izquierda) amplifica ruido en las subportadoras débiles — la nube se ensancha desproporcionadamente; MMSE (derecha) la contiene mediante regularización con $1/\text{SNR}$ — los puntos quedan más cerca de los símbolos ideales. La diferencia se ve más pronunciada a SNR baja, donde el regularizador domina.</figcaption>
</figure>
```

Estructura real verificada (Cell 18 del notebook): `plt.subplots(1, 3)` → tres paneles, no dos.
- Panel izquierdo (`axes[0]`): Factor de contracción MMSE α[k] por subportadora (barras coloreadas con colormap turbo)
- Panel central (`axes[1]`): Constelación ZF (scatter)
- Panel derecho (`axes[2]`): Constelación MMSE (scatter)

Alt-text correcto D-15: `Factor de contracción α[k] (izquierda), constelación ZF (centro) y MMSE (derecha) en un canal selectivo en frecuencia`

Caption sugerido D-15 (RESEARCH.md Code Examples, Claude puede pulir manteniendo los 3 paneles descritos):
```
**Figura 3.** Tres paneles del ecualizador MMSE en un canal selectivo en frecuencia.
**Izquierda:** factor de contracción $\alpha[k] \in (0,1)$ por subportadora — valores
$\approx 1$ (rojo) indican canal fuerte donde MMSE $\approx$ ZF; valores $\ll 1$
(azul/cian) indican *fades* donde el MMSE modera la amplificación. **Centro:**
constelación tras ecualizador ZF — los puntos muestran ruido amplificado en las
subportadoras débiles. **Derecha:** constelación tras ecualizador MMSE — la
contracción $\alpha[k]$ compacta la nube en las subportadoras débiles a costa
de un pequeño sesgo.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar referencias cruzadas parenthetical a las 5 dimensiones de §7 Síntesis</name>
  <files>index.md</files>
  <read_first>
    - index.md (líneas ~1064–1096, sección §7 Síntesis; usar `grep -n "^\*\*Dimensión" index.md` para localizar las 5 dimensiones)
    - .planning/phases/03-mejora-de-narrativa/03-CONTEXT.md (D-13 formato parenthetical, D-14 mapa de referencias por dimensión)
    - .planning/phases/03-mejora-de-narrativa/03-RESEARCH.md (sección "NARR-03: §7 Síntesis — Estado actual" con tabla de líneas y referencias)
    - .planning/phases/03-mejora-de-narrativa/03-PATTERNS.md (PATTERN 7 — estructura de cada dimensión y patrón de inserción)
  </read_first>
  <action>
Para cada una de las 5 dimensiones de §7 Síntesis (`**Dimensión 1:**` a `**Dimensión 5:**`), insertar la referencia parenthetical correspondiente al final de la oración "Implicación de diseño", **antes del punto final**.

Mapa D-14 (verbatim de CONTEXT.md):
- **Dimensión 1** (línea ~1078, termina en `...Doppler cause ICI.`): insertar ` (§2 y §4)` antes del `.` final → `...Doppler cause ICI (§2 y §4).`
- **Dimensión 2** (línea ~1080, termina en `...numerologías.`): insertar ` (§3)` antes del `.` final → `...numerologías (§3).`
- **Dimensión 3** (línea ~1082, termina en `...FFT radix-2.`): insertar ` (§2)` antes del `.` final → `...FFT radix-2 (§2).`
- **Dimensión 4** (línea ~1084, termina en `...LS y MMSE.`): insertar ` (§4.5, §4.6, §4.7)` antes del `.` final → `...LS y MMSE (§4.5, §4.6, §4.7).`
- **Dimensión 5** (línea ~1086, termina en `...ecualización de un tap pura.`): insertar ` (§7)` antes del `.` final → `...ecualización de un tap pura (§7).`

Nota Pitfall 6 de RESEARCH.md: la `(§7)` de la Dimensión 5 es auto-referencia válida a la sección §7 (PAPR: La Penalización de la Amplificación) — el contexto del documento lo deja claro y CONTEXT.md/D-14 lo confirma.

Localización: usar `grep -n "^\*\*Dimensión" index.md` para obtener las líneas exactas (pueden haberse desplazado tras Plans 01 y 02). El texto de cada dimensión es una sola línea (no multi-línea); buscar el final de la oración de "Implicación de diseño" — la última oración de la línea, que termina con un punto.

NO modifiques otras partes de §7 (línea del cierre práctico, párrafo de transición a Ejercicios). NO modifiques el orden de las dimensiones. NO cambies el formato `*Implicación de diseño*:` ni los números de dimensión.
  </action>
  <verify>
    <automated>grep -c "(§2 y §4)\." index.md</automated>
    <!-- esperado: 1 (Dimensión 1) -->
    <automated>grep -cE "numerologías \(§3\)\." index.md</automated>
    <!-- esperado: 1 (Dimensión 2) -->
    <automated>grep -cE "FFT radix-2 \(§2\)\." index.md</automated>
    <!-- esperado: 1 (Dimensión 3) -->
    <automated>grep -cF "(§4.5, §4.6, §4.7)." index.md</automated>
    <!-- esperado: 1 (Dimensión 4) -->
    <automated>grep -cE "ecualización de un tap pura \(§7\)\." index.md</automated>
    <!-- esperado: 1 (Dimensión 5) -->
    <automated>grep -c "^\*\*Dimensión [1-5]:" index.md</automated>
    <!-- esperado: 5 — todas las dimensiones siguen presentes y bien formateadas -->
  </verify>
  <acceptance_criteria>
    - 5 parentheticals presentes: cada uno de los siguientes greps retorna 1:
      - `grep -cF "(§2 y §4)." index.md` retorna 1
      - `grep -cE "numerologías \(§3\)\." index.md` retorna 1
      - `grep -cE "FFT radix-2 \(§2\)\." index.md` retorna 1
      - `grep -cF "(§4.5, §4.6, §4.7)." index.md` retorna 1
      - `grep -cE "tap pura \(§7\)\." index.md` retorna 1
    - Las 5 dimensiones siguen presentes con su formato original: `grep -c "^\*\*Dimensión [1-5]:" index.md` retorna 5.
    - Cada parenthetical aparece **antes** del punto final (no después): los 5 greps con `\.$` o `)\.` confirman que el orden es `... (§X).` y no `... .(§X)`.
    - El texto previo a las inserciones se preserva: `grep "Doppler cause ICI" index.md`, `grep "FFT radix-2" index.md`, etc., siguen retornando matches válidos.
  </acceptance_criteria>
  <done>
    Las 5 dimensiones de §7 tienen su referencia parenthetical insertada antes del punto final. El mapa D-14 está implementado exactamente (D1→§2 y §4, D2→§3, D3→§2, D4→§4.5–§4.7, D5→§7). Ningún otro contenido de §7 fue modificado.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reemplazar caption y alt-text de Figura 3 para describir los 3 paneles reales</name>
  <files>index.md</files>
  <read_first>
    - index.md (líneas ~935–938; usar `grep -n "mmse-vs-zf-constellation" index.md` para localizar; leer también la línea entera del `<figcaption>` para entender la estructura HTML/markdown actual)
    - .planning/phases/03-mejora-de-narrativa/03-CONTEXT.md (D-15 — descripción de 3 paneles)
    - .planning/phases/03-mejora-de-narrativa/03-RESEARCH.md (sección "Figura 3 — Caption actual vs correcto" con caption sugerido completo, y "Code Examples" con caption verbatim recomendado)
    - .planning/phases/03-mejora-de-narrativa/03-PATTERNS.md (PATTERN 4 — estructura HTML `<figure markdown="span">` + `<figcaption markdown="1">`; alt-text correcto y caption sugerido)
    - lab.ipynb (Cell 18 — `plt.subplots(1, 3)` confirma que la figura tiene 3 paneles; títulos y colormap turbo del panel izquierdo)
  </read_first>
  <action>
Reemplazar el alt-text y el `<figcaption>` actuales de Figura 3 (líneas ~935–938 de `index.md`) por la versión que describe los 3 paneles reales.

**Alt-text actual:** `Constelaciones QAM tras ecualización ZF (izquierda) vs MMSE (derecha)`
**Alt-text nuevo (D-15):** `Factor de contracción α[k] (izquierda), constelación ZF (centro) y MMSE (derecha) en un canal selectivo en frecuencia`

**Caption actual (resumen):** describe solo 2 paneles (ZF izquierda, MMSE derecha) y omite el panel izquierdo real (factor α[k]).
**Caption nuevo (D-15, formulación de PATTERNS.md PATTERN 4 / RESEARCH.md Code Examples):**

El caption nuevo debe:
1. Empezar con `**Figura 3.**` (en negrita).
2. Mencionar explícitamente que hay **tres paneles** (la palabra "tres" o "3" debe aparecer, o equivalentemente la enumeración explícita izquierda/centro/derecha).
3. Describir cada panel en orden izquierda → centro → derecha:
   - **Izquierda:** factor de contracción $\alpha[k] \in (0,1)$ por subportadora — valores $\approx 1$ (rojo) indican canal fuerte donde MMSE $\approx$ ZF; valores $\ll 1$ (azul/cian) indican *fades* donde el MMSE modera la amplificación.
   - **Centro:** constelación tras ecualizador ZF — los puntos muestran ruido amplificado en las subportadoras débiles.
   - **Derecha:** constelación tras ecualizador MMSE — la contracción $\alpha[k]$ compacta la nube en las subportadoras débiles a costa de un pequeño sesgo.
4. Estar contenido dentro de `<figcaption markdown="1">...</figcaption>` (mantener el atributo `markdown="1"` para que el LaTeX inline `$\alpha[k]$` se renderice — Pitfall: sin ese atributo el LaTeX no se renderiza dentro del tag HTML, ver shared patterns en PATTERNS.md).

Mantener la estructura HTML exterior intacta:
```html
<figure markdown="span">
  ![<alt-text nuevo>](figures/mmse-vs-zf-constellation.png)
  <figcaption markdown="1">**Figura 3.** <descripción de 3 paneles>...</figcaption>
</figure>
```

NO cambies la ruta de la imagen (`figures/mmse-vs-zf-constellation.png`). NO toques el `<figure markdown="span">` ni el `</figure>`. NO modifiques otras figuras del documento.
  </action>
  <verify>
    <automated>grep -c "mmse-vs-zf-constellation.png" index.md</automated>
    <!-- esperado: 1 (la imagen sigue referenciada exactamente una vez) -->
    <automated>grep -cE "tres paneles|izquierda.*centro.*derecha|α\[k\].*izquierda" index.md</automated>
    <!-- esperado: ≥ 1 — el caption nuevo nombra los tres paneles -->
    <automated>grep -cE "factor de contracción.*\\\\alpha\[k\]|α\[k\]" index.md</automated>
    <!-- esperado: ≥ 1 — el caption menciona el factor α[k] del panel izquierdo -->
    <automated>grep -c "figcaption markdown=\"1\"" index.md</automated>
    <!-- esperado: igual al estado inicial (no reducir el número de captions con markdown="1"); usualmente ≥ 3 -->
    <automated>grep -c "ZF (izquierda) amplifica ruido" index.md</automated>
    <!-- esperado: 0 — el caption viejo (2 paneles) fue reemplazado -->
  </verify>
  <acceptance_criteria>
    - El alt-text contiene "α[k]" o "alpha" y nombra "izquierda", "centro", "derecha": `grep -E "!\[.*α\[k\].*izquierda.*centro.*derecha\]\(figures/mmse-vs-zf-constellation.png\)" index.md` retorna 1 (o el equivalente con `\alpha` LaTeX si Claude opta por ese formato).
    - El caption nuevo contiene la palabra "tres" o nombra explícitamente "Izquierda" + "Centro" + "Derecha" como labels: `grep -cE "(\*\*Izquierda:\*\*|\*\*Centro:\*\*|\*\*Derecha:\*\*)" index.md` retorna ≥ 3.
    - El caption viejo de 2 paneles fue eliminado: `grep -c "ZF (izquierda) amplifica ruido" index.md` retorna 0.
    - El caption menciona el factor α[k] y su rango: `grep -c "factor de contracción\|α\[k\]\|alpha\[k\]\|\\\\alpha\[k\]" index.md` retorna ≥ 1.
    - La imagen sigue referenciada una sola vez: `grep -c "mmse-vs-zf-constellation.png" index.md` retorna 1.
    - El atributo `markdown="1"` en el figcaption está presente: `grep -c "figcaption markdown=\"1\".*Figura 3" index.md` retorna 1.
    - La estructura HTML exterior está intacta: `grep -c "<figure markdown=\"span\">" index.md` retorna el mismo número que en el estado inicial.
    - El caption menciona el comportamiento de las subportadoras débiles (fades): `grep -cE "fade|débil|amplificación" index.md` retorna ≥ 1.
  </acceptance_criteria>
  <done>
    El alt-text describe los 3 paneles explícitamente. El `<figcaption>` describe los 3 paneles en orden izquierda → centro → derecha, con el panel izquierdo identificado como factor de contracción α[k] y los dos siguientes como constelaciones ZF y MMSE. El caption viejo (de 2 paneles) ya no aparece en el archivo. La estructura HTML `<figure markdown="span">...<figcaption markdown="1">` se preservó.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown → MkDocs build | El `<figcaption markdown="1">` con LaTeX inline `$\alpha[k]$` solo se renderiza si el atributo `markdown="1"` se preserva. Romper la estructura `<figure>...</figure>` rompe el renderizado de la figura |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | `<figure>...</figure>` de Figura 3 | mitigate | Verificación `grep -c "<figure markdown=\"span\">" index.md` debe retornar el mismo número que antes; los tags abren y cierran balanceados |
| T-03-08 | Tampering | Atributo `markdown="1"` del figcaption | mitigate | Verificación `grep -c "figcaption markdown=\"1\".*Figura 3" index.md` retorna 1 — sin ese atributo el LaTeX inline `$\alpha[k]$` no se renderiza |
| T-03-09 | Information disclosure | Parentheticals §7 (referencias a §N) | accept | Riesgo bajo: las referencias son texto plano, sin enlaces; el único error posible (D5 → Sesión 07 externa vs §7 interna) está mitigado por la nota en RESEARCH.md Pitfall 6 |
| T-03-10 | Tampering | Otras figuras del documento (Figura 1, Figura 2) | mitigate | El edit es scoped a Figura 3 (`mmse-vs-zf-constellation.png`); verificación `grep -c "<figure markdown=\"span\">" index.md` confirma que el número total de figuras no cambió |
</threat_model>

<verification>
- `grep -c "mmse-vs-zf-constellation.png" index.md` retorna 1.
- `grep -c "ZF (izquierda) amplifica ruido" index.md` retorna 0 (caption viejo eliminado).
- `grep -cE "(\*\*Izquierda:\*\*|\*\*Centro:\*\*|\*\*Derecha:\*\*)" index.md` retorna ≥ 3.
- 5 parentheticals de §7 presentes (uno por dimensión, ver Task 1 verify).
- `grep -c "^\*\*Dimensión [1-5]:" index.md` retorna 5.
</verification>

<success_criteria>
- 5 dimensiones de §7 con referencia cruzada parenthetical (D-14).
- Figura 3 con caption + alt-text de 3 paneles (D-15).
- `index.md` sigue siendo válido MkDocs-Material (figcaption con `markdown="1"`).
- Cero modificaciones a otras figuras o secciones.
</success_criteria>

<output>
Crear `.planning/phases/03-mejora-de-narrativa/03-03-SUMMARY.md` al terminar con:
- Línea exacta de cada parenthetical insertado en §7
- Texto final del caption de Figura 3 (verbatim)
- Verificación de los conteos grep
- Confirmación de que la estructura `<figure>...</figure>` está intacta
</output>
