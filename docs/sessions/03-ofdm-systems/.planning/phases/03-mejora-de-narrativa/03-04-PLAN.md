---
phase: 03-mejora-de-narrativa
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - figures/cp-illustration.png
  - figures/ofdm-subcarriers.png
  - figures/zf-equalizer-effect.png
  - figures/zf-equalizer-qam-comparison.png
autonomous: false
requirements:
  - NARR-01
tags:
  - figures
  - assets
  - git

must_haves:
  truths:
    - "Cada uno de los 4 PNGs ha sido inspeccionado visualmente y se ha tomado una decisión explícita de commit o revert"
    - "Los PNGs que se commitean son mejoras de legibilidad (contraste, colormap turbo, ejes legibles)"
    - "Cualquier PNG que no mejore se revierte individualmente con `git checkout`"
    - "El working tree queda limpio respecto a los 4 PNGs (committed o reverted, no quedan modificaciones flotantes)"
  artifacts:
    - path: "figures/cp-illustration.png"
      provides: "Figura del CP — committed or reverted"
    - path: "figures/ofdm-subcarriers.png"
      provides: "Figura de subportadoras — committed or reverted"
    - path: "figures/zf-equalizer-effect.png"
      provides: "Figura del efecto ZF — committed or reverted"
    - path: "figures/zf-equalizer-qam-comparison.png"
      provides: "Figura ZF QAM — committed or reverted"
  key_links:
    - from: "PNGs en figures/"
      to: "referencias en index.md"
      via: "los nombres de archivo NO cambian — solo el contenido binario"
      pattern: "figures/(cp-illustration|ofdm-subcarriers|zf-equalizer-effect|zf-equalizer-qam-comparison)\\.png"
---

<objective>
Implementa D-16: inspección visual de los 4 PNGs modificados en el working tree (`cp-illustration.png`, `ofdm-subcarriers.png`, `zf-equalizer-effect.png`, `zf-equalizer-qam-comparison.png`) y decisión de commit o revert por archivo. El humano hace la inspección — Claude prepara los comandos de commit/revert según la decisión.

Purpose: Los 4 PNGs fueron re-generados con colormap turbo durante el trabajo previo. Si la nueva versión es una mejora de legibilidad (mayor contraste, mejores ejes, fondo blanco), se commitea; si no, se revierte. Esta decisión es visual y requiere humano.
Output: Decisión por archivo (commit o revert) ejecutada, working tree limpio respecto a estos 4 PNGs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mejora-de-narrativa/03-CONTEXT.md
@.planning/phases/03-mejora-de-narrativa/03-RESEARCH.md

<interfaces>
<!-- Estado del working tree (verificado por git diff --stat al momento de planning) -->

| Archivo | Tamaño antes | Tamaño después | Variación |
|---------|-------------|----------------|-----------|
| `cp-illustration.png` | 124 690 B | 123 047 B | −1.3% |
| `ofdm-subcarriers.png` | 386 283 B | 321 860 B | −16.7% |
| `zf-equalizer-effect.png` | 143 336 B | 145 468 B | +1.5% |
| `zf-equalizer-qam-comparison.png` | 426 252 B | 427 476 B | +0.3% |

Criterios de inspección visual (D-16):
1. ¿El contraste entre subportadoras fuertes/débiles es más claro con turbo que con el colormap anterior?
2. ¿Los ejes, labels y títulos son legibles?
3. ¿El fondo blanco está presente (no transparente, no gris)?

Si los tres criterios son SÍ → commitear. Si alguno falla → `git checkout figures/<nombre>.png` para revertir.

Los PNGs son referenciados desde `index.md` por nombre — los nombres NO cambian, solo el contenido binario. No hay riesgo de romper links.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Inspección visual de los 4 PNGs modificados (humano decide commit o revert por archivo)</name>
  <files>figures/cp-illustration.png, figures/ofdm-subcarriers.png, figures/zf-equalizer-effect.png, figures/zf-equalizer-qam-comparison.png</files>
  <read_first>
    - .planning/phases/03-mejora-de-narrativa/03-CONTEXT.md (D-16 protocolo de inspección y mensaje de commit)
    - .planning/phases/03-mejora-de-narrativa/03-RESEARCH.md (sección "4 PNGs modificados en working tree" con tabla de tamaños y protocolo de inspección)
  </read_first>
  <what-built>
    Los 4 PNGs (`cp-illustration.png`, `ofdm-subcarriers.png`, `zf-equalizer-effect.png`, `zf-equalizer-qam-comparison.png`) ya están modificados en el working tree desde antes de esta fase (re-generados con colormap turbo). Claude no los re-genera — solo prepara los comandos de commit o revert tras la decisión humana.
  </what-built>
  <how-to-verify>
    Para cada PNG, abrir tanto la versión actual (working tree) como la versión committed (HEAD) y compararlas:

    ```bash
    # Para cada archivo, sacar la versión committed a un temp file y abrirla:
    git show HEAD:figures/cp-illustration.png > /tmp/cp-illustration-OLD.png
    git show HEAD:figures/ofdm-subcarriers.png > /tmp/ofdm-subcarriers-OLD.png
    git show HEAD:figures/zf-equalizer-effect.png > /tmp/zf-equalizer-effect-OLD.png
    git show HEAD:figures/zf-equalizer-qam-comparison.png > /tmp/zf-equalizer-qam-comparison-OLD.png

    # Abrir cada par (OLD vs working tree) con tu visor preferido:
    xdg-open figures/cp-illustration.png &           # nueva versión
    xdg-open /tmp/cp-illustration-OLD.png &          # versión committed
    # ... repetir con los otros 3
    ```

    Para cada par, aplicar los 3 criterios D-16:
    1. ¿El contraste entre subportadoras fuertes/débiles es más claro con turbo que con el colormap anterior?
    2. ¿Los ejes, labels y títulos son legibles?
    3. ¿El fondo blanco está presente (no transparente, no gris)?

    Decisión por archivo:
    - **Sí a los tres** → commit (la nueva versión queda)
    - **No a alguno** → revert con `git checkout figures/<nombre>.png` (vuelve a HEAD)

    Reportar la decisión por archivo como lista, ejemplo:
    ```
    cp-illustration.png:           commit
    ofdm-subcarriers.png:          commit
    zf-equalizer-effect.png:       revert
    zf-equalizer-qam-comparison.png: commit
    ```
  </how-to-verify>
  <resume-signal>Reportar la decisión por archivo en formato `nombre.png: commit|revert`, o describir issues encontradas</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Ejecutar commit y/o revert según las decisiones del humano</name>
  <files>figures/cp-illustration.png, figures/ofdm-subcarriers.png, figures/zf-equalizer-effect.png, figures/zf-equalizer-qam-comparison.png</files>
  <read_first>
    - Mensaje del checkpoint Task 1 (decisión por archivo)
    - .planning/phases/03-mejora-de-narrativa/03-CONTEXT.md (D-16 mensaje de commit recomendado: `fig(03): commit re-generated figures with turbo colormap`)
  </read_first>
  <action>
Para cada PNG marcado como `revert`, ejecutar:
```bash
git checkout HEAD -- figures/<nombre>.png
```

Para los PNGs marcados como `commit`, hacer un único commit con todos juntos:
```bash
git add figures/<nombre-1>.png figures/<nombre-2>.png ...
git commit -m "fig(03): commit re-generated figures with turbo colormap"
```

Si los 4 son `revert` → no hay commit; ejecutar `git checkout HEAD -- figures/{cp-illustration,ofdm-subcarriers,zf-equalizer-effect,zf-equalizer-qam-comparison}.png` y reportar que ningún PNG cambió.

Si los 4 son `commit` → el mensaje de commit usa el formulario verbatim de D-16: `fig(03): commit re-generated figures with turbo colormap`. El co-author tag (`Co-Authored-By: Claude...`) se agrega según las reglas estándar de GSD (ver el bloque de commits del system prompt).

Tras el commit/revert, verificar que el working tree queda limpio respecto a estos 4 archivos:
```bash
git status --porcelain figures/cp-illustration.png figures/ofdm-subcarriers.png figures/zf-equalizer-effect.png figures/zf-equalizer-qam-comparison.png
```
La salida debe estar vacía (o tener solo archivos que ya estaban modificados desde antes).

NO modifiques otros archivos en `figures/`. NO modifiques `index.md` (las referencias por nombre son estables porque los nombres de archivo no cambian).
  </action>
  <verify>
    <automated>git status --porcelain figures/cp-illustration.png figures/ofdm-subcarriers.png figures/zf-equalizer-effect.png figures/zf-equalizer-qam-comparison.png | wc -l</automated>
    <!-- esperado: 0 — ninguno de los 4 PNGs queda en estado modificado tras commit/revert -->
    <automated>git log -1 --format=%s -- figures/ 2>/dev/null | grep -c "turbo colormap" || true</automated>
    <!-- esperado: ≥ 0 (1 si al menos un PNG se commiteó, 0 si todos se revirtieron) -->
  </verify>
  <acceptance_criteria>
    - `git status --porcelain figures/cp-illustration.png figures/ofdm-subcarriers.png figures/zf-equalizer-effect.png figures/zf-equalizer-qam-comparison.png` retorna salida vacía (los 4 archivos están limpios).
    - Si hubo al menos un `commit`: `git log -1 --format=%s` (en el HEAD reciente afectado a figures/) contiene la cadena `fig(03)` y `turbo colormap`.
    - Si todos fueron `revert`: no se crea ningún commit nuevo en esta tarea.
    - `index.md` no fue modificado en esta tarea: `git diff --name-only HEAD~1 HEAD 2>/dev/null | grep -v "^figures/" | grep -v "^.planning/"` debe estar vacío (o no incluir `index.md`).
    - Las 4 rutas de referencia en `index.md` siguen apuntando a archivos existentes: `for f in cp-illustration ofdm-subcarriers zf-equalizer-effect zf-equalizer-qam-comparison; do test -f figures/$f.png && echo "ok $f"; done` retorna 4 líneas `ok`.
  </acceptance_criteria>
  <done>
    Cada PNG está en su estado decidido (committed o reverted). Working tree limpio respecto a los 4 archivos. Si hubo commit, el mensaje sigue el formato `fig(03): commit re-generated figures with turbo colormap`. Las 4 rutas de imagen referenciadas en index.md siguen siendo válidas (archivos existentes).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Working tree → git history | Un commit irreversible sin inspección visual puede dejar figuras peores en el repo. La inspección humana es el único gate posible para juicio visual |
| Figures directory → index.md references | Los nombres de archivo en `figures/` deben mantenerse exactamente para no romper links en `index.md` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering | PNG binarios en figures/ | mitigate | Inspección visual humana (Task 1 checkpoint blocking) — sin aprobación no se commitea; revert individual posible con `git checkout` por archivo |
| T-03-12 | Information disclosure | Links rotos en index.md por nombres de archivo | accept | Los nombres NO cambian — solo el contenido binario; acceptance criteria verifica que los 4 archivos siguen existiendo en `figures/` |
| T-03-13 | Denial of service | git history corruption por commit incorrecto | accept | El commit usa nombres de archivo específicos (no `git add -A`); el revert individual con `git checkout` es seguro |
</threat_model>

<verification>
- `git status --porcelain` no muestra ninguno de los 4 PNGs como modificado al cerrar el plan.
- Las 4 rutas referenciadas en `index.md` siguen existiendo: `ls figures/cp-illustration.png figures/ofdm-subcarriers.png figures/zf-equalizer-effect.png figures/zf-equalizer-qam-comparison.png` retorna las 4 sin error.
- Si hubo commit: el mensaje contiene `fig(03)` y `turbo colormap`.
</verification>

<success_criteria>
- Decisión humana explícita registrada por archivo (commit o revert).
- Working tree limpio respecto a los 4 PNGs.
- Las referencias de imagen en `index.md` siguen siendo válidas.
- Si hubo commit, el mensaje sigue D-16: `fig(03): commit re-generated figures with turbo colormap`.
</success_criteria>

<output>
Crear `.planning/phases/03-mejora-de-narrativa/03-04-SUMMARY.md` al terminar con:
- Decisión por archivo (commit o revert)
- Hash del commit si se hizo
- Confirmación de working tree limpio
- Nota sobre criterios D-16 aplicados
</output>
