---
phase: 03-mejora-de-narrativa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.md
autonomous: true
requirements:
  - NARR-01
tags:
  - narrative
  - markdown
  - mkdocs

must_haves:
  truths:
    - "Cada bloque §4.1–§4.8 termina con un párrafo de transición pregunta-respuesta antes del `---` que lo cierra"
    - "El cierre de §4 contiene un bloque de código que demuestra la cadena completa bits→bits_hat con comentarios por sección"
    - "El bug WR-01 (doble `---` entre §4.8 y §5) está corregido — queda un único `---`"
    - "La transición existente 4.5→4.6 (líneas 784–785) permanece intacta"
  artifacts:
    - path: "index.md"
      provides: "7 transiciones nuevas + 1 bloque de cierre §4 + corrección WR-01"
      contains: "La pregunta natural es"
  key_links:
    - from: "§4.8 (línea ~907)"
      to: "§5 (línea 944)"
      via: "transición de cierre + snippet de cadena completa + un único `---`"
      pattern: "qpsk_demap\\(X_hat\\)"
    - from: "snippet de cierre §4"
      to: "funciones definidas en §4.1–§4.8"
      via: "nombres de variables D-08 (X, x_cp, y_noisy, Y, X_hat, bits_hat)"
      pattern: "X\\s*=\\s*qpsk_map\\(bits\\)"
---

<objective>
Fortalecer el hilo conductor de §4 insertando las 6 transiciones ausentes (4.1→4.2, 4.2→4.3, 4.3→4.4, 4.4→4.5, 4.6→4.7, 4.7→4.8), agregar el bloque de cierre §4 (nota D-07 + snippet de cadena completa D-06/D-08) entre §4.8 y §5, y corregir el bug WR-01 (doble `---`). Implementa NARR-01 + extensión.

Purpose: Cada bloque de §4 nombrará el problema que resuelve y la pregunta que deja pendiente; al final de §4 el estudiante verá la cadena bits→bits_hat completa, el mismo sistema que §5 medirá con BER.
Output: `index.md` con 7 nuevos párrafos de transición, un bloque de código de ~10 líneas con su nota previa, y un único `---` entre §4 y §5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-mejora-de-narrativa/03-CONTEXT.md
@.planning/phases/03-mejora-de-narrativa/03-RESEARCH.md
@.planning/phases/03-mejora-de-narrativa/03-PATTERNS.md

<interfaces>
<!-- Patrón canónico verbatim — todas las transiciones nuevas lo siguen estructuralmente -->
<!-- Fuente: index.md líneas 784–785 (NO MODIFICAR) -->

Transición template (§4.5→4.6 — ya existente, no se toca):
```
La pregunta natural es: ¿existe un ecualizador que sea más inteligente en esas
subportadoras? En lugar de invertir el canal ciegamente, ¿podría detectar que una
subportadora está muy atenuada y moderar su respuesta para no amplificar el ruido?
La respuesta es sí, y ese ecualizador es el MMSE.
```

Estructura: (1) "La pregunta natural es: ¿[problema pendiente del bloque actual]?" — (2) opcional matiz/elaboración — (3) "La respuesta es [respuesta breve], y [bloque siguiente]."

Funciones disponibles para el snippet de cierre (verificadas en lab.ipynb Cells 8–24):
- `qpsk_map(bits) → X: ndarray[complex]`             (§4.1)
- `ofdm_tx(X, N_CP) → x_cp: ndarray`                 (§4.2)
- `apply_channel(x, h) → y: ndarray` (conv lineal)   (§4.3)
- `ofdm_rx_no_channel(y, N, N_CP) → Y: ndarray`      (§4.4)
- `zf_equalizer(Y, h, N) → X_hat: ndarray`           (§4.5)
- `qpsk_demap(X_hat) → bits: ndarray` (hard, Gray)   (§4.8, callable en Cell 22)

Ubicaciones verificadas de separadores §4 (índices de línea actuales):
| Cierra bloque | Línea `---` |
|---------------|-------------|
| §4.1          | 591         |
| §4.2          | 635         |
| §4.3          | 669         |
| §4.4          | 696         |
| §4.5          | 786 (YA tiene transición — no insertar) |
| §4.6          | 827         |
| §4.7          | 905         |
| §4.8 (bug)    | 940 + 942 (doble — eliminar el 942) |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insertar las 6 transiciones intermedias §4 (4.1→4.2, 4.2→4.3, 4.3→4.4, 4.4→4.5, 4.6→4.7, 4.7→4.8)</name>
  <files>index.md</files>
  <read_first>
    - index.md (verificar contexto alrededor de líneas 591, 635, 669, 696, 827, 905 antes de insertar — las líneas pueden haberse desplazado tras ediciones previas; usar `grep -n "^---$"` o `grep -n "^#### 4\."` para localizar exactamente)
    - .planning/phases/03-mejora-de-narrativa/03-CONTEXT.md (D-01 estilo pregunta-respuesta, D-02 ubicación antes del `---`, D-05 lista de transiciones delegadas, D-03 transición 4.6→4.7 con frase específica)
    - .planning/phases/03-mejora-de-narrativa/03-RESEARCH.md (sección "NARR-01: Transiciones §4", notas individuales por transición con qué problema cierra cada bloque)
    - .planning/phases/03-mejora-de-narrativa/03-PATTERNS.md (PATTERN 1 — template canónico + tabla "Contenido por transición")
    - lab.ipynb (solo si se necesita verificar firma de alguna función mencionada en una transición)
  </read_first>
  <action>
Insertar 6 párrafos de transición en `index.md`, cada uno inmediatamente antes del `---` que cierra el bloque origen y separado por una línea en blanco del párrafo anterior. Cada transición sigue el patrón pregunta-respuesta del template canónico (PATTERN 1 en PATTERNS.md, líneas 784–785 de index.md). La transición existente 4.5→4.6 (líneas 784–785) NO se modifica.

Localiza cada inserción usando `grep -n "^---$" index.md` (los `---` listados en `<interfaces>` arriba), pero verifica el contexto inmediato (el `---` anterior debe estar precedido por el verification block del bloque que cierra; consulta la tabla de "Cierra bloque" en RESEARCH.md líneas 149–157).

Contenido temático por transición (de PATTERNS.md PATTERN 1 + RESEARCH.md notas 4.1→…→4.7→4.8):
- **4.1→4.2 (antes del `---` ~línea 591)** — `X` contiene N símbolos en frecuencia pero no es aún una señal transmisible en tiempo; el siguiente bloque (§4.2 IFFT + CP) convierte frecuencia→tiempo. Pregunta natural: ¿cómo convertir esos N números complejos en una señal banda-base muestreada lista para transmitir?
- **4.2→4.3 (antes del `---` ~línea 635)** — `x_cp` ya es banda-base discreta pero el canal real la deformará; el bloque siguiente (§4.3) modela esa deformación. Pregunta natural: ¿qué le hace al símbolo OFDM el canal multipath de la Sesión 01?
- **4.3→4.4 (antes del `---` ~línea 669)** — `y_noisy` lleva mezclados ecos de símbolos pasados; el CP debe descartarse y las subportadoras separarse antes de poder ecualizar. Pregunta natural: ¿cómo se aprovecha la convolución circular del CP para volver al dominio de subportadoras independientes?
- **4.4→4.5 (antes del `---` ~línea 696)** — `Y[k] = H[k]·X[k] + W[k]`; sin corregir la distorsión por subportadora el detector recibe una nube dispersa. Pregunta natural: ¿cómo invertir la distorsión por subportadora cuando cada `H[k]` es distinto?
- **4.6→4.7 (antes del `---` ~línea 827)** — D-03 (cita específica): "ZF y MMSE asumen que H[k] es conocido. En la práctica nadie nos lo da — hay que estimarlo. El siguiente bloque muestra cómo." Articular en estilo pregunta-respuesta del template: ZF y MMSE calculan `H = np.fft.fft(h_channel, n=N)` asumiendo h_channel conocido (líneas 758 y 809 de index.md); pero en un receptor real h_channel no se conoce. ¿Cómo se obtiene H[k] en la práctica? La respuesta son los pilotos del §4.7.
- **4.7→4.8 (antes del `---` ~línea 905)** — `H_est` permite ecualizar y producir `X_hat`, pero `X_hat` sigue siendo un número complejo; falta convertirlo en bits. Pregunta natural: ¿cómo se decide qué símbolo de la constelación corresponde a cada `X_hat` ruidoso?

Cada transición: 1–4 oraciones, texto plano corriente (sin admonition, sin negrita estructural, sin código). La transición a 4.6→4.7 usa la frase D-03 como núcleo pero re-articulada al template pregunta-respuesta.

NO modifiques la transición existente 4.5→4.6 (líneas 784–785). NO insertes la transición de cierre §4.8→§5 en esta tarea — eso pertenece a la Task 2. NO toques los `---` separadores.
  </action>
  <verify>
    <automated>grep -c "La pregunta natural es" index.md</automated>
    <!-- esperado: ≥ 7 (la existente §4.5 + las 6 nuevas) -->
    <automated>grep -c "La respuesta es" index.md</automated>
    <!-- esperado: ≥ 7 -->
    <automated>grep -n "^#### 4\." index.md | wc -l</automated>
    <!-- esperado: 8 — verifica que ningún `####` se perdió ni se duplicó -->
    <automated>sed -n '780,790p' index.md | grep -c "más inteligente en esas"</automated>
    <!-- esperado: 1 — la transición 4.5→4.6 existente no se borró -->
  </verify>
  <acceptance_criteria>
    - `grep -c "La pregunta natural es" index.md` retorna un número ≥ 7 (6 nuevas transiciones + 1 existente §4.5→4.6).
    - `grep -c "La respuesta es" index.md` retorna un número ≥ 7.
    - `sed -n '780,790p' index.md | grep "más inteligente en esas"` retorna 1 línea (la transición existente §4.5 sigue intacta).
    - `grep -n "ZF y MMSE asumen\|H\[k\] es conocido\|hay que estimarlo" index.md` retorna ≥ 1 match (transición 4.6→4.7 articula el gap D-03).
    - Las 8 subsecciones siguen presentes: `grep -c "^#### 4\." index.md` retorna 8.
    - Ningún `---` separador fue eliminado en §4 (esta tarea no toca el bug WR-01): `grep -c "^---$" index.md` retorna 27 (sin cambios respecto al estado inicial).
  </acceptance_criteria>
  <done>
    6 transiciones nuevas insertadas en estilo pregunta-respuesta, cada una antes del `---` que cierra su bloque, con espacio en blanco antes y después. La transición existente §4.5→4.6 intacta. El recuento de subsecciones §4.x es 8. El recuento total de `---` separadores no cambió.
  </done>
</task>

<task type="auto">
  <name>Task 2: Insertar bloque de cierre §4 (nota D-07 + snippet de cadena completa D-06/D-08 + transición D-04) y corregir bug WR-01</name>
  <files>index.md</files>
  <read_first>
    - index.md (líneas ~907–944: §4.8 QAM Demapper + Figura 3 + bug WR-01 + comienzo de §5; usar `grep -n "^---$\|^### 5\.\|mmse-vs-zf-constellation" index.md` para localizar)
    - .planning/phases/03-mejora-de-narrativa/03-CONTEXT.md (D-04 transición 4.8→§5, D-06 snippet de cadena completa, D-07 nota previa al snippet, D-08 nombres de variables, D-09 corrección WR-01)
    - .planning/phases/03-mejora-de-narrativa/03-RESEARCH.md (sección "NARR-01 Extensión: Bloque de cierre §4" con cadena Cell 24 verificada; sección "WR-01: Bug del doble `---`" con líneas 940–942)
    - .planning/phases/03-mejora-de-narrativa/03-PATTERNS.md (PATTERN 2 separador único, PATTERN 3 estructura interna §4, PATTERN 5 snippet con comentarios `# §X.Y` + nota D-07 como párrafo plano NO admonition)
    - lab.ipynb (Cell 24 `ofdm_ber_quick` — verificar el orden exacto de la cadena bits→X→x_cp→y_noisy→Y→X_hat→bits_hat; Cell 22 firma de `qpsk_demap`)
  </read_first>
  <action>
Reemplazar el rango entre `</figure>` de Figura 3 (línea ~938) y `### 5. Rendimiento End-to-End` (línea ~944) con la siguiente secuencia:

1. **Línea en blanco** tras `</figure>`.
2. **Párrafo de transición** (D-04, estilo pregunta-respuesta del PATTERN 1): articular que con el demapper la cadena completa está cerrada y la pregunta natural es ¿qué tan bien funciona el sistema? La respuesta es la curva BER de §5. Texto plano corriente, 2–4 oraciones.
3. **Línea en blanco**.
4. **Párrafo de nota D-07** (texto plano corriente, NO admonition colapsable — ver PATTERN 5): introduce el snippet diciendo que es el transceptor OFDM sin codificación de canal y que la BER de §5 mide exactamente este sistema; menciona que LDPC (Sesión 04) se añade como capa superior. Formulación de referencia (Claude puede adaptarla manteniendo el contenido): "Con todos los bloques en su lugar, la cadena completa puede ejecutarse de principio a fin. Este snippet es el transceptor OFDM sin codificación de canal — exactamente el sistema que §5 mide con la curva BER. La codificación LDPC (Sesión 04) se agrega como una capa superior a esta cadena."
5. **Línea en blanco**.
6. **Bloque de código Python** (D-06 + D-08, ver snippet de referencia en PATTERNS.md PATTERN 5 y RESEARCH.md "Code Examples"). Estructura exacta:

```python
# Transceptor OFDM uncoded — exactamente lo que §5 mide con la curva BER
bits     = rng.integers(0, 2, N * 2)               # bits aleatorios (2 bits/símbolo QPSK)
X        = qpsk_map(bits)                           # §4.1  mapper
x_cp     = ofdm_tx(X, N_CP)                         # §4.2  IFFT + CP
y_noisy  = apply_channel(x_cp, h_channel) + noise   # §4.3  canal + AWGN
Y        = ofdm_rx_no_channel(y_noisy, N, N_CP)     # §4.4  eliminar CP + FFT
X_hat    = zf_equalizer(Y, h_channel, N)            # §4.5  ecualizador ZF
bits_hat = qpsk_demap(X_hat)                        # §4.8  demapper
ber      = np.mean(bits != bits_hat)
```

Los nombres de variable son los de D-08, verificados contra Cell 24 del notebook. Los comentarios `# §4.X` etiquetan cada paso. Usar `zf_equalizer` (no MMSE) por simplicidad pedagógica — el lector ya conoce ambos.

7. **Línea en blanco**.
8. **Un único `---`** (corrige el bug WR-01: actualmente las líneas 940 y 942 son ambas `---` con la 941 vacía; tras la edición debe quedar solo uno).
9. **Línea en blanco**.
10. `### 5. Rendimiento End-to-End` sin tocar.

Importante (Pitfall 3 de RESEARCH.md): la inserción del bloque de cierre y la corrección del bug WR-01 ocurren en la misma operación de edición — no dejar tres `---` consecutivos. NO insertar `??? note` colapsable para la nota D-07 (Pitfall 2: el estudiante podría colapsarla). NO renombrar variables — `bits`, `X`, `x_cp`, `y_noisy`, `Y`, `X_hat`, `bits_hat` son los nombres canonical de D-08 (Pitfall 4).

NO modifiques la Figura 3 ni su caption (eso es Plan 03). NO modifiques §5 ni nada más allá del `### 5. Rendimiento End-to-End`.
  </action>
  <verify>
    <automated>grep -c "qpsk_demap(X_hat)" index.md</automated>
    <!-- esperado: ≥ 1 -->
    <automated>grep -c "Transceptor OFDM uncoded" index.md</automated>
    <!-- esperado: 1 -->
    <automated>grep -c "Sesión 04\|LDPC" index.md</automated>
    <!-- esperado: ≥ 1 (la nota D-07 menciona LDPC y Sesión 04) -->
    <automated>awk '/^---$/{c++} END{print c}' index.md</automated>
    <!-- esperado: 26 (estado inicial 27, menos 1 por la corrección WR-01) -->
    <automated>awk '/^---$/ {if (prev_was_dash || prev_was_blank_after_dash) print NR": consecutive ---"; prev_was_dash=1; prev_was_blank_after_dash=0; next} /^$/ {if (prev_was_dash) prev_was_blank_after_dash=1; else prev_was_blank_after_dash=0; prev_was_dash=0; next} {prev_was_dash=0; prev_was_blank_after_dash=0}' index.md | wc -l</automated>
    <!-- esperado: 0 — ningún par de `---` separado solo por línea vacía -->
    <automated>awk '/### 5\. Rendimiento End-to-End/{e=NR} /<\/figure>/{f=NR} END{print e-f}' index.md</automated>
    <!-- esperado: > 6 — debe haber al menos 6 líneas (transición + nota + snippet de 9 líneas + `---`) entre figcaption close y §5 -->
  </verify>
  <acceptance_criteria>
    - `grep -c "qpsk_demap(X_hat)" index.md` retorna ≥ 1 (snippet contiene la llamada al demapper).
    - `grep -c "qpsk_map(bits)" index.md` retorna ≥ 1.
    - `grep -c "ofdm_tx(X, N_CP)" index.md` retorna ≥ 1.
    - `grep -c "apply_channel(x_cp, h_channel)" index.md` retorna ≥ 1.
    - `grep -c "ofdm_rx_no_channel(y_noisy, N, N_CP)" index.md` retorna ≥ 1.
    - `grep -c "zf_equalizer(Y, h_channel, N)" index.md` retorna ≥ 1.
    - Los comentarios de sección están presentes: `grep -E "# §4\.[1-8]" index.md | wc -l` retorna ≥ 6.
    - El comentario header está presente: `grep "Transceptor OFDM uncoded" index.md` retorna 1 línea.
    - La nota D-07 menciona LDPC y Sesión 04: `grep "LDPC" index.md` y `grep "Sesión 04" index.md` retornan ≥ 1 línea cada uno.
    - Bug WR-01 corregido: `awk '/^---$/{c++} END{print c}' index.md` retorna 26 (estado inicial 27 menos 1).
    - Cero pares de `---` consecutivos separados solo por línea vacía en todo el archivo.
    - La transición D-04 contiene la pregunta de cierre §4: `grep -E "(cuán bien|qué tan bien|¿qué tal funciona|BER de §5|curva BER)" index.md | wc -l` retorna ≥ 1.
    - `### 5. Rendimiento End-to-End` sigue presente exactamente una vez: `grep -c "^### 5\. Rendimiento End-to-End$" index.md` retorna 1.
  </acceptance_criteria>
  <done>
    El bloque de cierre §4 (transición + nota + snippet de 9 líneas + `---` único) está insertado entre `</figure>` de Figura 3 y `### 5. Rendimiento End-to-End`. El bug WR-01 está corregido (un solo `---`). La nota es párrafo plano (no admonition). Los nombres de variable del snippet coinciden con D-08. El recuento total de `---` bajó de 27 a 26.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown → MkDocs build | El archivo `index.md` es renderizado por MkDocs-Material; markup malformado (admonition mal cerrada, fenced code sin lenguaje, separadores múltiples) puede romper el sitio o producir renderizado incorrecto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `---` separadores en §4 | mitigate | Tras edit, ejecutar `awk '/^---$/{c++} END{print c}' index.md` y comparar con el conteo esperado (26 tras Task 2); verificar que no quedan pares `---`/blank/`---` consecutivos |
| T-03-02 | Tampering | Transición existente 4.5→4.6 (líneas 784–785) | mitigate | Verificación `sed -n '780,790p' index.md | grep "más inteligente en esas"` debe retornar 1 línea — no borrar accidentalmente la transición canónica |
| T-03-03 | Information disclosure | Nombres de variable en el snippet | accept | Riesgo bajo: usar los nombres D-08 (`X`, `x_cp`, `y_noisy`, `Y`, `X_hat`, `bits_hat`) elimina ambigüedad pedagógica; la verificación grep confirma cada llamada literal |
| T-03-04 | Denial of service | MkDocs build | mitigate | Estructura del fenced code block (` ```python ... ``` `) es estándar; ningún cambio estructural; admonitions no se introducen en este plan |
</threat_model>

<verification>
- `awk '/^---$/{c++} END{print c}' index.md` retorna **26** (el estado inicial es 27; el bug WR-01 elimina exactamente 1).
- `grep -c "La pregunta natural es" index.md` retorna **≥ 8** (6 transiciones intermedias + 1 cierre §4.8→§5 + 1 existente §4.5→4.6).
- `grep -c "La respuesta es" index.md` retorna **≥ 8**.
- `grep -c "^#### 4\." index.md` retorna **8** (ninguna subsección §4.x perdida ni duplicada).
- `grep "qpsk_demap(X_hat)" index.md` retorna ≥ 1 línea (snippet de cierre presente).
- Cero pares de `---` consecutivos en todo el archivo.
- `sed -n '780,790p' index.md | grep "más inteligente en esas"` retorna 1 línea (transición canónica §4.5→4.6 intacta).
</verification>

<success_criteria>
- 6 transiciones nuevas (4.1→4.2, 4.2→4.3, 4.3→4.4, 4.4→4.5, 4.6→4.7, 4.7→4.8) presentes en estilo pregunta-respuesta.
- 1 transición de cierre §4.8→§5 (D-04) presente.
- 1 bloque de cierre §4 (nota D-07 párrafo plano + snippet D-06 de 9 líneas con comentarios `# §4.X`) entre `</figure>` y `### 5. Rendimiento End-to-End`.
- Bug WR-01 corregido: un único `---` entre §4 y §5.
- Transición existente §4.5→4.6 (líneas ~784–785) sin modificar.
- `index.md` sigue siendo válido MkDocs-Material (estructura de fenced code y admonitions intactas).
</success_criteria>

<output>
Crear `.planning/phases/03-mejora-de-narrativa/03-01-SUMMARY.md` al terminar con:
- Líneas exactas donde quedó cada transición insertada
- Snippet final (10 líneas con comentarios `# §4.X`)
- Verificación de los conteos grep
- Observaciones de la nota D-07 (formato párrafo plano)
- Nota sobre la transición existente §4.5→4.6 preservada
</output>
