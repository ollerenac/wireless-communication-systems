---
phase: 04-revisi-n-final
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md
autonomous: true
requirements:
  - revisión transversal (NARR-01, NARR-02, NARR-03, CORR-01, CORR-02, CORR-03)
tags:
  - verification
  - structural-check
  - publishability-report
  - mkdocs

must_haves:
  truths:
    - "El reporte de publicabilidad existe en .planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md con un veredicto explícito por check (✅/⚠️/❌)"
    - "El reporte contiene el texto verbatim de las 6 transiciones nuevas de §4 (4.1→4.2, 4.2→4.3, 4.3→4.4, 4.4→4.5, 4.6→4.7, 4.7→4.8) en secuencia leíble"
    - "El reporte destaca explícitamente la transición §4.6→§4.7 como ítem human_needed proveniente de la Fase 3 (D-06)"
    - "Todas las referencias `figures/...png` en index.md resuelven a archivos existentes en figures/ (0 referencias rotas)"
    - "El conteo de separadores `---` en index.md no regresó (sigue siendo 26 tras Fase 3)"
    - "Hay 8 transiciones §4 con el patrón pregunta-respuesta presentes (6 nuevas + 1 existente 4.5→4.6 + 1 cierre §4→§5)"
    - "Hay ≥ 5 parenthéticals `(§...)` en la zona de §7 Síntesis (uno por cada dimensión)"
  artifacts:
    - path: ".planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md"
      provides: "Reporte de publicabilidad para barrido humano del profesor"
      contains: "Transición §4.6→§4.7 (HUMAN_NEEDED)"
  key_links:
    - from: "Referencias `figures/...png` en index.md"
      to: "Archivos PNG en figures/"
      via: "verificación de existencia disco vs grep en index.md"
      pattern: "figures/.*\\.png"
    - from: "5 parenthéticals §7"
      to: "Secciones §2, §3, §4.5, §4.6, §4.7, §7"
      via: "grep `(§` en bloque §7 Síntesis"
      pattern: "\\(§"
---

<objective>
Ejecutar la verificación estructural automática de `index.md` y producir el `04-PUBLISHABILITY-REPORT.md` que el profesor usará para el barrido humano final. La verificación cubre las cuatro palancas declaradas en D-03: referencias de figura, separadores `---`, transiciones §4 (con patrón pregunta-respuesta), y parenthéticals de §7. El reporte incluye además el texto verbatim de las 6 transiciones §4 en secuencia (D-05) y destaca la transición §4.6→§4.7 marcada human_needed en Fase 3 (D-06).

Purpose: El profesor recibe un único documento legible donde puede leer las 6 transiciones de corrido (detectar saltos de tono) y validar la transición human_needed sin necesidad de abrir `index.md`.

Output: `.planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md` con tabla de checks (✅/⚠️/❌), bloque "Transiciones §4 en secuencia (verbatim)" y bloque destacado "Transición §4.6→§4.7 (HUMAN_NEEDED)".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-revisi-n-final/04-CONTEXT.md
@.planning/phases/03-mejora-de-narrativa/03-VERIFICATION.md
@.planning/phases/03-mejora-de-narrativa/03-CONTEXT.md

<interfaces>
<!-- Patrones de verificación canónicos -->

Estado esperado de `index.md` (verificado al cierre de Fase 3):
- 1357 líneas
- 26 separadores `^---$` (`awk '/^---$/{c++} END{print c}'`)
- 8 ocurrencias de "La pregunta natural es" (6 nuevas §4 + 1 existente §4.5→4.6 + 1 cierre §4.8→§5)
- 8 ocurrencias de "La respuesta es"
- 13 referencias `figures/*.png` (todas resueltas)
- 5 parenthéticals `(§...)` en §7 Síntesis: `(§2 y §4)`, `(§3)`, `(§2)`, `(§4.5, §4.6, §4.7)`, `(§7)`

Figuras presentes en disco (17 PNG en figures/ — confirmado pre-Fase 4):
ofdm-ber.png, ofdm-ber-equalizers.png, ofdm-per-subcarrier-ber.png, ofdm-subcarriers.png, ofdm-ifft-transmitter.png, ofdm-time-domain.png, cp-illustration.png, cp-effect-constellation.png, isi-problem.png, zf-noise-amplification.png, zf-equalizer-effect.png, zf-equalizer-qam-comparison.png, mmse-vs-zf-constellation.png, qpsk-decision-regions.png, lte-resource-grid-pilots.png, channel-estimation-ls.png, channel-estimation-pilots.png

Texto actual de la transición §4.6→§4.7 (línea ~835 de index.md — fuente: 03-VERIFICATION.md item #5):
"tanto el ZF como el MMSE calculan H[k] a partir de `h_channel` — asumiendo que el canal es perfectamente conocido por el receptor. En la práctica nadie entrega ese vector al receptor. ¿Cómo se obtiene H[k] cuando el canal es desconocido? La respuesta es transmitir símbolos piloto conocidos en posiciones conocidas, y estimar el canal a partir de ellos."

Template canónico §4.5 (NO debe haber cambiado):
"La pregunta natural es: ¿existe un ecualizador que sea más inteligente en esas subportadoras? En lugar de invertir el canal ciegamente, ¿podría detectar que una subportadora está muy atenuada y moderar su respuesta para no amplificar el ruido? La respuesta es sí, y ese ecualizador es el MMSE."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ejecutar los 4 checks estructurales automáticos sobre index.md (D-03)</name>
  <files>(solo lectura — no se modifica index.md)</files>
  <read_first>
    - index.md (el documento bajo verificación — leerlo de principio a fin para entender el estado actual y registrar cualquier anomalía visible)
    - .planning/phases/04-revisi-n-final/04-CONTEXT.md (D-03 lista exacta de los 4 checks; D-04 foco especial §4; D-05 estructura del reporte)
    - .planning/phases/03-mejora-de-narrativa/03-VERIFICATION.md (resultados esperados de Fase 3 — punto de partida para los conteos)
  </read_first>
  <action>
Ejecutar los 4 checks estructurales sobre `index.md` y registrar el resultado de cada uno con veredicto (✅ PASS / ⚠️ WARN / ❌ FAIL) y la evidencia exacta (comando + output). Esta tarea NO modifica `index.md` — solo recolecta evidencia para el reporte. Guardar los resultados intermedios en memoria de trabajo del agente o en un archivo temporal para que la Task 2 los consuma.

**Check 1 — Referencias de figura resueltas:**
- Extraer todas las referencias: `grep -oE "figures/[a-zA-Z0-9_-]+\.png" index.md | sort -u`
- Listar archivos en disco: `ls -1 figures/*.png | sort -u`
- Cruzar ambas listas. Toda referencia debe existir en disco. Toda figura en disco debe (idealmente) estar referenciada al menos una vez — registrar huérfanas como ⚠️ no como ❌ (Fase 1 ya las catalogó).
- Veredicto:
  - ✅ si toda referencia resuelve y no hay huérfanas
  - ⚠️ si toda referencia resuelve pero hay PNG huérfanas en disco
  - ❌ si alguna referencia no resuelve

**Check 2 — Separadores `---` sin regresión:**
- `awk '/^---$/{c++} END{print c}' index.md` debe retornar 26 (estado al cierre de Fase 3, post-WR-01)
- Ejecutar también el chequeo de pares consecutivos: `awk '/^---$/ {if (prev_was_dash || prev_was_blank_after_dash) print NR": consecutive ---"; prev_was_dash=1; prev_was_blank_after_dash=0; next} /^$/ {if (prev_was_dash) prev_was_blank_after_dash=1; else prev_was_blank_after_dash=0; prev_was_dash=0; next} {prev_was_dash=0; prev_was_blank_after_dash=0}' index.md`
- Veredicto:
  - ✅ si conteo = 26 y cero pares consecutivos
  - ❌ si conteo ≠ 26 o hay pares consecutivos (indica regresión de WR-01)

**Check 3 — 6 transiciones §4 nuevas + 2 preexistentes/cierre presentes con patrón pregunta-respuesta:**
- `grep -c "La pregunta natural es" index.md` debe retornar 8
- `grep -c "La respuesta es" index.md` debe retornar 8
- `grep -n "La pregunta natural es" index.md` debe listar 8 líneas dentro del rango de §4 (las líneas ~591, 637, 673, 702, 792, 835, 915, 952 según 03-VERIFICATION.md)
- Veredicto:
  - ✅ si ambos conteos son 8 y todas las líneas están dentro de §4
  - ⚠️ si conteos son ≥ 7 pero < 8 (registrar cuál falta)
  - ❌ si conteo < 7

**Check 4 — 5 parenthéticals §7 Síntesis presentes:**
- Localizar el rango de §7: `grep -n "^## .*Síntesis\|^## 7\|^### 7\." index.md` (registrar las líneas)
- Dentro del rango §7, contar parenthéticals con sección referenciada: `awk '/^## .*[Ss]íntesis|^## 7|^### 7\./{in=1} in==1 {print}' index.md | grep -cE "\(§[0-9]"` debe retornar ≥ 5
- Verificar individualmente los 5 esperados (D-14 de Fase 3):
  - `grep -F "(§2 y §4)" index.md` retorna ≥ 1
  - `grep -F "(§3)" index.md` retorna ≥ 1 (Dimensión 2 — CP / numerologías)
  - `grep -F "(§2)" index.md` retorna ≥ 1 (Dimensión 3 — FFT)
  - `grep -F "(§4.5, §4.6, §4.7)" index.md` retorna ≥ 1
  - `grep -F "(§7)" index.md` retorna ≥ 1 (Dimensión 5 — PAPR)
- Veredicto:
  - ✅ si los 5 individuales pasan
  - ⚠️ si pasan 4/5 (registrar el faltante)
  - ❌ si pasan < 4

Registrar cada comando ejecutado y su output literal en un buffer (markdown table) que la Task 2 leerá. NO modificar `index.md`.
  </action>
  <verify>
    <automated>grep -oE "figures/[a-zA-Z0-9_-]+\.png" index.md | sort -u | wc -l</automated>
    <!-- esperado: > 0 (idealmente 13 referencias únicas) -->
    <automated>for ref in $(grep -oE "figures/[a-zA-Z0-9_-]+\.png" index.md | sort -u); do test -f "$ref" || echo "MISSING: $ref"; done | wc -l</automated>
    <!-- esperado: 0 (cero referencias rotas) -->
    <automated>awk '/^---$/{c++} END{print c}' index.md</automated>
    <!-- esperado: 26 -->
    <automated>grep -c "La pregunta natural es" index.md</automated>
    <!-- esperado: 8 -->
    <automated>grep -c "La respuesta es" index.md</automated>
    <!-- esperado: 8 -->
    <automated>grep -cF "(§" index.md</automated>
    <!-- esperado: ≥ 5 -->
  </verify>
  <acceptance_criteria>
    - `for ref in $(grep -oE "figures/[a-zA-Z0-9_-]+\.png" index.md | sort -u); do test -f "$ref" || echo "MISSING: $ref"; done | wc -l` retorna exactamente 0.
    - `awk '/^---$/{c++} END{print c}' index.md` retorna exactamente 26.
    - `grep -c "La pregunta natural es" index.md` retorna exactamente 8.
    - `grep -c "La respuesta es" index.md` retorna exactamente 8.
    - `grep -cF "(§" index.md` retorna un número ≥ 5.
    - `grep -cF "(§2 y §4)" index.md` retorna ≥ 1.
    - `grep -cF "(§4.5, §4.6, §4.7)" index.md` retorna ≥ 1.
    - Verificación de pares `---` consecutivos retorna 0 ocurrencias.
    - Cada uno de los 4 checks tiene un veredicto explícito (✅/⚠️/❌) registrado para la Task 2.
  </acceptance_criteria>
  <done>
    Los 4 checks ejecutados con sus comandos y outputs registrados. Cada check tiene un veredicto explícito. `index.md` no fue modificado. La evidencia (tabla intermedia) está lista para que Task 2 la incluya en el reporte.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extraer texto verbatim de las 6 transiciones §4 + transición human_needed §4.6→§4.7 y generar el reporte de publicabilidad</name>
  <files>.planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md (nuevo)</files>
  <read_first>
    - index.md (extraer texto literal de las transiciones de §4; usar `grep -n "La pregunta natural es" index.md` para localizar las 8 líneas y leer el párrafo completo de cada una)
    - .planning/phases/04-revisi-n-final/04-CONTEXT.md (D-05 estructura del reporte; D-06 transición human_needed)
    - .planning/phases/03-mejora-de-narrativa/03-VERIFICATION.md (texto exacto actual de la transición §4.6→§4.7 que el profesor debe revisar)
    - Resultados de Task 1 (tabla de checks con veredictos)
  </read_first>
  <action>
Crear el archivo `.planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md` con la estructura siguiente (en español, formato MkDocs-Material compatible):

**1. Frontmatter / header del reporte:**
- Título: "Reporte de Publicabilidad — Sesión 03 OFDM"
- Fecha de generación: 2026-05-22 o 2026-05-23 (según ejecución real)
- Veredicto global: una línea resumen (ej. "Listo para publicar pendiente revisión humana de la transición §4.6→§4.7" o equivalente)

**2. Sección "Resumen de Checks Estructurales":**
Tabla markdown con columnas: Check | Comando | Resultado esperado | Resultado obtenido | Veredicto. Una fila por cada uno de los 4 checks de Task 1:
- Check 1: Referencias de figura → comando `grep -oE "figures/.*\.png" | sort -u` + cross-check con disco → veredicto ✅/⚠️/❌
- Check 2: Separadores `---` → comando `awk` + verificación de pares consecutivos → veredicto
- Check 3: 8 transiciones §4 patrón pregunta-respuesta → `grep -c "La pregunta natural es"` + `grep -c "La respuesta es"` → veredicto
- Check 4: 5 parenthéticals §7 → `grep -cF "(§"` + verificación individual de los 5 → veredicto

**3. Sección "Transiciones §4 en secuencia (verbatim)":**
Lista numerada de las 6 transiciones nuevas + el cierre §4.8→§5, presentadas en orden de lectura para que el profesor las lea de corrido y detecte saltos de tono. Por cada transición:
- Etiqueta: "§4.1 → §4.2", "§4.2 → §4.3", "§4.3 → §4.4", "§4.4 → §4.5", "§4.6 → §4.7", "§4.7 → §4.8", "§4.8 → §5" (cierre)
- Número de línea aproximado en index.md
- Texto verbatim del párrafo completo (extraído de index.md, sin recortar)

Procedimiento de extracción: ejecutar `grep -n "La pregunta natural es" index.md` para obtener las 8 líneas; para cada una, leer el rango `[línea_pregunta - 3, línea_pregunta + 8]` para capturar el párrafo completo (puede tener más de una oración antes de "La pregunta natural es"). Omitir explícitamente la transición §4.5→§4.6 existente (template canónico — no requiere revisión).

**4. Sección destacada "Transición §4.6 → §4.7 — REQUIERE REVISIÓN HUMANA (D-06)":**
- Encabezado de sección con un admonition `!!! warning "Revisión humana pendiente"` o `!!! note "Decisión editorial requerida"` para que sea visualmente prominente
- Contexto: explicar que esta transición fue marcada `human_needed` en la Fase 3 (referencia a 03-VERIFICATION.md item #5)
- Texto actual verbatim de la transición (extraído de la línea ~835 de index.md — el texto provisto en 04-CONTEXT.md `<interfaces>` debe coincidir):
  "tanto el ZF como el MMSE calculan H[k] a partir de `h_channel` — asumiendo que el canal es perfectamente conocido por el receptor. En la práctica nadie entrega ese vector al receptor. ¿Cómo se obtiene H[k] cuando el canal es desconocido? La respuesta es transmitir símbolos piloto conocidos en posiciones conocidas, y estimar el canal a partir de ellos."
- Pregunta para el profesor: "¿La articulación es pedagógicamente suficiente para el nivel de posgrado? Si requiere ajuste, indique la redacción preferida; se commitea en Plan 04-03."

**5. Sección "Plantilla canónica §4.5 (referencia)":**
Texto verbatim del template §4.5 para que el profesor compare el tono uniforme:
"La pregunta natural es: ¿existe un ecualizador que sea más inteligente en esas subportadoras? En lugar de invertir el canal ciegamente, ¿podría detectar que una subportadora está muy atenuada y moderar su respuesta para no amplificar el ruido? La respuesta es sí, y ese ecualizador es el MMSE."

**6. Sección "Próximos pasos":**
- Si todos los checks pasan en ✅ y el profesor aprueba la transición §4.6→§4.7 → ejecutar Plan 04-03 para cerrar tracking + `mkdocs build`
- Si algún check da ⚠️ o ❌ → identificar acción correctiva antes de Plan 04-03
- Si la transición §4.6→§4.7 requiere edición → registrar la edición acordada en el commit log de Plan 04-03

El archivo debe ser válido markdown (Mkdocs-Material no es requerido para el reporte, pero se sigue el mismo estilo para consistencia). NO se modifica `index.md` en esta tarea.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md && echo OK</automated>
    <!-- esperado: OK -->
    <automated>grep -c "Transición §4" .planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md</automated>
    <!-- esperado: ≥ 6 -->
    <automated>grep -cF "asumiendo que el canal es perfectamente conocido por el receptor" .planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md</automated>
    <!-- esperado: 1 (texto verbatim de la transición human_needed) -->
    <automated>grep -cE "(HUMAN_NEEDED|human_needed|REQUIERE REVISIÓN HUMANA|Decisión editorial)" .planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md</automated>
    <!-- esperado: ≥ 1 -->
    <automated>grep -cE "(✅|⚠️|❌)" .planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md</automated>
    <!-- esperado: ≥ 4 (un veredicto por check) -->
    <automated>grep -cF "más inteligente en esas subportadoras" .planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md</automated>
    <!-- esperado: 1 (template §4.5 incluido como referencia) -->
  </verify>
  <acceptance_criteria>
    - El archivo `.planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md` existe: `test -f .planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md` exit code 0.
    - El reporte contiene al menos 4 veredictos visuales: `grep -cE "(✅|⚠️|❌)" .planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md` retorna ≥ 4.
    - El reporte enumera al menos 6 transiciones §4 verbatim: `grep -c "Transición §4\|§4\.[0-9] → §4\.[0-9]\|§4\.[0-9] → §5" .planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md` retorna ≥ 6.
    - El texto exacto de la transición human_needed está incluido: `grep -cF "asumiendo que el canal es perfectamente conocido por el receptor" .planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md` retorna 1.
    - La transición §4.6→§4.7 está destacada como ítem que requiere revisión humana: `grep -cE "(HUMAN_NEEDED|REQUIERE REVISIÓN HUMANA|Decisión editorial requerida)" .planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md` retorna ≥ 1.
    - El template canónico §4.5 está incluido como referencia: `grep -cF "más inteligente en esas subportadoras" .planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md` retorna 1.
    - El reporte cita los comandos exactos ejecutados (no solo el resultado): `grep -cE "(grep -c|awk|grep -oE)" .planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md` retorna ≥ 3.
    - `index.md` NO fue modificado en esta tarea: `git diff --name-only index.md` no debe listar `index.md`.
  </acceptance_criteria>
  <done>
    El archivo `04-PUBLISHABILITY-REPORT.md` existe con: (a) tabla de los 4 checks estructurales con veredicto explícito, (b) las 6 transiciones §4 nuevas + cierre §4.8→§5 en secuencia con texto verbatim, (c) bloque destacado para la transición human_needed §4.6→§4.7 con la pregunta para el profesor, (d) template canónico §4.5 como referencia de tono, (e) sección de próximos pasos. `index.md` permanece sin cambios.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verificación automatizada → Decisión humana | El reporte es la interfaz entre los checks automáticos y el barrido humano del profesor; debe incluir comandos exactos para que el profesor pueda re-ejecutarlos si duda del resultado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | Veredicto del reporte | mitigate | Cada veredicto va acompañado del comando exacto + output literal; el profesor puede re-ejecutar y verificar sin confiar ciegamente en el agente |
| T-04-02 | Tampering | index.md durante verificación | mitigate | Esta tarea NO modifica index.md; verificar con `git diff --name-only index.md` al cierre que el archivo no aparece en el diff |
| T-04-03 | Information disclosure | Texto verbatim de transiciones | accept | Las transiciones son contenido público de la sesión; exposición intencional para barrido humano |
| T-04-04 | Repudiation | Decisión sobre §4.6→§4.7 | mitigate | El reporte cita explícitamente 03-VERIFICATION.md item #5 como origen del estado human_needed y deja registro de la pregunta planteada al profesor |
</threat_model>

<verification>
- `test -f .planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md` exit code 0.
- `grep -cE "(✅|⚠️|❌)" .planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md` retorna ≥ 4.
- `grep -cF "asumiendo que el canal es perfectamente conocido por el receptor" .planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md` retorna 1.
- `grep -cF "más inteligente en esas subportadoras" .planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md` retorna 1.
- `git diff --name-only index.md` no lista `index.md` (sin modificaciones).
- 4 checks estructurales ejecutados con veredicto explícito en el reporte.
- 6 transiciones §4 nuevas + cierre §4→§5 transcritas verbatim en secuencia.
- Transición §4.6→§4.7 destacada con bloque visual prominente.
</verification>

<success_criteria>
- El reporte de publicabilidad existe y es completo (4 checks + 6 transiciones + bloque human_needed + template referencia).
- Cualquier referencia de figura rota se marca como ❌ con la lista exacta de archivos faltantes.
- Cualquier regresión de WR-01 (`---` doble) se marca como ❌ con número de línea.
- El profesor puede tomar la decisión sobre §4.6→§4.7 leyendo solo este reporte.
- `index.md` no se modifica en este plan.
</success_criteria>

<output>
Crear `.planning/phases/04-revisi-n-final/04-01-SUMMARY.md` al terminar con:
- Veredicto de cada uno de los 4 checks (✅/⚠️/❌)
- Conteo verificado: 26 separadores `---`, 8 transiciones "La pregunta natural es"
- Lista de las 7 transiciones (6 nuevas + cierre) con sus líneas
- Confirmación de que `index.md` no fue modificado (`git diff --name-only index.md` vacío)
- Path del reporte generado: `.planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md`
</output>