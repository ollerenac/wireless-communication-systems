---
phase: 04-revisi-n-final
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lab.ipynb
autonomous: true
requirements:
  - LAB-01
tags:
  - notebook
  - lab
  - exercise-alignment
  - jupyter

must_haves:
  truths:
    - "El notebook lab.ipynb se ejecuta end-to-end sin errores (jupyter nbconvert --execute exit code 0)"
    - "El número de celdas no cambió: ni se agregaron ni se eliminaron celdas (D-02)"
    - "Ningún código Python fue modificado — solo texto en celdas markdown existentes (D-02)"
    - "El orden de los ejercicios en lab.ipynb corresponde al orden de las secciones de index.md a las que están alineados"
    - "Los enunciados de los ejercicios usan la misma terminología y nombres de variable que el bloque correspondiente de index.md (X, x_cp, y_noisy, Y, X_hat, bits_hat)"
  artifacts:
    - path: "lab.ipynb"
      provides: "Notebook ejecutable con enunciados de ejercicios alineados a index.md"
      contains: "Ejercicio"
  key_links:
    - from: "Ejercicios del lab.ipynb (celdas markdown)"
      to: "Secciones §4.x de index.md"
      via: "Terminología consistente (nombres de variable, fórmulas, vocabulario)"
      pattern: "(qpsk_map|ofdm_tx|apply_channel|ofdm_rx_no_channel|zf_equalizer|mmse_equalizer|ls_channel_estimate|qpsk_demap)"
---

<objective>
Cerrar el requisito LAB-01 cumpliendo el scope completo definido en D-01: (a) correr el notebook end-to-end sin errores, (b) revisar el orden de los ejercicios (celdas markdown que comienzan con `**Ejercicio**` o equivalente) contra el orden de las secciones de `index.md`, y (c) comparar los enunciados de cada ejercicio contra la terminología del bloque correspondiente en `index.md`, ajustando solo texto en celdas markdown existentes donde haya desalineación. Bajo D-02: no agregar ni eliminar celdas, no tocar código Python, no reordenar celdas.

Purpose: Que un estudiante pueda trabajar el lab siguiendo el orden de `index.md` sin encontrar saltos de orden ni discrepancias de vocabulario entre la narrativa y los enunciados de los ejercicios.

Output: `lab.ipynb` con (1) misma cantidad de celdas que antes, (2) ejecución limpia end-to-end, (3) cero cambios en código Python, (4) enunciados de ejercicios alineados textualmente con `index.md` donde se detectó desalineación.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-revisi-n-final/04-CONTEXT.md
@.planning/phases/03-mejora-de-narrativa/03-CONTEXT.md

<interfaces>
<!-- Restricciones D-02 (LÍMITE DE EDICIÓN — CRÍTICO) -->

D-02 — Solo se permite editar **texto dentro de celdas markdown que ya existen**. Específicamente:
- ✅ Permitido: corregir typos, alinear vocabulario, ajustar nombres de variable en el enunciado de un ejercicio para que coincidan con el bloque §4.x de index.md, reformular oraciones para que el alumno entienda el contexto.
- ❌ Prohibido:
  - Agregar nuevas celdas (markdown o código)
  - Eliminar celdas existentes
  - Reordenar celdas (la dependencia de ejecución se rompería)
  - Modificar el contenido de cualquier celda `cell_type: code` (el código Python es ground truth)
  - Cambiar el output de las celdas de código (se preservan tras re-ejecución)
  - Modificar metadata del notebook salvo lo que `jupyter nbconvert --execute --inplace` toque automáticamente

Mapeo de bloques §4 a funciones (verificado en 03-CONTEXT.md):
| Sección | Función Python | Variables canonical (D-08 de Fase 3) |
|---------|----------------|--------------------------------------|
| §4.1 Mapper | qpsk_map(bits) | bits, X |
| §4.2 IFFT + CP | ofdm_tx(X, N_CP) | X, x_cp |
| §4.3 Canal | apply_channel(x, h) | x_cp, h_channel, y_noisy |
| §4.4 RX sin canal | ofdm_rx_no_channel(y, N, N_CP) | y_noisy, Y |
| §4.5 ZF | zf_equalizer(Y, h, N) | Y, h_channel, X_hat |
| §4.6 MMSE | mmse_equalizer(Y, h, N, SNR_dB) | Y, h_channel, X_hat, SNR_dB |
| §4.7 LS estimación | ls_channel_estimate(Y, pilot_idx, X_pilot, N) | Y, pilot_idx, X_pilot, H_est |
| §4.8 Demapper | qpsk_demap(X_hat) | X_hat, bits_hat |

Estado inicial verificado (Fase 3 wave 4):
- 17 celdas de código (`"cell_type": "code"`) — número que NO debe cambiar
- 0 errores en ejecución end-to-end (confirmado en wave 4 de Fase 3 con nbconvert)

Comando de ejecución end-to-end estándar:
`jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.timeout=300 lab.ipynb`

Path del notebook (relativo al worktree del proyecto): `lab.ipynb` en la raíz de `docs/sessions/03-ofdm-systems/`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventariar el notebook — listar celdas y localizar ejercicios (LAB-01.a antes de ejecutar)</name>
  <files>(solo lectura — no se modifica lab.ipynb)</files>
  <read_first>
    - lab.ipynb (leer todas las celdas; identificar celdas markdown con `**Ejercicio`, `### Ejercicio`, `## Ejercicio` o equivalente; registrar el índice de celda y el primer encabezado de cada una)
    - .planning/phases/04-revisi-n-final/04-CONTEXT.md (D-01 scope, D-02 restricciones)
    - .planning/phases/03-mejora-de-narrativa/03-CONTEXT.md (D-08 nombres de variable canonical)
  </read_first>
  <action>
Inventariar la estructura del notebook ANTES de ejecutarlo:

1. **Conteo de celdas por tipo:**
   - Total de celdas (markdown + code)
   - Conteo de celdas de código (debe ser 17 según estado de Fase 3)
   - Conteo de celdas markdown
   - Registrar ambos números en el resumen para Task 4

2. **Localización de ejercicios:**
   - Buscar todas las celdas markdown cuyo contenido contenga `Ejercicio` (case-sensitive) — formatos esperados: `**Ejercicio N**:`, `### Ejercicio N`, `## Ejercicio: ...`, etc.
   - Para cada ejercicio detectado, registrar:
     - Índice de celda (0-based o 1-based — declarar la convención usada)
     - Título o enunciado breve (primera línea del contenido)
     - Sección de `index.md` a la que aparenta corresponder (inferida por keywords: "QPSK mapper" → §4.1; "IFFT" / "CP" → §4.2; "canal" / "convolución" → §4.3; "FFT" / "eliminar CP" → §4.4; "ZF" / "ecualizador zero-forcing" → §4.5; "MMSE" → §4.6; "pilotos" / "estimación" / "LS" → §4.7; "demapper" / "decisión" → §4.8)

3. **Detección de desalineaciones potenciales:**
   - Si el orden inferido §4.x de los ejercicios NO es monotónico ascendente (ej. un ejercicio asociado a §4.5 aparece antes que uno asociado a §4.3), registrarlo como ⚠️ posible desalineación de orden.
   - Si un ejercicio menciona una función o variable que NO está en la tabla D-08 del bloque correspondiente, registrarlo como ⚠️ posible desalineación de terminología.

4. NO ejecutar el notebook todavía. NO modificar el archivo. Producir solo el inventario en memoria de trabajo o archivo temporal.
  </action>
  <verify>
    <automated>jupyter nbconvert --to script lab.ipynb --stdout 2>/dev/null | grep -c "^# In\[" </automated>
    <!-- esperado: 17 (conteo de celdas code; el flag --to script convierte cada celda code a un bloque "# In[N]:") -->
    <automated>python -c "import json; nb=json.load(open('lab.ipynb')); print(sum(1 for c in nb['cells'] if c['cell_type']=='code'))"</automated>
    <!-- esperado: 17 -->
    <automated>python -c "import json; nb=json.load(open('lab.ipynb')); print(sum(1 for c in nb['cells'] if c['cell_type']=='markdown' and 'Ejercicio' in ''.join(c.get('source', []))))"</automated>
    <!-- esperado: ≥ 1 (al menos un ejercicio debe existir; registrar el número exacto) -->
  </verify>
  <acceptance_criteria>
    - El inventario está completo: conteo de celdas code, conteo de celdas markdown, lista numerada de ejercicios con índice + título + sección §4.x inferida.
    - El conteo de celdas code es exactamente 17: `python -c "import json; nb=json.load(open('lab.ipynb')); print(sum(1 for c in nb['cells'] if c['cell_type']=='code'))"` retorna 17.
    - Las desalineaciones potenciales (orden o terminología) están registradas explícitamente con índice de celda + descripción.
    - `lab.ipynb` no fue modificado en esta tarea: `git diff --name-only lab.ipynb` no lista `lab.ipynb`.
  </acceptance_criteria>
  <done>
    Inventario producido con: (a) conteos por tipo de celda confirmando 17 code cells, (b) lista numerada de ejercicios con su índice + sección §4.x inferida, (c) lista de desalineaciones detectadas (puede estar vacía si todo está alineado). El notebook no fue ejecutado ni modificado.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ejecutar el notebook end-to-end con nbconvert y verificar exit code 0 (LAB-01.a)</name>
  <files>lab.ipynb (se actualizará in-place con outputs frescos; código sin cambios)</files>
  <read_first>
    - lab.ipynb (revisar metadata `kernelspec` para confirmar el kernel disponible — típicamente `python3`)
    - .planning/phases/04-revisi-n-final/04-CONTEXT.md (D-01.a scope, D-02 restricciones)
  </read_first>
  <action>
Ejecutar el notebook end-to-end usando `jupyter nbconvert` y verificar que termine sin errores.

Comando estándar:
`jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.timeout=300 lab.ipynb`

Si el comando falla:
1. Capturar el error (celda + traceback)
2. STOP — NO intentar corregir código Python (D-02). Reportar el error al usuario y terminar la fase con status `human_needed`.
3. Si el error es un problema de kernel/entorno (no del código): cambiar `--ExecutePreprocessor.timeout` a 600 y reintentar una sola vez.

Si el comando tiene éxito:
1. Verificar exit code == 0
2. Verificar que el conteo de celdas no cambió: `python -c "import json; nb=json.load(open('lab.ipynb')); print(sum(1 for c in nb['cells'] if c['cell_type']=='code'))"` debe seguir retornando 17.
3. Verificar que `git diff --stat lab.ipynb` muestra cambios SOLO en bloques de `outputs` y `execution_count` (no en `source` de celdas code) — usar `git diff lab.ipynb | grep -E "^\+ *\"source\"|^\- *\"source\"" | wc -l` debería retornar 0 cambios de source en celdas code (los únicos cambios de source legítimos vendrán de la Task 3 sobre celdas markdown).
4. NO commitear todavía — eso sucede al cierre del plan, después de Task 3.

Nota: `--inplace` re-escribe `lab.ipynb` con los outputs frescos. Esto es esperado y deseado para confirmar reproducibilidad.
  </action>
  <verify>
    <automated>jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.timeout=300 lab.ipynb && echo EXIT_OK</automated>
    <!-- esperado: EXIT_OK al final del output -->
    <automated>python -c "import json; nb=json.load(open('lab.ipynb')); print(sum(1 for c in nb['cells'] if c['cell_type']=='code'))"</automated>
    <!-- esperado: 17 -->
    <automated>python -c "import json; nb=json.load(open('lab.ipynb')); errors=[i for i,c in enumerate(nb['cells']) if c['cell_type']=='code' and any(o.get('output_type')=='error' for o in c.get('outputs', []))]; print('ERRORS:', errors)"</automated>
    <!-- esperado: ERRORS: [] -->
  </verify>
  <acceptance_criteria>
    - El comando `jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.timeout=300 lab.ipynb` termina con exit code 0.
    - Cero celdas de código terminan con un output de tipo `error`: el script de verificación retorna `ERRORS: []`.
    - El conteo de celdas code permanece en 17: `python -c "import json; nb=json.load(open('lab.ipynb')); print(sum(1 for c in nb['cells'] if c['cell_type']=='code'))"` retorna 17.
    - El conteo total de celdas no cambió respecto a Task 1.
    - Las firmas/cuerpos de las celdas de código no cambiaron: `git diff lab.ipynb` no muestra modificaciones dentro de bloques `"cell_type": "code"` en el campo `"source"`.
  </acceptance_criteria>
  <done>
    Notebook ejecutado in-place con éxito (exit 0). Cero outputs de error. Conteo de 17 code cells preservado. Los únicos cambios en `lab.ipynb` son outputs y `execution_count` (esperado por re-ejecución).
  </done>
</task>

<task type="auto">
  <name>Task 3: Comparar enunciados de ejercicios contra index.md y ajustar texto markdown donde haya desalineación (LAB-01.b + LAB-01.c, bajo D-02)</name>
  <files>lab.ipynb (solo celdas markdown existentes — D-02)</files>
  <read_first>
    - lab.ipynb (texto de cada celda markdown identificada como ejercicio en Task 1)
    - index.md (bloques §4.1 a §4.8; usar `grep -n "^#### 4\." index.md` para localizar cada subsección; leer el rango completo de cada bloque para entender la terminología del enunciado pedagógico)
    - .planning/phases/04-revisi-n-final/04-CONTEXT.md (D-01.b orden, D-01.c terminología, D-02 restricciones críticas)
    - .planning/phases/03-mejora-de-narrativa/03-CONTEXT.md (tabla D-08 de variables canonical)
  </read_first>
  <action>
Para cada ejercicio inventariado en Task 1, ejecutar la comparación contra el bloque correspondiente de `index.md` y ajustar SOLO si hay desalineación detectable.

**Procedimiento por ejercicio:**

1. **Identificar la sección §4.x del bloque correspondiente** (usar la inferencia de Task 1 + verificar leyendo la celda).
2. **Leer el bloque §4.x completo en index.md** (de `#### 4.X ...` hasta el próximo `---`).
3. **Comparar terminología:** ¿Los nombres de variable en el enunciado coinciden con D-08 (X, x_cp, y_noisy, Y, X_hat, bits_hat, H_est)?
4. **Comparar conceptos clave:** ¿El enunciado usa los mismos términos que la narrativa? Ejemplos de discrepancias típicas:
   - Enunciado dice "símbolos transmitidos" pero index.md dice "símbolos QAM en frecuencia X"
   - Enunciado dice "vector de canal" pero index.md dice "respuesta impulsiva h_channel"
   - Enunciado dice "ecualizar" pero index.md distingue "ecualizador ZF" vs "ecualizador MMSE"

**Criterios de decisión (qué editar vs qué dejar):**

- ✅ EDITAR si: el ejercicio usa un nombre de variable distinto del canonical (ej. `Z` en lugar de `Y`) o vocabulario distinto del bloque correspondiente.
- ✅ EDITAR si: el ejercicio referencia una sección/fórmula que ya no existe o que cambió de nombre tras Fases 2–3.
- ❌ NO EDITAR si: el ejercicio es pedagógicamente más explícito que la narrativa (su propósito es ese).
- ❌ NO EDITAR si: la edición requiere agregar ejemplos numéricos o expandir el enunciado (eso es alcance v2, fuera de scope).
- ❌ NO EDITAR si: la desalineación es solo de orden — la celda no se mueve (D-02 prohíbe reordenar). Registrar en el summary como "desalineación de orden no corregible bajo D-02" y dejar al profesor.

**Procedimiento de edición:**

- Solo se edita el campo `source` de celdas con `cell_type: markdown` que YA contienen el ejercicio. Usar Edit/Write directos sobre el JSON del notebook, preservando estructura JSON.
- Cada edición debe ser mínima: cambiar nombre de variable, ajustar 1–3 palabras de vocabulario. NO reescribir enunciados enteros.
- Tras cada edición, verificar que el archivo JSON sigue siendo válido: `python -c "import json; json.load(open('lab.ipynb'))" && echo VALID_JSON`.
- Tras todas las ediciones, NO re-ejecutar el notebook (los outputs ya son frescos de Task 2 y las celdas markdown no afectan ejecución).

**Si no se detecta ninguna desalineación corregible bajo D-02:** registrar explícitamente en el summary "Cero ediciones necesarias — los enunciados de ejercicios ya están alineados con index.md" y dejar el archivo sin cambios.
  </action>
  <verify>
    <automated>python -c "import json; json.load(open('lab.ipynb'))" && echo VALID_JSON</automated>
    <!-- esperado: VALID_JSON -->
    <automated>python -c "import json; nb=json.load(open('lab.ipynb')); print(sum(1 for c in nb['cells'] if c['cell_type']=='code'))"</automated>
    <!-- esperado: 17 (sin cambios) -->
    <automated>python -c "import json; nb=json.load(open('lab.ipynb')); print(len(nb['cells']))"</automated>
    <!-- esperado: igual al conteo total registrado en Task 1 (sin add/remove de celdas) -->
    <automated>git diff lab.ipynb | grep -E "^\+ *\"cell_type\": \"code\"" | wc -l</automated>
    <!-- esperado: 0 (ninguna celda code agregada) -->
    <automated>git diff lab.ipynb | grep -E "^\- *\"cell_type\": \"code\"" | wc -l</automated>
    <!-- esperado: 0 (ninguna celda code eliminada) -->
  </verify>
  <acceptance_criteria>
    - `lab.ipynb` sigue siendo JSON válido: `python -c "import json; json.load(open('lab.ipynb'))"` exit code 0.
    - Cero celdas de código agregadas o eliminadas: `git diff lab.ipynb | grep -cE "^[+-] *\"cell_type\": \"code\""` retorna 0.
    - El conteo de 17 code cells se mantiene: `python -c "import json; nb=json.load(open('lab.ipynb')); print(sum(1 for c in nb['cells'] if c['cell_type']=='code'))"` retorna 17.
    - El conteo total de celdas no cambió respecto a Task 1 (registrado en el inventario).
    - Si se hicieron ediciones, todas ocurren en campos `source` de celdas `cell_type: markdown` que ya existían: `git diff lab.ipynb | grep -E "^[+-]" | grep -v "source\|outputs\|execution_count\|metadata" | wc -l` retorna 0 (toda línea agregada/eliminada está dentro de `source`, `outputs`, `execution_count` o `metadata`).
    - Si se hicieron ediciones, ninguna ocurre dentro de un bloque `cell_type: code` en `source`: verificable por inspección del diff o por el script `python -c "..."` que compare source de cada code cell pre/post.
    - El summary registra explícitamente qué se editó (con índice de celda + descripción del cambio) o "Cero ediciones necesarias".
  </acceptance_criteria>
  <done>
    Cada ejercicio fue comparado contra su bloque §4.x. Las desalineaciones corregibles bajo D-02 fueron arregladas con ediciones mínimas en celdas markdown existentes. El archivo JSON sigue siendo válido. Cero celdas agregadas/eliminadas. Cero código Python tocado. Cualquier desalineación de orden no corregible queda registrada para el profesor.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verificación final post-ediciones — re-ejecución de confirmación y commit</name>
  <files>lab.ipynb</files>
  <read_first>
    - lab.ipynb (estado final)
    - .planning/phases/04-revisi-n-final/04-CONTEXT.md (D-01 scope completo, D-02 restricciones, D-07 exit criterion — commits)
  </read_first>
  <action>
Confirmación final tras las ediciones de Task 3:

1. **Re-ejecutar el notebook una segunda vez** para confirmar que las ediciones de markdown no rompieron nada (debe ser idempotente):
   - `jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.timeout=300 lab.ipynb`
   - Verificar exit code 0
   - Verificar `ERRORS: []` con el mismo script de Task 2

2. **Verificar invariantes finales:**
   - Conteo de code cells == 17
   - Conteo total de celdas == valor registrado en Task 1
   - JSON válido

3. **Commit:**
   - `git add lab.ipynb`
   - Mensaje de commit (D-07 — commit con mensaje descriptivo):
     - Si hubo ediciones de markdown: `lab(04): align exercise statements with index.md terminology (LAB-01)`
     - Si solo hubo re-ejecución: `lab(04): re-execute notebook end-to-end for LAB-01 verification`
   - `git commit -m "<mensaje>"`

4. NO push — push es responsabilidad del profesor (deferido fuera de Fase 4 según 04-CONTEXT.md `<deferred>`).
  </action>
  <verify>
    <automated>jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.timeout=300 lab.ipynb && echo EXIT_OK</automated>
    <!-- esperado: EXIT_OK -->
    <automated>python -c "import json; nb=json.load(open('lab.ipynb')); errors=[i for i,c in enumerate(nb['cells']) if c['cell_type']=='code' and any(o.get('output_type')=='error' for o in c.get('outputs', []))]; print('ERRORS:', errors)"</automated>
    <!-- esperado: ERRORS: [] -->
    <automated>python -c "import json; nb=json.load(open('lab.ipynb')); print(sum(1 for c in nb['cells'] if c['cell_type']=='code'))"</automated>
    <!-- esperado: 17 -->
    <automated>git log --oneline -1 -- lab.ipynb</automated>
    <!-- esperado: 1 commit reciente con mensaje conteniendo "LAB-01" o "lab(04)" -->
    <automated>git status --porcelain lab.ipynb</automated>
    <!-- esperado: vacío (commit aplicado, working tree limpio) -->
  </verify>
  <acceptance_criteria>
    - Re-ejecución idempotente exitosa: exit code 0.
    - Cero outputs de error tras la re-ejecución.
    - Conteo de 17 code cells preservado.
    - JSON válido.
    - `git log --oneline -1 -- lab.ipynb` muestra el commit reciente de esta tarea.
    - `git status --porcelain lab.ipynb` retorna vacío (working tree limpio respecto a lab.ipynb).
    - El mensaje del commit referencia LAB-01 o lab(04): `git log -1 --pretty=%B -- lab.ipynb | grep -E "LAB-01|lab\(04\)"` retorna ≥ 1 línea.
  </acceptance_criteria>
  <done>
    Notebook re-ejecutado con éxito tras las ediciones de markdown. Invariantes verificadas (17 code cells, JSON válido, cero errores). Cambios commiteados con mensaje descriptivo referenciando LAB-01. Working tree limpio.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Texto markdown ↔ Código Python (D-02) | El notebook contiene celdas de ambos tipos en JSON; un error de edición puede tocar código accidentalmente. D-02 prohíbe esto y se verifica con diff. |
| Notebook como ground truth | Las funciones definidas en code cells son la fuente de verdad pedagógica; modificarlas (incluso accidentalmente) invalida toda la sesión. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | Celdas code de lab.ipynb | mitigate | Verificación obligatoria en Task 3: `git diff lab.ipynb | grep -E "^[+-]" | grep -v "source\|outputs\|execution_count\|metadata"` retorna 0; verificación adicional `grep -cE "^[+-] *\"cell_type\": \"code\""` retorna 0 (ninguna celda code agregada/eliminada). |
| T-04-06 | Tampering | Estructura JSON | mitigate | `python -c "import json; json.load(open('lab.ipynb'))"` tras cada edición; abortar si el JSON deja de ser válido. |
| T-04-07 | Denial of service | Ejecución del notebook | mitigate | `--ExecutePreprocessor.timeout=300` (5 min); si el notebook excede el timeout, reintentar con 600s; si persiste, escalar a human_needed (sin tocar código). |
| T-04-08 | Repudiation | Decisión sobre desalineaciones no corregibles | mitigate | Registrar en el summary cada desalineación de orden que no se corrigió bajo D-02, con justificación. |
</threat_model>

<verification>
- `jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.timeout=300 lab.ipynb` retorna exit code 0 (ejecutado dos veces: Task 2 y Task 4).
- Cero celdas terminan con output de tipo `error`.
- 17 code cells preservadas en todo momento.
- JSON válido tras todas las ediciones.
- Cero líneas modificadas dentro de `source` de celdas `cell_type: code`.
- Commit aplicado con mensaje referenciando LAB-01.
- `git status --porcelain lab.ipynb` retorna vacío al cierre.
</verification>

<success_criteria>
- LAB-01 completo bajo el scope D-01 (a + b + c) y la restricción D-02.
- Notebook ejecuta end-to-end sin errores.
- Enunciados de ejercicios alineados con la terminología de index.md (donde fue corregible bajo D-02).
- Cambios commiteados.
- Cero modificaciones a código Python.
- Cero celdas agregadas o eliminadas.
</success_criteria>

<output>
Crear `.planning/phases/04-revisi-n-final/04-02-SUMMARY.md` al terminar con:
- Inventario inicial: número total de celdas, número de code cells (debe ser 17), número de ejercicios detectados con su índice + sección §4.x
- Resultado de la primera ejecución (Task 2): exit code, conteo de errores
- Lista de desalineaciones detectadas vs corregidas (formato: "celda N: enunciado mencionaba X, se cambió a Y") — o "Cero ediciones necesarias"
- Lista de desalineaciones de orden NO corregibles bajo D-02 (registradas para el profesor)
- Resultado de la segunda ejecución (Task 4): exit code, conteo de errores
- Hash del commit aplicado a lab.ipynb
- Confirmación final de invariantes (17 code cells, JSON válido, cero código modificado)
</output>