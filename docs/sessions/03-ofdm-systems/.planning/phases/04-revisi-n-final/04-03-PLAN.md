---
phase: 04-revisi-n-final
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - revisión transversal (cierre tracking) + exit criterion D-07
tags:
  - tracking
  - requirements
  - project-evolution
  - mkdocs-build

must_haves:
  truths:
    - "NARR-01 está marcado como Validated en REQUIREMENTS.md con referencia a Fase 3"
    - "NARR-02 está marcado como Validated en REQUIREMENTS.md con referencia a Fase 3"
    - "NARR-03 está marcado como Validated en REQUIREMENTS.md con referencia a Fase 3"
    - "LAB-01 está marcado como Validated en REQUIREMENTS.md con referencia a Fase 4"
    - "PROJECT.md Requirements section refleja las 7 entradas Validated (5 anteriores + NARR-01/02/03 + LAB-01) — sin Active pendientes salvo decisiones diferidas explícitas"
    - "PROJECT.md Last updated muestra 2026-05-22 o 2026-05-23"
    - "PROJECT.md Key Decisions table refleja las decisiones de cierre de las 4 fases"
    - "`mkdocs build` corre sin errores y sin warnings desde el directorio que contiene mkdocs.yml"
    - "Todos los cambios de Fase 4 están commiteados (working tree limpio para los archivos de Fase 4)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Tabla de Traceability + Requirements actualizada con NARR-01/02/03 + LAB-01 en estado Complete/Validated"
      contains: "Validated"
    - path: ".planning/PROJECT.md"
      provides: "Documento evolucionado al cierre de Fase 4 (Last updated, Requirements, Key Decisions)"
      contains: "Last updated: 2026-05-2"
  key_links:
    - from: "REQUIREMENTS.md Active section"
      to: "REQUIREMENTS.md Validated section"
      via: "Movimiento de NARR-01, NARR-02, NARR-03, LAB-01 con referencia de fase"
      pattern: "Validated en Fase"
    - from: "PROJECT.md Last updated"
      to: "Fase 4 cerrada"
      via: "Actualización de fecha + sincronización con STATE.md"
      pattern: "Last updated: 2026-05-2"
---

<objective>
Cerrar el tracking del milestone v1.0 ejecutando los pasos finales de Fase 4 (D-08 + D-09 + D-07):
1. Actualizar `.planning/REQUIREMENTS.md`: mover NARR-01, NARR-02, NARR-03 de Active a Validated (con referencia a Fase 3) y mover LAB-01 a Validated (con referencia a Fase 4).
2. Evolucionar `.planning/PROJECT.md`: actualizar Requirements section (cero items Active pendientes), Key Decisions table (resultados de las 4 fases), Last updated date.
3. Ejecutar `mkdocs build` desde el directorio raíz del repo (donde está `mkdocs.yml`) verificando exit code 0 sin warnings ni errors.
4. Commitear los cambios de tracking con mensajes descriptivos (D-07).

Purpose: Dejar el repositorio en estado "publicable" — toda la trazabilidad cerrada, el sitio MkDocs renderiza limpio, y los planes futuros pueden continuar sabiendo que el milestone v1.0 está formalmente cerrado.

Output: REQUIREMENTS.md + PROJECT.md actualizados y commiteados; `mkdocs build` exit code 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-revisi-n-final/04-CONTEXT.md
@.planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md
@.planning/phases/03-mejora-de-narrativa/03-VERIFICATION.md

<interfaces>
<!-- Estado inicial de REQUIREMENTS.md (líneas relevantes — verificar con grep antes de editar) -->

REQUIREMENTS.md sección Narrativa (estado actual — todos marcados [ ] Pending):
- NARR-01: Fortalecer el hilo conductor de la Sección 4 (línea 13)
- NARR-02: Revisar y mejorar la Introducción (línea 14)
- NARR-03: Integrar mejor la síntesis de §7 (línea 15)

REQUIREMENTS.md sección Laboratorio (estado actual — Pending):
- LAB-01: Revisar lab.ipynb (línea 19)

REQUIREMENTS.md Traceability table (estado actual):
| LAB-01 | Phase 1 (diagnóstico) → Phase 2 (corrección) | Pending |
| NARR-01 | Phase 3 | Pending |
| NARR-02 | Phase 3 | Pending |
| NARR-03 | Phase 3 | Pending |

PROJECT.md estado actual (líneas relevantes):
- Línea 23 (aprox): listado "Active" incluye NARR-01, NARR-02, NARR-03, WR-01, Figura 3, 4 PNGs — la mayoría ya validados, esto debe limpiarse
- Línea 75: `*Last updated: 2026-05-22`
- Tabla Key Decisions: 2 decisiones iniciales (línea ~53–55) — debe expandirse con las decisiones de las Fases 1–4

mkdocs.yml location:
- `/home/researcher/Teaching/uni/2026/wireless-communication-systems/mkdocs.yml` (raíz del repo, NO en docs/sessions/03-ofdm-systems/)
- El build se ejecuta desde la raíz del repo, no desde el directorio de la sesión

Fecha de hoy (de currentDate en CLAUDE.md): 2026-05-22 (si la ejecución se extiende: 2026-05-23 si el agente pasa la medianoche)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Actualizar .planning/REQUIREMENTS.md — mover NARR-01/02/03 + LAB-01 a Validated (D-08)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (estado actual completo — leer todo el archivo, ~44 líneas)
    - .planning/phases/04-revisi-n-final/04-CONTEXT.md (D-08 instrucción exacta)
    - .planning/phases/03-mejora-de-narrativa/03-VERIFICATION.md (referencia de Fase 3 que valida NARR-01/02/03)
  </read_first>
  <action>
Editar `.planning/REQUIREMENTS.md` con los siguientes cambios mínimos y precisos:

**Cambio 1 — Sección "Narrativa" (líneas ~13–15):** marcar cada checkbox `[ ]` como `[x]` para NARR-01, NARR-02, NARR-03. La línea NARR-01 actual `- [ ] **NARR-01**: Fortalecer el hilo conductor...` se convierte en `- [x] **NARR-01**: Fortalecer el hilo conductor...`. Idem para NARR-02 y NARR-03.

**Cambio 2 — Sección "Laboratorio" (línea ~19):** marcar el checkbox `[ ]` como `[x]` para LAB-01. La línea actual `- [ ] **LAB-01**: Revisar lab.ipynb...` se convierte en `- [x] **LAB-01**: Revisar lab.ipynb...`.

**Cambio 3 — Tabla Traceability (líneas ~37–43):** cambiar el estado de las 4 filas afectadas:
- Fila LAB-01: cambiar "Pending" → "Complete" y actualizar la columna Phase a `Phase 1 (diagnóstico) → Phase 2 (corrección) → Phase 4 (cierre)` (preservar formato de la columna).
- Fila NARR-01: cambiar "Pending" → "Complete" (la columna Phase ya dice "Phase 3" — no se modifica).
- Fila NARR-02: cambiar "Pending" → "Complete".
- Fila NARR-03: cambiar "Pending" → "Complete".

Mantener intactas las filas CORR-01, CORR-02, CORR-03 (ya están en "Complete").

Estructura de la tabla y orden de las filas: no cambiar. Solo los valores de las celdas de estado.

Ningún checkbox preexistente que ya esté en `[x]` se vuelve a tocar. NO agregar nuevas líneas. NO eliminar líneas.
  </action>
  <verify>
    <automated>grep -E "^- \[x\] \*\*NARR-01\*\*" .planning/REQUIREMENTS.md | wc -l</automated>
    <!-- esperado: 1 -->
    <automated>grep -E "^- \[x\] \*\*NARR-02\*\*" .planning/REQUIREMENTS.md | wc -l</automated>
    <!-- esperado: 1 -->
    <automated>grep -E "^- \[x\] \*\*NARR-03\*\*" .planning/REQUIREMENTS.md | wc -l</automated>
    <!-- esperado: 1 -->
    <automated>grep -E "^- \[x\] \*\*LAB-01\*\*" .planning/REQUIREMENTS.md | wc -l</automated>
    <!-- esperado: 1 -->
    <automated>grep -E "^\| (NARR-0[1-3]|LAB-01) " .planning/REQUIREMENTS.md | grep -c "Complete"</automated>
    <!-- esperado: 4 (las 4 filas de traceability ahora dicen Complete) -->
    <automated>grep -E "^- \[ \]" .planning/REQUIREMENTS.md | wc -l</automated>
    <!-- esperado: 0 (cero requisitos v1 pendientes) -->
  </verify>
  <acceptance_criteria>
    - `grep -cE "^- \[x\] \*\*NARR-01\*\*" .planning/REQUIREMENTS.md` retorna 1.
    - `grep -cE "^- \[x\] \*\*NARR-02\*\*" .planning/REQUIREMENTS.md` retorna 1.
    - `grep -cE "^- \[x\] \*\*NARR-03\*\*" .planning/REQUIREMENTS.md` retorna 1.
    - `grep -cE "^- \[x\] \*\*LAB-01\*\*" .planning/REQUIREMENTS.md` retorna 1.
    - La tabla de Traceability muestra "Complete" para LAB-01: `grep -E "^\| LAB-01 " .planning/REQUIREMENTS.md | grep -c "Complete"` retorna 1.
    - La tabla de Traceability muestra "Complete" para NARR-01, NARR-02, NARR-03: `grep -E "^\| NARR-0[1-3] " .planning/REQUIREMENTS.md | grep -c "Complete"` retorna 3.
    - Cero requisitos v1 pendientes: `grep -cE "^- \[ \]" .planning/REQUIREMENTS.md` retorna 0.
    - Estructura preservada (mismo número de líneas, mismo orden): `wc -l .planning/REQUIREMENTS.md` retorna el mismo número que antes del cambio (~44 líneas).
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md actualizado: los 4 checkboxes (NARR-01, NARR-02, NARR-03, LAB-01) marcados `[x]`, las 4 filas de Traceability muestran "Complete". Estructura preservada.
  </done>
</task>

<task type="auto">
  <name>Task 2: Evolucionar .planning/PROJECT.md al cierre de Fase 4 (D-09)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (estado actual completo — 76 líneas)
    - .planning/phases/04-revisi-n-final/04-CONTEXT.md (D-09 instrucción + estilo de las transiciones anteriores)
    - .planning/ROADMAP.md (success criteria de las 4 fases, para sintetizar en Key Decisions)
    - .planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md (resultados de los checks estructurales de Plan 04-01)
    - .planning/phases/03-mejora-de-narrativa/03-CONTEXT.md (decisiones D-01 a D-16 de Fase 3 que dan contexto para Key Decisions)
  </read_first>
  <action>
Evolucionar `.planning/PROJECT.md` siguiendo el bloque "Evolution" del propio archivo (líneas 56–67). Cambios específicos:

**Cambio 1 — Sección "## Requirements > ### Validated":** agregar 4 nuevos bullets al final de la lista de Validated, en este orden:
- `- [x] Fortalecer el hilo conductor de §4 con transiciones pregunta-respuesta y bloque de cierre cadena completa — Validado en Fase 3 (NARR-01)`
- `- [x] Cerrar la brecha narrativa Sesión 02 → OFDM en la Introducción — Validado en Fase 3 (NARR-02)`
- `- [x] Integrar §7 Síntesis con referencias cruzadas a las secciones donde se desarrolló cada dimensión — Validado en Fase 3 (NARR-03)`
- `- [x] Revisar lab.ipynb: ejecución end-to-end + alineación de enunciados con index.md — Validado en Fase 4 (LAB-01)`

Mantener los 5 bullets de Validated preexistentes intactos.

**Cambio 2 — Sección "## Requirements > ### Active":** vaciar (todos los items se movieron a Validated, Out of Scope o se confirmaron como completos). El bloque debe quedar con un único bullet placeholder o vacío. Sugerencia de estilo:
`- *(ningún item Active restante — todos los requisitos v1 están completos o explícitamente diferidos)*`

**Cambio 3 — Tabla "## Key Decisions":** actualizar las 2 filas existentes (ambas en "Pending") y agregar 2 filas nuevas que sintetizan las decisiones críticas de las 4 fases:
- Fila 1 (Notebook como ground truth): cambiar "Pending" → "Ejecutado en Fase 2: conflictos resueltos ajustando index.md (snippets MMSE, LS, fórmula N/(N+N_CP))"
- Fila 2 (Preservar estructura de secciones): cambiar "Pending" → "Confirmado: estructura §1-§7 intacta, solo contenido y narrativa modificados"
- Agregar fila nueva: `| Transiciones §4 pregunta-respuesta | El template canónico §4.5 (ya existente) define el estilo para las 7 nuevas transiciones | Ejecutado en Fase 3: 8 transiciones presentes, 1 human_needed validada en Fase 4 |`
- Agregar fila nueva: `| lab.ipynb solo markdown editables | El código Python es ground truth; los enunciados de ejercicios son ajustables | Ejecutado en Fase 4: LAB-01 verificado end-to-end, cero cambios de código |`

**Cambio 4 — Línea "Last updated":** actualizar la fecha de la última línea del archivo de `2026-05-22` a la fecha de hoy. Si el agente ejecuta antes de medianoche: `2026-05-22`; si pasa de medianoche: `2026-05-23`.

Mantener intacto todo el resto del documento (## What This Is, ## Core Value, ## Context, ## Constraints, ## Evolution).
  </action>
  <verify>
    <automated>grep -c "\- \[x\]" .planning/PROJECT.md</automated>
    <!-- esperado: ≥ 9 (5 preexistentes + 4 nuevas = 9 bullets Validated) -->
    <automated>grep -c "Validado en Fase 3 (NARR-01)" .planning/PROJECT.md</automated>
    <!-- esperado: 1 -->
    <automated>grep -c "Validado en Fase 4 (LAB-01)" .planning/PROJECT.md</automated>
    <!-- esperado: 1 -->
    <automated>grep -E "ningún item Active|Active restante" .planning/PROJECT.md | wc -l</automated>
    <!-- esperado: ≥ 1 (sección Active vaciada con placeholder) -->
    <automated>grep -c "Last updated: 2026-05-2" .planning/PROJECT.md</automated>
    <!-- esperado: 1 (fecha actualizada) -->
    <automated>grep -c "Transiciones §4 pregunta-respuesta" .planning/PROJECT.md</automated>
    <!-- esperado: 1 (nueva fila Key Decisions) -->
  </verify>
  <acceptance_criteria>
    - `grep -cE "Validado en Fase 3 \(NARR-01\)" .planning/PROJECT.md` retorna 1.
    - `grep -cE "Validado en Fase 3 \(NARR-02\)" .planning/PROJECT.md` retorna 1.
    - `grep -cE "Validado en Fase 3 \(NARR-03\)" .planning/PROJECT.md` retorna 1.
    - `grep -cE "Validado en Fase 4 \(LAB-01\)" .planning/PROJECT.md` retorna 1.
    - `grep -c "Last updated: 2026-05-2" .planning/PROJECT.md` retorna 1.
    - `grep -c "Transiciones §4 pregunta-respuesta" .planning/PROJECT.md` retorna 1.
    - `grep -c "lab.ipynb solo markdown editables" .planning/PROJECT.md` retorna 1.
    - La sección Active no contiene ítems en `[ ]`: `grep -cE "^- \[ \]" .planning/PROJECT.md` retorna 0.
    - Las 2 filas originales de Key Decisions ya no dicen "Pending": `grep -c "Pending" .planning/PROJECT.md` retorna 0.
  </acceptance_criteria>
  <done>
    PROJECT.md evolucionado: 4 nuevos bullets Validated (NARR-01/02/03, LAB-01), sección Active vaciada, tabla Key Decisions con 4 filas y sin "Pending", Last updated actualizado.
  </done>
</task>

<task type="auto">
  <name>Task 3: Ejecutar mkdocs build + commitear todos los cambios de Fase 4 (D-07)</name>
  <files>index.md, lab.ipynb, .planning/REQUIREMENTS.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/phases/04-revisi-n-final/04-CONTEXT.md (D-07 exit criterion — mkdocs build desde directorio con mkdocs.yml)
    - .planning/phases/04-revisi-n-final/04-PUBLISHABILITY-REPORT.md (resultado de Plan 04-01 — si hay ⚠️/❌ que requieran acción antes del build)
  </read_first>
  <action>
Ejecutar la secuencia de cierre en orden:

**Paso 1 — Localizar mkdocs.yml y ejecutar build:**
El archivo `mkdocs.yml` está en la raíz del repositorio: `/home/researcher/Teaching/uni/2026/wireless-communication-systems/`.
Ejecutar: `cd /home/researcher/Teaching/uni/2026/wireless-communication-systems && mkdocs build --strict 2>&1`
Si `mkdocs build --strict` falla por algún warning convertido en error, ejecutar `mkdocs build` (sin `--strict`) y reportar los warnings. Si el build falla por error real (página no encontrada, sintaxis inválida), diagnosticar en `index.md` y corregir antes de continuar.

**Paso 2 — Verificar working tree post-build:**
`git status --porcelain` — el build puede generar un directorio `site/` que ya debería estar en `.gitignore`; si no está, NO commitear el directorio `site/`.
`git diff --name-only` — listar archivos modificados en Fase 4 (esperados: `index.md` si hubo ajuste de D-06, `lab.ipynb` si hubo ajuste de enunciados en Plan 04-02, `.planning/REQUIREMENTS.md`, `.planning/PROJECT.md`, `.planning/phases/04-revisi-n-final/*.md`).

**Paso 3 — Commitear cambios en grupos semánticos:**
Usar `git add` específico (nunca `git add .`) para cada commit:
- Commit A (si `lab.ipynb` fue modificado en Plan 04-02): `git commit -m "docs(lab): alinear enunciados de ejercicios con terminología de index.md (LAB-01)"`
- Commit B (tracking tracking): `git add .planning/REQUIREMENTS.md .planning/PROJECT.md && git commit -m "docs(tracking): cerrar milestone v1.0 — NARR-01/02/03 y LAB-01 Validated (Fase 4)"`
- Commit C (planes y reporte de Fase 4): `git add .planning/phases/04-revisi-n-final/ && git commit -m "docs(04): plans y reporte de publicabilidad — Fase 4 Revisión Final"`

Si `lab.ipynb` no fue modificado (todos los enunciados ya estaban alineados), omitir Commit A.

**Paso 4 — Confirmar working tree limpio para los archivos de Fase 4:**
`git status --porcelain .planning/ index.md lab.ipynb` debe retornar vacío (0 líneas) para los archivos de la fase.
  </action>
  <verify>
    <automated>cd /home/researcher/Teaching/uni/2026/wireless-communication-systems && mkdocs build 2>&1 | tail -5</automated>
    <!-- esperado: "INFO - Documentation built in N.N seconds" sin líneas ERROR -->
    <automated>git status --porcelain .planning/REQUIREMENTS.md .planning/PROJECT.md</automated>
    <!-- esperado: vacío (committeados) -->
    <automated>git log --oneline -5</automated>
    <!-- esperado: al menos 1 commit nuevo de Fase 4 en el top -->
  </verify>
  <acceptance_criteria>
    - `mkdocs build` (o `mkdocs build --strict`) termina con exit code 0 desde el directorio raíz del repositorio.
    - La salida del build no contiene líneas que empiecen con `ERROR`.
    - `git status --porcelain .planning/REQUIREMENTS.md .planning/PROJECT.md` retorna vacío (archivos commiteados).
    - `git status --porcelain .planning/phases/04-revisi-n-final/` retorna vacío (planes y reporte commiteados).
    - `git log --oneline -3` muestra al menos 1 commit nuevo con mensaje que menciona "Fase 4" o "tracking" o "LAB-01".
    - Si `lab.ipynb` fue modificado: `git status --porcelain lab.ipynb` retorna vacío (commiteado).
    - `git status --porcelain` retorna cero líneas para los archivos de Fase 4 (working tree limpio).
  </acceptance_criteria>
  <done>
    mkdocs build exit code 0, todos los cambios de Fase 4 commiteados en grupos semánticos, working tree limpio. El milestone v1.0 está formalmente cerrado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown → MkDocs build | `index.md` renderizado por MkDocs-Material; cualquier markup malformado introducido en fases previas podría manifestarse aquí |
| Notebook → nbconvert | `lab.ipynb` ejecutado por nbconvert; celdas de código con dependencias implícitas de estado pueden fallar si el orden de ejecución cambia |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `lab.ipynb` celdas de código | mitigate | D-02: ninguna celda de código se toca; verificar con `jupyter nbconvert --to script lab.ipynb \| grep -c "# In\["` que el número de celdas de código no cambió |
| T-04-02 | Denial of service | `mkdocs build` | mitigate | Ejecutar build antes de commitear — si falla, diagnosticar en `index.md` antes de marcar Fase 4 como completa |
| T-04-03 | Tampering | `.planning/REQUIREMENTS.md` | mitigate | Cambios mínimos y precisos (solo checkboxes y celdas de tabla); `wc -l` antes y después debe coincidir |
| T-04-04 | Information disclosure | `04-PUBLISHABILITY-REPORT.md` | accept | El reporte es un documento de trabajo interno del profesor — no se publica en el sitio MkDocs |
</threat_model>

<verification>
- `mkdocs build` (o `mkdocs build --strict`) desde la raíz del repo retorna exit code **0** sin líneas ERROR.
- `grep -cE "^- \[x\] \*\*(NARR-01|NARR-02|NARR-03|LAB-01)\*\*" .planning/REQUIREMENTS.md` retorna **4**.
- `grep -cE "^- \[ \]" .planning/REQUIREMENTS.md` retorna **0** (cero requisitos v1 pendientes).
- `grep -c "Last updated: 2026-05-2" .planning/PROJECT.md` retorna **1** (fecha actualizada).
- `git status --porcelain .planning/ index.md lab.ipynb` retorna **vacío** (working tree limpio para Fase 4).
- `git log --oneline -5` muestra al menos 1 commit de cierre de Fase 4.
</verification>

<success_criteria>
- REQUIREMENTS.md: los 4 requisitos v1 pendientes (NARR-01, NARR-02, NARR-03, LAB-01) marcados como Complete/Validated — cero requisitos v1 en estado Pending o Active.
- PROJECT.md: evolucionado al cierre de Fase 4 — Active section vacía, 4 nuevos bullets Validated, Key Decisions con 4 filas y sin "Pending", Last updated actualizado.
- `mkdocs build` exit code 0 sin errores desde el directorio raíz del repositorio.
- Todos los cambios de Fase 4 commiteados con mensajes descriptivos — working tree limpio.
</success_criteria>

<output>
Crear `.planning/phases/04-revisi-n-final/04-03-SUMMARY.md` al terminar con:
- Resultado del mkdocs build (exit code, warnings si los hubo)
- Lista de commits creados (hash + mensaje)
- Estado final de REQUIREMENTS.md (tabla de traceability)
- Estado final de PROJECT.md (secciones Validated, Active vacío, Last updated)
- Nota sobre si lab.ipynb fue modificado o no en Plan 04-02
</output>