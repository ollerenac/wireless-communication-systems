---
phase: 02-correcci-n-de-contenido
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - figures/ofdm-ber-equalizers.png
  - figures/ofdm-per-subcarrier-ber.png
  - figures/mmse-vs-zf-constellation.png
  - figures/channel-estimation-pilots.png
  - figures/qpsk-decision-regions.png
  - figures/ofdm-time-domain.png
  - figures/cp-effect-constellation.png
autonomous: true
requirements:
  - CORR-02
  - LAB-01
tags:
  - figures
  - git
  - assets

must_haves:
  truths:
    - "Las 6 figuras nuevas generadas por lab.ipynb están versionadas en git"
    - "Las dos referencias rotas a `figures/ofdm-ber-equalizers.png` (líneas 814, 953 de index.md) resuelven a archivo existente y tracked"
    - "La referencia rota a `figures/ofdm-per-subcarrier-ber.png` (línea 961 de index.md) resuelve a archivo existente y tracked"
    - "`figures/ofdm-ber.png` permanece sin renombrar ni eliminar"
    - "`lab.ipynb` no fue modificado en este plan"
  artifacts:
    - path: "figures/ofdm-ber-equalizers.png"
      provides: "BER ZF vs MMSE comparison (refs §4.7 line 814, §4.8 line 953)"
    - path: "figures/ofdm-per-subcarrier-ber.png"
      provides: "BER por subportadora (ref §Ejercicios línea 961)"
    - path: "figures/mmse-vs-zf-constellation.png"
      provides: "Comparación de constelaciones ZF vs MMSE (will be referenced in §4.8 by Plan 03)"
    - path: "figures/channel-estimation-pilots.png"
      provides: "Figura de ejercicio (no referenciada — D-04)"
    - path: "figures/qpsk-decision-regions.png"
      provides: "Figura de ejercicio (no referenciada — D-04)"
    - path: "figures/ofdm-time-domain.png"
      provides: "Figura de ejercicio (no referenciada — D-04)"
    - path: "figures/cp-effect-constellation.png"
      provides: "Figura de ejercicio (no referenciada — D-04)"
  key_links:
    - from: "index.md line 814"
      to: "figures/ofdm-ber-equalizers.png"
      via: "markdown image reference"
      pattern: "ofdm-ber-equalizers\\.png"
    - from: "index.md line 961"
      to: "figures/ofdm-per-subcarrier-ber.png"
      via: "markdown image reference"
      pattern: "ofdm-per-subcarrier-ber\\.png"
---

<objective>
Versionar en git las 6 figuras PNG generadas durante la auditoría (Fase 1) para resolver las 3 referencias rotas en `index.md` (BLOCKER-S.03/S.04/S.05) y dejar disponible en disco la figura huérfana `mmse-vs-zf-constellation.png` que el Plan 03 referenciará en §4.8 (D-03). También se versionan 4 figuras de ejercicios (D-04) que no se referencian en index.md pero deben quedar comprometidas para que el repo sea auto-contenido.

Purpose: Resolver CORR-02 (referencias rotas) sin tocar texto narrativo; verificar LAB-01 ejecutando el notebook end-to-end sin modificarlo.
Output: 6 PNGs nuevos tracked en git + verificación de que `lab.ipynb` corre limpio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-correcci-n-de-contenido/02-CONTEXT.md
@.planning/phases/02-correcci-n-de-contenido/02-PATTERNS.md
@.planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FINDINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar estado de disco de las 6 figuras y agregar a git</name>
  <files>
    figures/ofdm-ber-equalizers.png,
    figures/ofdm-per-subcarrier-ber.png,
    figures/mmse-vs-zf-constellation.png,
    figures/channel-estimation-pilots.png,
    figures/qpsk-decision-regions.png,
    figures/ofdm-time-domain.png,
    figures/cp-effect-constellation.png
  </files>
  <read_first>
    .planning/phases/02-correcci-n-de-contenido/02-CONTEXT.md,
    .planning/phases/02-correcci-n-de-contenido/02-PATTERNS.md,
    .planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FINDINGS.md
  </read_first>
  <action>
Per D-01 y D-04, agregar al índice de git los siguientes 7 PNGs (las 6 declaradas en D-01 más la huérfana `mmse-vs-zf-constellation.png` que ya estaba en disco pero figura como modificada según `git status`):

Comandos exactos:

  git add figures/ofdm-ber-equalizers.png
  git add figures/ofdm-per-subcarrier-ber.png
  git add figures/mmse-vs-zf-constellation.png
  git add figures/channel-estimation-pilots.png
  git add figures/qpsk-decision-regions.png
  git add figures/ofdm-time-domain.png
  git add figures/cp-effect-constellation.png

Regla D-02 (no negociable): NO ejecutar `git rm figures/ofdm-ber.png` ni `git mv figures/ofdm-ber.png ...`. Ese archivo es pre-existente y debe permanecer intacto.

Notas para el executor:
- `figures/mmse-vs-zf-constellation.png` y `figures/ofdm-ber-equalizers.png` aparecen como modificadas (no untracked) en `git status` — eso es esperado, son las que se regeneraron durante la auditoría de Fase 1. `git add` las stagea de todos modos.
- Las otras 5 (`ofdm-per-subcarrier-ber.png`, `channel-estimation-pilots.png`, `qpsk-decision-regions.png`, `ofdm-time-domain.png`, `cp-effect-constellation.png`) aparecen como untracked — `git add` las añade al índice por primera vez.
- Verificar con `ls -la figures/` que los 7 archivos existen físicamente antes de `git add`. Si alguno falta, abortar y reportar — la auditoría confirmó que están en disco (ver `01-AUDIT-FINDINGS.md` §2 nota sobre estado de disco).
- NO modificar `figures/cp-illustration.png`, `figures/ofdm-subcarriers.png`, `figures/zf-equalizer-effect.png`, `figures/zf-equalizer-qam-comparison.png` (también aparecen como modificadas en `git status` pero NO están en el alcance de este plan; cualquier corrección suya queda diferida a una fase posterior si fuera necesario).
- NO modificar `index.md` en esta tarea — la inserción de la referencia a `mmse-vs-zf-constellation.png` en §4.8 (D-03) es responsabilidad del Plan 03.
- NO tocar `lab.ipynb`. La Task 2 lo verifica solo en modo lectura/ejecución.

Después del `git add`, hacer un único commit con mensaje exacto:

  docs(02): commit generated figures from lab.ipynb audit

  Resuelve BLOCKER-S.03/S.04/S.05 (referencias rotas) versionando las
  figuras generadas por lab.ipynb durante la auditoría de Fase 1.
  Incluye la huérfana mmse-vs-zf-constellation.png (será referenciada
  por Plan 02-03 en §4.8 per D-03) y 4 figuras de ejercicios (D-04).
  No se renombra ni elimina figures/ofdm-ber.png (D-02).

  Requirement: CORR-02
  </action>
  <verify>
    <automated>
test -f figures/ofdm-ber-equalizers.png &amp;&amp; \
test -f figures/ofdm-per-subcarrier-ber.png &amp;&amp; \
test -f figures/mmse-vs-zf-constellation.png &amp;&amp; \
test -f figures/channel-estimation-pilots.png &amp;&amp; \
test -f figures/qpsk-decision-regions.png &amp;&amp; \
test -f figures/ofdm-time-domain.png &amp;&amp; \
test -f figures/cp-effect-constellation.png &amp;&amp; \
test -f figures/ofdm-ber.png &amp;&amp; \
git ls-files --error-unmatch figures/ofdm-ber-equalizers.png figures/ofdm-per-subcarrier-ber.png figures/mmse-vs-zf-constellation.png figures/channel-estimation-pilots.png figures/qpsk-decision-regions.png figures/ofdm-time-domain.png figures/cp-effect-constellation.png figures/ofdm-ber.png &amp;&amp; \
echo "OK: 7 nuevas + ofdm-ber.png intacto, todas tracked"
    </automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files figures/ofdm-ber-equalizers.png` retorna la ruta (file tracked).
    - `git ls-files figures/ofdm-per-subcarrier-ber.png` retorna la ruta.
    - `git ls-files figures/mmse-vs-zf-constellation.png` retorna la ruta.
    - `git ls-files figures/channel-estimation-pilots.png` retorna la ruta.
    - `git ls-files figures/qpsk-decision-regions.png` retorna la ruta.
    - `git ls-files figures/ofdm-time-domain.png` retorna la ruta.
    - `git ls-files figures/cp-effect-constellation.png` retorna la ruta.
    - `git ls-files figures/ofdm-ber.png` retorna la ruta (NO eliminado, NO renombrado — D-02).
    - El commit creado por este plan toca exclusivamente archivos bajo `figures/` — `git show --stat HEAD` no lista `lab.ipynb` ni `index.md` ni nada fuera de `figures/`.
    - El mensaje del commit contiene el token `CORR-02` (literal).
  </acceptance_criteria>
  <done>
    7 figuras tracked en git (incluida la huérfana D-03 y las 4 de D-04); `figures/ofdm-ber.png` intacto; commit creado con referencia a CORR-02.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verificar LAB-01 — ejecutar lab.ipynb end-to-end sin modificarlo</name>
  <files>
    lab.ipynb
  </files>
  <read_first>
    .planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FINDINGS.md,
    .planning/phases/02-correcci-n-de-contenido/02-CONTEXT.md
  </read_first>
  <action>
Verificar que `lab.ipynb` corre limpio end-to-end (requisito LAB-01) **sin modificar el notebook**. El notebook es ground truth (CONTEXT.md §domain): no se edita.

Comando exacto:

  jupyter nbconvert --to notebook --execute lab.ipynb --output /tmp/lab.executed.ipynb --ExecutePreprocessor.timeout=300

Reglas:
- El archivo de salida va a `/tmp/` para que NUNCA toque el árbol de trabajo. No usar `--output lab.executed.ipynb` (eso lo crearía en el cwd).
- Después de la ejecución, verificar con `git diff -- lab.ipynb` que el notebook fuente NO fue modificado. Si la salida no es vacía, abortar y reportar (algo lo tocó indebidamente).
- Si `jupyter` no está disponible en PATH, intentar `python -m jupyter nbconvert ...`. Si tampoco funciona, registrar en SUMMARY "LAB-01 no verificable en este entorno — Fase 1 ya confirmó ejecución limpia" y continuar (no es bloqueante: la auditoría de Fase 1 lo verificó el 2026-05-22).
- Tras la verificación, eliminar `/tmp/lab.executed.ipynb` con `rm -f /tmp/lab.executed.ipynb`.

No se hace commit en esta tarea — es solo verificación.
  </action>
  <verify>
    <automated>
(jupyter nbconvert --to notebook --execute lab.ipynb --output /tmp/lab.executed.ipynb --ExecutePreprocessor.timeout=300 2>&amp;1 | tail -5; \
  test -z "$(git diff -- lab.ipynb)" &amp;&amp; echo "OK: lab.ipynb unchanged"; \
  rm -f /tmp/lab.executed.ipynb) || echo "LAB-01 verification deferred — Phase 1 audit already confirmed clean run"
    </automated>
  </verify>
  <acceptance_criteria>
    - `jupyter nbconvert --to notebook --execute lab.ipynb` returns exit code 0 (notebook runs clean), OR la herramienta no está disponible y se registra explícitamente la deferencia con referencia a Fase 1.
    - `git diff -- lab.ipynb` devuelve vacío después de la ejecución (notebook fuente no modificado).
    - `/tmp/lab.executed.ipynb` no existe al finalizar la tarea.
    - El árbol de trabajo no tiene `lab.executed.ipynb` ni `lab.nbconvert.ipynb` ni similares.
  </acceptance_criteria>
  <done>
    LAB-01 verificado: `lab.ipynb` corre end-to-end sin error de celda y permanece byte-idéntico al estado pre-ejecución.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Working tree → git index | Archivos binarios PNG cruzan al índice via `git add` |
| Notebook → kernel | `jupyter nbconvert --execute` ejecuta código Python del notebook |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `figures/ofdm-ber.png` (archivo pre-existente) | mitigate | D-02 explícito: prohibido `git rm` o `git mv` sobre ese archivo; acceptance criterion verifica `git ls-files figures/ofdm-ber.png` |
| T-02-02 | Tampering | `lab.ipynb` (ground truth) | mitigate | Task 2 escribe salida a `/tmp/`; verifica `git diff -- lab.ipynb` vacío post-ejecución |
| T-02-03 | Information Disclosure | Figuras PNG binarias | accept | Las figuras son material pedagógico público; sin PII, sin secretos |
| T-02-04 | Denial of Service | `jupyter nbconvert --execute` con timeout | mitigate | `--ExecutePreprocessor.timeout=300` limita ejecución a 5 min/celda; Fase 1 confirmó que el notebook corre en <3 min |
</threat_model>

<verification>
- `git ls-files figures/` debe listar al menos los 7 archivos clave (los 6 de D-01 más `mmse-vs-zf-constellation.png` huérfana de D-03) tras este plan.
- `figures/ofdm-ber.png` debe permanecer tracked y sin modificación de contenido (D-02).
- `lab.ipynb` debe permanecer byte-idéntico tras la verificación de LAB-01 (Task 2).
- `git log --oneline -1` debe mostrar el commit creado por Task 1 con mensaje referenciando CORR-02.
- `git status` no debe listar `figures/ofdm-ber-equalizers.png`, `figures/ofdm-per-subcarrier-ber.png`, `figures/mmse-vs-zf-constellation.png`, `figures/channel-estimation-pilots.png`, `figures/qpsk-decision-regions.png`, `figures/ofdm-time-domain.png`, `figures/cp-effect-constellation.png` después del commit.
</verification>

<success_criteria>
- [ ] Los 7 PNGs declarados son tracked en git (verificable con `git ls-files`).
- [ ] `figures/ofdm-ber.png` permanece tracked e intacto.
- [ ] Commit único creado, su `git show --stat` solo lista archivos bajo `figures/`.
- [ ] Mensaje del commit contiene `CORR-02`.
- [ ] `lab.ipynb` permanece byte-idéntico al estado pre-plan (verificable con `git diff lab.ipynb` vacío).
- [ ] LAB-01 verificado o explícitamente deferido a auditoría previa.
- [ ] CORR-02 (parcial: figuras en disco) + LAB-01 cumplidos.
</success_criteria>

<output>
Create `.planning/phases/02-correcci-n-de-contenido/02-01-SUMMARY.md` when done.
</output>
