---
phase: 02-correcci-n-de-contenido
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - index.md
autonomous: true
requirements:
  - CORR-01
tags:
  - formulas
  - latex
  - mkdocs

must_haves:
  truths:
    - "El factor de normalización del receptor en la demostración de ortogonalidad (§2 línea ~240) es `1/√N` — consistente con IFFT `norm='ortho'`"
    - "La nota desplegable de §2 (línea ~249) usa `1/√N` en ambos miembros — consistente internamente con BLOCKER-S.01"
    - "La fórmula de `η_neta` en §6 (línea ~1029) usa `N/(N+N_CP)` etiquetado `eficiencia temporal`"
    - "Un estudiante que aplique la fórmula corregida de §6 con N=2048, N_CP=144 obtiene el mismo factor 0.934 que el cálculo numérico de la línea 1037"
  artifacts:
    - path: "index.md"
      provides: "Documento pedagógico con 3 fórmulas LaTeX corregidas en §2 y §6"
      contains: "frac{1}{\\\\sqrt{N}}\\\\sum_{n=0}^{N-1} x[n]"
  key_links:
    - from: "index.md §2 línea 240"
      to: "definición de IFFT de la línea 229 con factor 1/√N"
      via: "consistencia matemática de normalización"
      pattern: "\\\\frac\\{1\\}\\{\\\\sqrt\\{N\\}\\}.*x\\[n\\]"
    - from: "index.md §6 línea 1029 (fórmula simbólica)"
      to: "index.md §6 línea 1037 (cálculo numérico)"
      via: "ambos deben usar el mismo factor de eficiencia temporal N/(N+N_CP) = 1 - N_CP/(N+N_CP)"
      pattern: "\\\\text\\{eficiencia temporal\\}"
---

<objective>
Corregir las 3 fórmulas LaTeX defectuosas catalogadas como BLOCKER-S.01, MINOR-01 y BLOCKER-S.02 en `01-AUDIT-FINDINGS.md`. Las correcciones son ediciones aisladas de display math (`$$...$$`) en líneas no contiguas (§2 línea 240, §2 línea 249, §6 línea 1029) que no modifican el número de líneas del archivo — por eso este plan puede correr en paralelo con el Plan 02-01 (sin solapamiento de archivos) y antes del Plan 02-03 (que sí cambia el número de líneas).

Purpose: Resolver CORR-01 parcial — fórmulas matemáticas verificables. Sin estas correcciones, un estudiante que siga la derivación obtiene `X[k]/√N` en lugar de `X[k]` (BLOCKER-S.01) o `η ≈ 0.22 bit/s/Hz` en lugar de `≈ 3.5 bit/s/Hz` (BLOCKER-S.02).
Output: 3 ediciones puntuales en `index.md`, un único commit referenciando CORR-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-correcci-n-de-contenido/02-CONTEXT.md
@.planning/phases/02-correcci-n-de-contenido/02-PATTERNS.md
@.planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FINDINGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corregir BLOCKER-S.01 y MINOR-01 en §2 (líneas 240 y 249) — factor 1/N → 1/√N</name>
  <files>index.md</files>
  <read_first>
    index.md (rango líneas 229–260, ya leído por el planner),
    .planning/phases/02-correcci-n-de-contenido/02-PATTERNS.md (Pattern 3 y "Edit Sequence"),
    .planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FINDINGS.md (sección 1, BLOCKER-S.01 y MINOR-01)
  </read_first>
  <action>
Aplicar DOS ediciones en `index.md` — ambas reemplazan un factor `1/N` por `1/√N` en el miembro izquierdo de una identidad LaTeX. No se modifica nada más del archivo en esta tarea.

**Edición 1 (BLOCKER-S.01, D-06) — línea 240 del archivo actual:**

Buscar exactamente esta línea (texto literal, sin cambios de espaciado):

  $$\frac{1}{N}\sum_{n=0}^{N-1} x[n]\, e^{-j2\pi kn/N} = \frac{1}{\sqrt{N}}\sum_{l=0}^{N-1} X[l] \underbrace{\left(\frac{1}{N}\sum_{n=0}^{N-1} e^{j2\pi (l-k)n/N}\right)}_{\text{término de interferencia de }l\text{ sobre }k}$$

Reemplazar por (cambio: el primer `\frac{1}{N}` se vuelve `\frac{1}{\sqrt{N}}`; el `\frac{1}{N}` dentro del `\underbrace` NO se toca — ese sí es 1/N porque pertenece al promedio de cancelación):

  $$\frac{1}{\sqrt{N}}\sum_{n=0}^{N-1} x[n]\, e^{-j2\pi kn/N} = \frac{1}{\sqrt{N}}\sum_{l=0}^{N-1} X[l] \underbrace{\left(\frac{1}{N}\sum_{n=0}^{N-1} e^{j2\pi (l-k)n/N}\right)}_{\text{término de interferencia de }l\text{ sobre }k}$$

**Edición 2 (MINOR-01, D-08) — línea 249 del archivo actual (dentro del bloque `??? note` que abre en línea 242):**

Buscar exactamente esta línea (texto literal, 4 espacios de indentación heredados del admonition):

      $$\frac{1}{N}\sum_{n=0}^{N-1} x[n]\, e^{-j2\pi kn/N} = \frac{1}{N}\sum_{n=0}^{N-1} \left(\frac{1}{\sqrt{N}} \sum_{l=0}^{N-1} X[l]\, e^{j2\pi ln/N}\right) e^{-j2\pi kn/N}$$

Reemplazar por (cambio: ambos `\frac{1}{N}` exteriores se vuelven `\frac{1}{\sqrt{N}}`; el `\frac{1}{\sqrt{N}}` interior NO se toca, ya está correcto):

      $$\frac{1}{\sqrt{N}}\sum_{n=0}^{N-1} x[n]\, e^{-j2\pi kn/N} = \frac{1}{\sqrt{N}}\sum_{n=0}^{N-1} \left(\frac{1}{\sqrt{N}} \sum_{l=0}^{N-1} X[l]\, e^{j2\pi ln/N}\right) e^{-j2\pi kn/N}$$

**Importante: preservar la indentación de 4 espacios** del admonition en la edición 2 — eso es lo que MkDocs-Material requiere para que la fórmula se renderice dentro del `??? note`.

**Restricciones:**
- NO modificar la línea 253 (`= \frac{1}{\sqrt{N}} \sum_{l=0}^{N-1} X[l] \left(\frac{1}{N}\sum_{n=0}^{N-1} e^{j2\pi ln/N}\, e^{-j2\pi kn/N}\right)$$`) — esa ya es correcta porque es el paso siguiente del desarrollo después de sacar el `1/√N` y `X[l]` de la suma sobre `n`. El `1/N` que queda dentro pertenece al promedio de cancelación de ortogonalidad y debe permanecer.
- NO modificar la línea 257 ni la línea 263 — esas también son correctas (el `1/N` dentro del paréntesis es el promedio de cancelación, no el factor de la FFT/IFFT).
- NO modificar la línea 229 (definición original de la IFFT — ya usa `\frac{1}{\sqrt{N}}` y está bien).
- NO modificar la línea 245 (definición de `x[n]` dentro del admonition — ya usa `\frac{1}{\sqrt{N}}` y está bien).

Usar la herramienta `Edit` con `old_string` y `new_string` exactos (no construir reemplazos con sed). Cada edición debe ser única en el archivo (el texto buscado solo aparece una vez en cada caso — Pattern 3 de PATTERNS.md confirma esto).
  </action>
  <verify>
    <automated>
grep -c "^\$\$\\\\frac{1}{\\\\sqrt{N}}\\\\sum_{n=0}^{N-1} x\[n\]\\\\, e\^{-j2\\\\pi kn/N} = \\\\frac{1}{\\\\sqrt{N}}\\\\sum_{l=0}^{N-1} X\[l\] \\\\underbrace" index.md | grep -qx 1 &amp;&amp; \
grep -c "    \$\$\\\\frac{1}{\\\\sqrt{N}}\\\\sum_{n=0}^{N-1} x\[n\]\\\\, e\^{-j2\\\\pi kn/N} = \\\\frac{1}{\\\\sqrt{N}}\\\\sum_{n=0}^{N-1} \\\\left(\\\\frac{1}{\\\\sqrt{N}}" index.md | grep -qx 1 &amp;&amp; \
! grep -n "^\$\$\\\\frac{1}{N}\\\\sum_{n=0}^{N-1} x\[n\]" index.md &amp;&amp; \
! grep -n "    \$\$\\\\frac{1}{N}\\\\sum_{n=0}^{N-1} x\[n\]" index.md &amp;&amp; \
echo "OK: BLOCKER-S.01 + MINOR-01 corregidos, sin residuos 1/N en miembro izq"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep` confirma que aparece exactamente una vez la cadena `$$\frac{1}{\sqrt{N}}\sum_{n=0}^{N-1} x[n]\, e^{-j2\pi kn/N} = \frac{1}{\sqrt{N}}\sum_{l=0}^{N-1} X[l] \underbrace` al inicio de línea (sin indentación) — corresponde a la edición 1 (línea 240).
    - `grep` confirma que aparece exactamente una vez la cadena `    $$\frac{1}{\sqrt{N}}\sum_{n=0}^{N-1} x[n]\, e^{-j2\pi kn/N} = \frac{1}{\sqrt{N}}\sum_{n=0}^{N-1} \left(\frac{1}{\sqrt{N}}` con 4 espacios de indentación — corresponde a la edición 2 (línea 249).
    - `grep -n "^\$\$\\\\frac{1}{N}\\\\sum_{n=0}^{N-1} x\[n\]" index.md` no retorna ninguna línea (no quedan residuos del `1/N` original al inicio del miembro izquierdo en §2).
    - `grep -n "    \$\$\\\\frac{1}{N}\\\\sum_{n=0}^{N-1} x\[n\]" index.md` no retorna ninguna línea (no quedan residuos en el admonition).
    - El número total de líneas de `index.md` permanece igual al inicial (las ediciones reemplazan línea por línea, no insertan ni eliminan).
    - Las líneas 229 (definición IFFT), 245 (definición x[n] en nota), 253, 257, 263 NO han sido modificadas: `git diff index.md` solo debe mostrar dos hunks, ambos en el rango 240–249.
  </acceptance_criteria>
  <done>
    BLOCKER-S.01 y MINOR-01 corregidos: el factor de normalización del receptor en §2 es `1/√N` tanto en el cuerpo (línea 240) como en la nota desplegable (línea 249). El número total de líneas del archivo no cambió.
  </done>
</task>

<task type="auto">
  <name>Task 2: Corregir BLOCKER-S.02 en §6 (línea 1029) — invertir fracción CP y relabelar</name>
  <files>index.md</files>
  <read_first>
    index.md (rango líneas 1020–1040, ya leído por el planner),
    .planning/phases/02-correcci-n-de-contenido/02-PATTERNS.md (Pattern 3 y "Edit Sequence" fila 3),
    .planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FINDINGS.md (sección 1, BLOCKER-S.02)
  </read_first>
  <action>
Aplicar UNA edición en `index.md` línea 1029, según D-07. Esta edición invierte la fracción del primer término de la fórmula simbólica y cambia su etiqueta — sin cambiar el número de líneas del archivo.

Buscar exactamente esta línea (texto literal):

  $$\eta_{\text{neta}} = \underbrace{\frac{N_{CP}}{N + N_{CP}}}_{\text{overhead CP}} \times \underbrace{\frac{N - N_{\text{guard}} - N_{\text{pilot}}}{N}}_{\text{overhead frecuencial}} \times \log_2 M \times r_c$$

Reemplazar por (cambios: numerador y denominador de la primera fracción se invierten — `N_{CP}` → `N`; etiqueta `\text{overhead CP}` → `\text{eficiencia temporal}`):

  $$\eta_{\text{neta}} = \underbrace{\frac{N}{N + N_{CP}}}_{\text{eficiencia temporal}} \times \underbrace{\frac{N - N_{\text{guard}} - N_{\text{pilot}}}{N}}_{\text{overhead frecuencial}} \times \log_2 M \times r_c$$

**Restricciones:**
- NO modificar la línea 1033 (`$$\text{Overhead CP} = \frac{144}{2048 + 144} = \frac{144}{2192} \approx 6{,}6\%$$`). Esa fórmula calcula explícitamente el **overhead CP** como `N_CP/(N+N_CP) ≈ 0.066` — es coherente con la fórmula simbólica corregida (la eficiencia temporal es el complemento: `1 − overhead CP = N/(N+N_CP) ≈ 0.934`). El texto "Overhead CP" aquí se refiere al cálculo intermedio del overhead, no a un factor de la fórmula de `η_neta`.
- NO modificar la línea 1037 (`$$\eta_{\text{neta}} = (1 - 0{,}066) \times (1 - 0{,}07) \times 6 \times \frac{2}{3} \approx 3{,}5\ \text{bit/s/Hz}$$`). Esta línea ya usa `(1 − 0.066) = 0.934 = N/(N+N_CP)` correctamente — confirma la consistencia interna que la corrección persigue.
- NO modificar el texto narrativo de las líneas 1020–1028 ni 1030–1040.

Usar la herramienta `Edit` con `old_string` y `new_string` exactos. El texto buscado aparece una única vez en el archivo (es la línea 1029 íntegra y no hay otra ocurrencia de `\underbrace{\frac{N_{CP}}{N + N_{CP}}}_{\text{overhead CP}}` en index.md).
  </action>
  <verify>
    <automated>
grep -c "\\\\underbrace{\\\\frac{N}{N + N_{CP}}}_{\\\\text{eficiencia temporal}}" index.md | grep -qx 1 &amp;&amp; \
! grep -n "\\\\underbrace{\\\\frac{N_{CP}}{N + N_{CP}}}_{\\\\text{overhead CP}}" index.md &amp;&amp; \
grep -c "\\\\text{Overhead CP} = \\\\frac{144}{2048 + 144}" index.md | grep -qx 1 &amp;&amp; \
grep -c "(1 - 0{,}066) \\\\times (1 - 0{,}07) \\\\times 6 \\\\times \\\\frac{2}{3}" index.md | grep -qx 1 &amp;&amp; \
echo "OK: BLOCKER-S.02 corregido, contexto numerico intacto"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep` confirma que aparece exactamente una vez la subcadena `\underbrace{\frac{N}{N + N_{CP}}}_{\text{eficiencia temporal}}` en `index.md`.
    - `grep` no retorna ninguna línea para la subcadena `\underbrace{\frac{N_{CP}}{N + N_{CP}}}_{\text{overhead CP}}` (formulación errónea eliminada por completo).
    - `grep` confirma que la línea 1033 (`\text{Overhead CP} = \frac{144}{2048 + 144}`) permanece literalmente intacta — la etiqueta "Overhead CP" como descripción del cálculo del overhead sigue siendo válida y no se renombra.
    - `grep` confirma que la línea 1037 (`(1 - 0{,}066) \times (1 - 0{,}07) \times 6 \times \frac{2}{3}`) permanece literalmente intacta.
    - El número total de líneas de `index.md` permanece igual al inicial (reemplazo línea por línea).
    - `git diff index.md` para esta tarea muestra exactamente un hunk en la línea 1029.
  </acceptance_criteria>
  <done>
    BLOCKER-S.02 corregido: la fórmula simbólica de `η_neta` usa `N/(N+N_CP)` con etiqueta `eficiencia temporal`. El cálculo numérico de la línea 1037 (que ya era correcto) ahora es coherente con la fórmula simbólica.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit de las correcciones de fórmulas (3 ediciones, 1 archivo)</name>
  <files>index.md</files>
  <read_first>
    index.md (verificación post-edits)
  </read_first>
  <action>
Verificar previo al commit que `git diff index.md` muestra exactamente:
- 1 hunk en torno a la línea 240 (edición de Task 1, parte 1).
- 1 hunk en torno a la línea 249 (edición de Task 1, parte 2).
- 1 hunk en torno a la línea 1029 (edición de Task 2).
- Ningún hunk fuera de esos tres rangos.

Si hay más hunks de los esperados, abortar y reportar — algo se modificó accidentalmente.

Comando del commit:

  git add index.md
  git commit -m "docs(02): fix LaTeX formulas in §2 (orthogonality) and §6 (spectral efficiency)

  BLOCKER-S.01 (D-06): line 240, receiver normalization factor 1/N → 1/√N
    so the orthogonality derivation yields X[k] (not X[k]/√N), matching the
    norm='ortho' IFFT convention used in lab.ipynb and NumPy.
  MINOR-01 (D-08): line 249, same correction propagated into the collapsible
    note that develops the substitution.
  BLOCKER-S.02 (D-07): line 1029, η_neta first factor N_CP/(N+N_CP) → N/(N+N_CP)
    and label 'overhead CP' → 'eficiencia temporal', making the symbolic
    formula consistent with the numeric calculation on line 1037 ((1−0.066)).

  Requirement: CORR-01"

NO incluir otros archivos en este commit. NO ejecutar `git add -A` ni `git add .`. Stage exclusivamente `index.md`.
  </action>
  <verify>
    <automated>
git log --oneline -1 | grep -q "fix LaTeX formulas" &amp;&amp; \
git show --stat HEAD | grep -q "index.md" &amp;&amp; \
git show --name-only HEAD | grep -v "^commit\|^Author\|^Date\|^ \|^$" | sort -u | grep -v "^docs" | grep -qx "index.md" || \
(git show --name-only HEAD | tail -n +6 | sort -u | grep -qx "index.md" &amp;&amp; \
 test "$(git show --name-only HEAD | tail -n +6 | grep -cv '^$')" = "1") &amp;&amp; \
echo "OK: solo index.md commiteado con mensaje correcto"
    </automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%s` contiene la subcadena `fix LaTeX formulas`.
    - `git log -1 --pretty=%B` contiene los tokens literales `BLOCKER-S.01`, `MINOR-01`, `BLOCKER-S.02`, y `CORR-01`.
    - `git show --name-only HEAD` lista exclusivamente `index.md` (no otros archivos).
    - `git status --short` no muestra `index.md` modificado tras el commit.
  </acceptance_criteria>
  <done>
    Las tres correcciones de fórmulas LaTeX están versionadas en un único commit que solo toca `index.md` y referencia explícitamente BLOCKER-S.01, MINOR-01, BLOCKER-S.02 y CORR-01.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Working tree → index.md | Ediciones de display math LaTeX cruzan al árbol de trabajo |
| Source LaTeX → MkDocs-Material render | El texto Markdown es interpretado por MkDocs y MathJax/KaTeX al construir el sitio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | Líneas LaTeX no objetivo (229, 245, 253, 257, 263, 1033, 1037) | mitigate | Acceptance criteria de Tasks 1, 2 y 3 verifican explícitamente con `grep` que esas líneas permanecen intactas; `git diff` debe mostrar solo 3 hunks |
| T-02-06 | Information Disclosure | Cambio inadvertido de notación matemática | accept | El cambio `1/N → 1/√N` es el contenido pedagógico correcto (no expone información sensible); la convención IFFT `norm='ortho'` está documentada en D-06 |
| T-02-07 | Tampering | Indentación de admonition `??? note` (4 espacios) | mitigate | Action de Task 1 indica explícitamente preservar 4 espacios de indentación en la edición de línea 249; acceptance grep busca esos 4 espacios literales |
| T-02-08 | Repudiation | Commit que mezcla ediciones de fórmula con otras correcciones | mitigate | Task 3 stage solo `index.md` y exige `git show --name-only HEAD` retornar exclusivamente `index.md` |
</threat_model>

<verification>
- Las tres líneas objetivo (240, 249, 1029) contienen el texto LaTeX corregido (verificable con `grep` literal por las subcadenas distintivas).
- Las líneas adyacentes (229, 245, 253, 257, 263, 1033, 1037) permanecen literalmente intactas.
- El número total de líneas de `index.md` no cambió: `wc -l index.md` retorna el mismo valor que pre-plan.
- `git diff` antes del commit muestra exactamente 3 hunks.
- El commit final referencia explícitamente CORR-01 en su mensaje.
- MkDocs sigue parseando el archivo (el plan no introduce sintaxis nueva — solo cambia `\frac{1}{N}` por `\frac{1}{\sqrt{N}}` y `\frac{N_{CP}}{N + N_{CP}}` por `\frac{N}{N + N_{CP}}`).
</verification>

<success_criteria>
- [ ] BLOCKER-S.01 corregido (línea 240).
- [ ] MINOR-01 corregido (línea 249).
- [ ] BLOCKER-S.02 corregido (línea 1029, fracción + etiqueta).
- [ ] Líneas adyacentes y no objetivo intactas.
- [ ] Número de líneas del archivo sin cambio.
- [ ] Commit único con solo `index.md` modificado, referenciando CORR-01.
- [ ] CORR-01 (parcial: fórmulas) cumplido.
</success_criteria>

<output>
Create `.planning/phases/02-correcci-n-de-contenido/02-02-SUMMARY.md` when done.
</output>
