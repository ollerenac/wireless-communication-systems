---
phase: 02-correcci-n-de-contenido
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - index.md
autonomous: true
requirements:
  - CORR-01
  - CORR-02
  - CORR-03
tags:
  - snippets
  - figures
  - python
  - mkdocs

must_haves:
  truths:
    - "El snippet MMSE en §4.6 muestra la firma completa `def mmse_equalizer(Y, h, N, SNR_dB):` con docstring y `return` — el alumno puede invocar la función con la misma API que lab.ipynb"
    - "El snippet LS en §4.7 muestra la firma completa `def ls_channel_estimate(Y, pilot_idx, X_pilot, N):` con interpolación real/imag separada — coincide con lab.ipynb celda `23ad1479`"
    - "La sección §4.8 (QAM Demapper) cierra con un bloque `<figure markdown=\"span\">` referenciando `figures/mmse-vs-zf-constellation.png` antes del `---` divider"
    - "Después de este plan no quedan referencias rotas en index.md ni figuras huérfanas en disco (la huérfana mmse-vs-zf-constellation.png queda referenciada en §4.8)"
  artifacts:
    - path: "index.md"
      provides: "Snippets MMSE y LS alineados con notebook + referencia a mmse-vs-zf-constellation.png en §4.8"
      contains: "def mmse_equalizer(Y, h, N, SNR_dB)"
  key_links:
    - from: "index.md §4.6 snippet MMSE"
      to: "lab.ipynb celda 81830cd0 — función mmse_equalizer"
      via: "función Python con misma firma y comportamiento"
      pattern: "def mmse_equalizer\\(Y, h, N, SNR_dB\\)"
    - from: "index.md §4.7 snippet LS"
      to: "lab.ipynb celda 23ad1479 — función ls_channel_estimate"
      via: "función Python con misma firma e interpolación real/imag"
      pattern: "def ls_channel_estimate\\(Y, pilot_idx, X_pilot, N\\)"
    - from: "index.md §4.8 (después del párrafo LLR)"
      to: "figures/mmse-vs-zf-constellation.png"
      via: "<figure markdown=\"span\"> block con figcaption"
      pattern: "figures/mmse-vs-zf-constellation\\.png"
---

<objective>
Aplicar las 3 ediciones restantes catalogadas en `01-AUDIT-FINDINGS.md` que sí cambian el número de líneas de `index.md`: MINOR-03 (expansión del snippet MMSE de 3 → 5 líneas de código), MINOR-04 (expansión del snippet LS de 9 → 6 líneas, manteniendo función), y D-03/MINOR-02 (insertar bloque `<figure>` en §4.8 referenciando la huérfana `mmse-vs-zf-constellation.png` que el Plan 02-01 ya versionó).

**Importancia del orden:** Las ediciones se aplican de menor a mayor línea (806 → 886 → ~933-934). Tras las expansiones de snippets, la línea de inserción en §4.8 ya no será 934 — el executor DEBE recalcular el número final usando un ancla textual (no un número de línea hardcodeado).

Purpose: Resolver CORR-03 (snippets alineados), cerrar CORR-02 (eliminar huérfana sin referenciar) y completar CORR-01 (todas las correcciones de contenido catalogadas en Fase 1).
Output: 3 ediciones puntuales en `index.md`, un único commit referenciando CORR-01/CORR-02/CORR-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-correcci-n-de-contenido/02-CONTEXT.md
@.planning/phases/02-correcci-n-de-contenido/02-PATTERNS.md
@.planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FINDINGS.md
@.planning/phases/02-correcci-n-de-contenido/02-01-SUMMARY.md
@.planning/phases/02-correcci-n-de-contenido/02-02-SUMMARY.md

<interfaces>
<!-- Firmas de las funciones canónicas en lab.ipynb (ground truth) que index.md debe replicar -->

From lab.ipynb celda `81830cd0` (MMSE equalizer):

```python
def mmse_equalizer(Y, h, N, SNR_dB):
    """MMSE: regulariza la inversión del canal → limita amplificación de ruido en fades."""
    H   = np.fft.fft(h, n=N)
    SNR = 10 ** (SNR_dB / 10)
    return (np.conj(H) / (np.abs(H)**2 + 1/SNR)) * Y
```

From lab.ipynb celda `23ad1479` (LS channel estimate):

```python
def ls_channel_estimate(Y, pilot_idx, X_pilot, N):
    """Estimación LS en pilotos + interpolación lineal a todas las subportadoras."""
    H_ls = Y[pilot_idx] / X_pilot
    H_est = (np.interp(np.arange(N), pilot_idx, H_ls.real) +
             1j * np.interp(np.arange(N), pilot_idx, H_ls.imag))
    return H_est
```

Pattern existente para `<figure markdown="span">` (de index.md líneas 233–236, Figura 2 — modelo a copiar):

```html
<figure markdown="span">
  ![El transmisor OFDM es una IFFT](figures/ofdm-ifft-transmitter.png)
  <figcaption markdown="1">**Figura 2.** $N$ símbolos en frecuencia $X[k]$ (izquierda) entran a la IFFT y producen $N$ muestras en tiempo $x[n]$ (derecha). La tasa de muestreo $f_s = N\cdot\Delta f$ es la que garantiza que $\Delta f$ se cancele en el exponente.</figcaption>
</figure>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alinear snippet MMSE (MINOR-03, D-09) — líneas 806–808</name>
  <files>index.md</files>
  <read_first>
    index.md (rango 800–815, ya leído por el planner),
    .planning/phases/02-correcci-n-de-contenido/02-PATTERNS.md (Pattern 5 — MMSE correction target),
    .planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FINDINGS.md (sección 3, MINOR-03)
  </read_first>
  <action>
Reemplazar el snippet inline de MMSE de §4.6 con la firma completa de función del notebook (D-09).

**Buscar** (3 líneas literales dentro de un bloque ```python ... ```, líneas 806–808 del archivo actual):

```
SNR_lin   = 10 ** (SNR_dB / 10)
X_hat     = (np.conj(H) / (np.abs(H)**2 + 1/SNR_lin)) * Y
```

(el `old_string` debe incluir las dos líneas anteriores y posteriores como ancla para evitar match ambiguo — usar las líneas 806, 807, 808 del archivo, que están entre los fences ```python en 806 y ``` en 809).

Estrategia recomendada con la herramienta `Edit`:
- `old_string` =
```
```python
SNR_lin   = 10 ** (SNR_dB / 10)
X_hat     = (np.conj(H) / (np.abs(H)**2 + 1/SNR_lin)) * Y
```
```
- `new_string` =
```
```python
def mmse_equalizer(Y, h, N, SNR_dB):
    """MMSE: regulariza la inversión del canal → limita amplificación de ruido en fades."""
    H   = np.fft.fft(h, n=N)
    SNR = 10 ** (SNR_dB / 10)
    return (np.conj(H) / (np.abs(H)**2 + 1/SNR)) * Y
```
```

Notas:
- Mantener los fences ```python ... ``` exactamente como están (sin lenguaje extra, sin atributos MkDocs).
- La firma debe ser **literal** `def mmse_equalizer(Y, h, N, SNR_dB):` (4 parámetros, en ese orden).
- El docstring va entre triple comillas dobles `"""..."""` (no triple simples).
- Indentación del cuerpo: 4 espacios (estándar PEP-8).
- El bloque resultante tiene 5 líneas de código (def, docstring, H=, SNR=, return) en lugar de las 2 originales — **el archivo crece en 3 líneas netas**.

**Restricciones:**
- NO modificar el `??? note "Los dos límites del MMSE"` que está justo antes (líneas 802–804).
- NO modificar el `??? example "Verificación"` que viene después (líneas 811–814) — ese bloque sigue siendo válido tal cual: muestra la figura `ofdm-ber-equalizers.png` (ya versionada en Plan 02-01).
- NO añadir comentarios extra al snippet.
  </action>
  <verify>
    <automated>
grep -c "def mmse_equalizer(Y, h, N, SNR_dB):" index.md | grep -qx 1 &amp;&amp; \
grep -c "\"\"\"MMSE: regulariza la inversión del canal" index.md | grep -qx 1 &amp;&amp; \
grep -c "return (np.conj(H) / (np.abs(H)\*\*2 + 1/SNR)) \* Y" index.md | grep -qx 1 &amp;&amp; \
! grep -n "^SNR_lin   = 10 \*\* (SNR_dB / 10)$" index.md &amp;&amp; \
! grep -n "^X_hat     = (np.conj(H) / (np.abs(H)\*\*2 + 1/SNR_lin)) \* Y$" index.md &amp;&amp; \
echo "OK: MMSE snippet alineado con notebook"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep` confirma que la firma `def mmse_equalizer(Y, h, N, SNR_dB):` aparece exactamente una vez en `index.md`.
    - `grep` confirma que el docstring `"""MMSE: regulariza la inversión del canal` aparece exactamente una vez.
    - `grep` confirma que la línea `return (np.conj(H) / (np.abs(H)**2 + 1/SNR)) * Y` (sin `_lin` en `SNR`) aparece exactamente una vez — coincide con la API del notebook.
    - `grep` no retorna match para `SNR_lin   = 10 ** (SNR_dB / 10)` ni para `X_hat     = (np.conj(H) / (np.abs(H)**2 + 1/SNR_lin)) * Y` (el snippet inline original eliminado).
    - El número total de líneas de `index.md` aumentó exactamente en +3 respecto al inicio del Plan 02-03 (def + docstring + H= + SNR= + return reemplazan a SNR_lin= + X_hat= ⇒ 5 − 2 = +3 líneas).
    - El bloque ```python ... ``` que envolvía el snippet sigue presente y solo contiene la función nueva (verificable porque `! grep -n` rechazó las líneas antiguas).
  </acceptance_criteria>
  <done>
    El snippet de MMSE en §4.6 muestra la función invocable completa con la misma firma `mmse_equalizer(Y, h, N, SNR_dB)` que usa lab.ipynb. MINOR-03 resuelto.
  </done>
</task>

<task type="auto">
  <name>Task 2: Alinear snippet LS (MINOR-04, D-09) — líneas 886–895</name>
  <files>index.md</files>
  <read_first>
    index.md (rango 880–900, ya leído por el planner),
    .planning/phases/02-correcci-n-de-contenido/02-PATTERNS.md (Pattern 5 — LS correction target),
    .planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FINDINGS.md (sección 3, MINOR-04)
  </read_first>
  <action>
Reemplazar el snippet inline de estimación LS de §4.7 con la firma completa de función del notebook (D-09).

**Buscar** (10 líneas literales dentro de un bloque ```python ... ```, líneas 886–895 del archivo actual — la fence ```python está en 886 y la cierre ``` en 896):

```
```python
pilot_spacing = 8
pilot_idx     = np.arange(0, N, pilot_spacing)
X_pilot       = np.ones(len(pilot_idx))            # pilotos BPSK: valor conocido = +1

# Estimación LS en posiciones piloto
H_ls = Y[pilot_idx] / X_pilot

# Interpolación lineal al resto de subportadoras
H_est = np.interp(np.arange(N), pilot_idx, H_ls)
```
```

(usar el bloque completo incluyendo fences como `old_string` único — el contenido es único en el archivo).

Reemplazar con:

```
```python
def ls_channel_estimate(Y, pilot_idx, X_pilot, N):
    """Estimación LS en pilotos + interpolación lineal a todas las subportadoras."""
    H_ls = Y[pilot_idx] / X_pilot
    H_est = (np.interp(np.arange(N), pilot_idx, H_ls.real) +
             1j * np.interp(np.arange(N), pilot_idx, H_ls.imag))
    return H_est
```
```

Notas:
- Firma literal: `def ls_channel_estimate(Y, pilot_idx, X_pilot, N):` — 4 parámetros, en ese orden exacto.
- La interpolación se aplica por separado a la parte real e imaginaria de `H_ls` y se recombinan con `1j *` — esta es la diferencia funcional clave con el snippet inline (la versión inline pasaba un array complejo a `np.interp`, que en NumPy ≥1.17 funciona pero no es estrictamente correcto).
- Indentación del cuerpo: 4 espacios; la continuación de la línea `H_est = (...)` se alinea visualmente bajo el paréntesis abierto (13 espacios de indentación para `1j * ...`).
- El bloque resultante tiene 6 líneas de código en lugar de las 9 originales — **el archivo decrece en 3 líneas netas**.

**Restricciones:**
- NO modificar el texto narrativo previo (líneas 834–885) ni la nota desplegable `??? note "Cómo funciona esto en LTE"` (líneas 865–884) ni la admonition `??? note "Densidad mínima de pilotos"` (líneas 848–863).
- NO modificar el bloque `??? example "Verificación"` que viene después (líneas 898–903) — `H_est` sigue siendo la misma variable y `H_true = np.fft.fft(h_channel, n=N)` sigue siendo válido.
- NO eliminar el comentario explicativo `# pilotos BPSK: valor conocido = +1`: ese comentario era parte del snippet ANTIGUO. La nueva función ya transporta esa información implícita en su firma (`X_pilot` es parámetro). No reintroducir el comentario en otro lugar.
  </action>
  <verify>
    <automated>
grep -c "def ls_channel_estimate(Y, pilot_idx, X_pilot, N):" index.md | grep -qx 1 &amp;&amp; \
grep -c "\"\"\"Estimación LS en pilotos + interpolación lineal" index.md | grep -qx 1 &amp;&amp; \
grep -c "1j \* np.interp(np.arange(N), pilot_idx, H_ls.imag))" index.md | grep -qx 1 &amp;&amp; \
grep -c "return H_est" index.md | grep -qx 1 &amp;&amp; \
! grep -n "^pilot_spacing = 8$" index.md &amp;&amp; \
! grep -n "^H_ls  = Y\[pilot_idx\] / X_pilot$" index.md &amp;&amp; \
echo "OK: LS snippet alineado con notebook"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep` confirma que la firma `def ls_channel_estimate(Y, pilot_idx, X_pilot, N):` aparece exactamente una vez en `index.md`.
    - `grep` confirma que el docstring `"""Estimación LS en pilotos + interpolación lineal` aparece exactamente una vez.
    - `grep` confirma que la línea `1j * np.interp(np.arange(N), pilot_idx, H_ls.imag))` aparece exactamente una vez (interpolación imaginaria separada).
    - `grep` confirma que la línea `return H_est` aparece al menos una vez (la nueva función).
    - `grep` no retorna match para `pilot_spacing = 8` (el snippet inline original eliminado).
    - El número total de líneas de `index.md` cambió en **−3 líneas netas** respecto al estado posterior a Task 1 (snippet de 10 líneas con fences → 8 líneas con fences).
    - El bloque `??? example "Verificación"` posterior conserva el cálculo `H_true = np.fft.fft(h_channel, n=N)` (verificable con `grep "H_true = np.fft.fft(h_channel, n=N)" index.md`).
  </acceptance_criteria>
  <done>
    El snippet de estimación LS en §4.7 muestra la función invocable completa con la misma firma e interpolación real/imag que usa lab.ipynb. MINOR-04 resuelto.
  </done>
</task>

<task type="auto">
  <name>Task 3: Insertar bloque <figure> de mmse-vs-zf-constellation.png en §4.8 (D-03, MINOR-02)</name>
  <files>index.md</files>
  <read_first>
    index.md (rango 907–937 — §4.8 completa, ya leído por el planner),
    .planning/phases/02-correcci-n-de-contenido/02-PATTERNS.md (Pattern 1 — figure block, y Pattern 6 — §4.8 insertion point),
    .planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FINDINGS.md (sección 2, MINOR-02 huérfana)
  </read_first>
  <action>
Insertar un bloque `<figure markdown="span">` referenciando `figures/mmse-vs-zf-constellation.png` al final de §4.8 (QAM Demapper), justo después del párrafo de cierre sobre LLR y antes del divider `---` que separa §4.8 de §5.

**Anclaje textual (no usar números de línea):** Las Tasks 1 y 2 cambiaron el número total de líneas del archivo. Para localizar el punto de inserción, usar la siguiente cadena ancla — termina en `Sesión 04.` que es el final del último párrafo de §4.8:

  El LLR cuantifica la *confianza* en la decisión: un valor grande en magnitud indica alta certeza; un valor próximo a cero indica ambigüedad. Un decodificador FEC (LDPC, turbo) puede explotar esa información de confianza para corregir errores con una eficiencia muy superior a la que obtendría a partir de bits duros. La transición de decisión hard a soft es uno de los saltos más importantes en el diseño de receptores modernos — se tratará en detalle en la Sesión 04.

Inmediatamente después de esa línea hay una línea en blanco y luego `---`. La inserción va **entre la línea ancla y la línea en blanco que precede al `---`**.

**Estrategia con la herramienta `Edit`:**

- `old_string` (las 3 líneas literales del cierre actual de §4.8):

  El LLR cuantifica la *confianza* en la decisión: un valor grande en magnitud indica alta certeza; un valor próximo a cero indica ambigüedad. Un decodificador FEC (LDPC, turbo) puede explotar esa información de confianza para corregir errores con una eficiencia muy superior a la que obtendría a partir de bits duros. La transición de decisión hard a soft es uno de los saltos más importantes en el diseño de receptores modernos — se tratará en detalle en la Sesión 04.

  ---

- `new_string` (añadir bloque `<figure>` entre el párrafo y el `---`):

  El LLR cuantifica la *confianza* en la decisión: un valor grande en magnitud indica alta certeza; un valor próximo a cero indica ambigüedad. Un decodificador FEC (LDPC, turbo) puede explotar esa información de confianza para corregir errores con una eficiencia muy superior a la que obtendría a partir de bits duros. La transición de decisión hard a soft es uno de los saltos más importantes en el diseño de receptores modernos — se tratará en detalle en la Sesión 04.

  <figure markdown="span">
    ![Constelaciones QAM tras ecualización ZF (izquierda) vs MMSE (derecha)](figures/mmse-vs-zf-constellation.png)
    <figcaption markdown="1">**Figura 3.** Dispersión de la constelación QAM tras ecualización en un canal selectivo en frecuencia: ZF (izquierda) amplifica ruido en las subportadoras débiles — la nube se ensancha desproporcionadamente; MMSE (derecha) la contiene mediante regularización con $1/\text{SNR}$ — los puntos quedan más cerca de los símbolos ideales. La diferencia se ve más pronunciada a SNR baja, donde el regularizador domina.</figcaption>
  </figure>

  ---

**Decisiones de Claude's discretion (CONTEXT.md):**
- **Numeración:** La figura se rotula como **Figura 3** — es la tercera con `<figure>` numerada en el documento (Figura 1 en línea 60, Figura 2 en línea 234). Si en un grep posterior aparece otra "Figura 3" en index.md previa a este punto, el executor debe abortar y reportar — la auditoría no detectó esa numeración, pero conviene verificar.
- **Indentación:** Dos espacios para el `![alt](...)` y el `<figcaption>` dentro del `<figure>` — consistente con Pattern 1 de PATTERNS.md (réplica del estilo de la Figura 2 línea 233–236).
- **Caption:** redacción explícita sobre ZF (izquierda) vs MMSE (derecha) — refleja la organización típica de la figura generada por lab.ipynb.

**Restricciones:**
- NO modificar el contenido del párrafo ancla (las palabras "El LLR cuantifica..." hasta "...Sesión 04." no cambian).
- NO modificar el divider `---` (sigue siendo el separador entre §4.8 y §5).
- NO insertar la figura dentro de un admonition `??? note` o `??? example` — va al nivel de la sección, igual que las Figuras 1 y 2 (Pattern 1).
- NO añadir referencias a las otras figuras huérfanas del Plan 02-01 (`channel-estimation-pilots.png`, `qpsk-decision-regions.png`, `ofdm-time-domain.png`, `cp-effect-constellation.png`) — D-04 explícitamente las deja sin referenciar en index.md.

Tras la inserción, el archivo crece en **+8 líneas netas** (5 líneas del bloque `<figure>` + 1 línea en blanco antes + 1 línea en blanco después; en el `new_string` ya se contemplan estos espaciados — el balance neto entre `old_string` (3 líneas) y `new_string` (10 líneas con espacios) es exactamente +8 si se preservan los blank lines del original; si el executor verifica `wc -l` antes y después y obtiene +7 o +9, debe ajustar manualmente el espaciado y rehacer el `Edit`).
  </action>
  <verify>
    <automated>
grep -c "figures/mmse-vs-zf-constellation.png" index.md | grep -qx 1 &amp;&amp; \
grep -c "\*\*Figura 3.\*\*" index.md | grep -qx 1 &amp;&amp; \
grep -c "Constelaciones QAM tras ecualización ZF" index.md | grep -qx 1 &amp;&amp; \
awk '/se tratará en detalle en la Sesión 04\./,/^---$/' index.md | grep -q "figures/mmse-vs-zf-constellation.png" &amp;&amp; \
echo "OK: figura mmse-vs-zf-constellation insertada en §4.8 antes del divider"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep` confirma que `figures/mmse-vs-zf-constellation.png` aparece exactamente una vez en `index.md` (anteriormente cero — era huérfana en disco).
    - `grep` confirma que `**Figura 3.**` aparece exactamente una vez (numeración correlativa tras Figuras 1 y 2 pre-existentes).
    - `grep` confirma que la cadena `Constelaciones QAM tras ecualización ZF` aparece exactamente una vez.
    - Un `awk` que recorre desde la línea ancla "se tratará en detalle en la Sesión 04." hasta el siguiente `---` solo, encuentra el bloque `<figure>` insertado (verifica que la inserción está en §4.8, no en otra sección).
    - El archivo crece en **+8 líneas netas** respecto al estado posterior a Task 2 (el bloque `<figure>` con su caption y espaciado adyacente añade exactamente 8 líneas — `wc -l index.md` antes y después debe diferir en 8).
    - La figura usa el patrón `<figure markdown="span">` (Pattern 1) — verificable con `grep -A 3 "figures/mmse-vs-zf-constellation.png" index.md | grep -q "markdown=\"1\""`.
  </acceptance_criteria>
  <done>
    §4.8 cierra con un bloque `<figure>` numerado (Figura 3) que referencia `figures/mmse-vs-zf-constellation.png` — la figura huérfana queda ligada al texto y MINOR-02 + D-03 resueltos. El divider `---` final de §4.8 permanece en su lugar como separador con §5.
  </done>
</task>

<task type="auto">
  <name>Task 4: Verificación final + commit unificado de Plan 02-03</name>
  <files>index.md</files>
  <read_first>
    index.md (verificación post-edits — usar grep en lugar de re-Read completo),
    .planning/phases/02-correcci-n-de-contenido/02-CONTEXT.md
  </read_first>
  <action>
Antes del commit, ejecutar la siguiente batería de checks de cierre de Fase 2 — todos deben pasar:

  # 1. No quedan referencias rotas en index.md
  for png in $(grep -oE 'figures/[a-zA-Z0-9_-]+\.png' index.md | sort -u); do
    test -f "$png" || echo "BROKEN: $png"
  done | tee /tmp/broken-refs.txt
  test ! -s /tmp/broken-refs.txt &amp;&amp; rm /tmp/broken-refs.txt

  # 2. No quedan figuras huérfanas tracked sin referencia en index.md
  # (Las 4 figuras de ejercicios — D-04 — están excluidas explícitamente)
  excluded="channel-estimation-pilots qpsk-decision-regions ofdm-time-domain cp-effect-constellation ofdm-ber"
  for png in figures/*.png; do
    base=$(basename "$png" .png)
    case " $excluded " in *" $base "*) continue;; esac
    grep -q "$(basename "$png")" index.md || echo "ORPHAN: $png"
  done | tee /tmp/orphans.txt
  test ! -s /tmp/orphans.txt &amp;&amp; rm /tmp/orphans.txt

  # 3. Snippets alineados
  grep -q "def mmse_equalizer(Y, h, N, SNR_dB):" index.md || echo "MISSING: mmse_equalizer"
  grep -q "def ls_channel_estimate(Y, pilot_idx, X_pilot, N):" index.md || echo "MISSING: ls_channel_estimate"

Si algún check falla, abortar y reportar — NO commitear.

Si todos los checks pasan, hacer el commit:

  git add index.md
  git commit -m "docs(02): align MMSE/LS snippets to lab.ipynb and add §4.8 constellation figure

  MINOR-03 (D-09): line 806–808 inline MMSE snippet → full function signature
    def mmse_equalizer(Y, h, N, SNR_dB) matching lab.ipynb cell 81830cd0.
  MINOR-04 (D-09): line 886–895 inline LS snippet → full function signature
    def ls_channel_estimate(Y, pilot_idx, X_pilot, N) matching lab.ipynb cell
    23ad1479 (with separate real/imag interpolation).
  MINOR-02/D-03: insert <figure> block referencing figures/mmse-vs-zf-constellation.png
    at end of §4.8 (QAM Demapper) — the previously-orphan figure is now bound
    to the MMSE/ZF discussion. No remaining broken figure refs or orphans
    (channel-estimation-pilots, qpsk-decision-regions, ofdm-time-domain,
    cp-effect-constellation explicitly left unreferenced per D-04).

  Requirement: CORR-01, CORR-02, CORR-03"

Stage exclusivamente `index.md`.
  </action>
  <verify>
    <automated>
# Re-run the same closure checks before declaring done
broken=$(for png in $(grep -oE 'figures/[a-zA-Z0-9_-]+\.png' index.md | sort -u); do test -f "$png" || echo broken; done | wc -l) &amp;&amp; \
orphans=$(excluded=" channel-estimation-pilots qpsk-decision-regions ofdm-time-domain cp-effect-constellation ofdm-ber "; for png in figures/*.png; do base=$(basename "$png" .png); case "$excluded" in *" $base "*) continue;; esac; grep -q "$(basename "$png")" index.md || echo orphan; done | wc -l) &amp;&amp; \
test "$broken" = "0" &amp;&amp; test "$orphans" = "0" &amp;&amp; \
grep -q "def mmse_equalizer(Y, h, N, SNR_dB):" index.md &amp;&amp; \
grep -q "def ls_channel_estimate(Y, pilot_idx, X_pilot, N):" index.md &amp;&amp; \
git log --oneline -1 | grep -q "align MMSE/LS snippets" &amp;&amp; \
echo "OK: Fase 2 cerrada — 0 refs rotas, 0 huérfanas no exceptuadas, snippets alineados"
    </automated>
  </verify>
  <acceptance_criteria>
    - El check de referencias rotas reporta 0 archivos faltantes (cada `figures/*.png` referenciado en `index.md` existe en disco).
    - El check de huérfanas reporta 0 archivos no exceptuados (todas las figuras tracked menos las 5 explícitamente exceptuadas — las 4 de D-04 más `ofdm-ber.png` de D-02 — están referenciadas en `index.md`).
    - `grep` confirma `def mmse_equalizer(Y, h, N, SNR_dB):` presente.
    - `grep` confirma `def ls_channel_estimate(Y, pilot_idx, X_pilot, N):` presente.
    - `git log -1 --pretty=%B` contiene los tokens literales `MINOR-02`, `MINOR-03`, `MINOR-04`, `CORR-01`, `CORR-02`, `CORR-03`.
    - `git show --name-only HEAD` lista exclusivamente `index.md`.
    - `git status --short` no muestra `index.md` modificado tras el commit.
  </acceptance_criteria>
  <done>
    Fase 2 cerrada en términos de Plan 02-03: snippets alineados con lab.ipynb (CORR-03), huérfana ligada en §4.8 (CORR-02 completo), ediciones finales de CORR-01 commiteadas. Plan listo para SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Working tree → index.md | Ediciones de markdown + HTML inline (`<figure>`) cruzan al árbol de trabajo |
| index.md → MkDocs build | El nuevo bloque `<figure markdown="span">` debe ser parseado correctamente por MkDocs-Material |
| index.md → figures/ disk | Las nuevas referencias a `mmse-vs-zf-constellation.png` deben resolver a un archivo existente (versionado por Plan 02-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | Numeración de figuras pre-existente (Figura 1 línea 60, Figura 2 línea 234) | mitigate | Acceptance criterion de Task 3 verifica `**Figura 3.**` aparece exactamente UNA vez; action instruye al executor a abortar si encuentra una "Figura 3" previa |
| T-02-10 | Tampering | Snippet `??? example "Verificación"` adyacente a los snippets reemplazados (líneas 811–814 y 898–903) | mitigate | Restricciones explícitas en action de Tasks 1 y 2; verify con grep confirma que `H_est = np.fft.fft(h_channel, n=N)` y `figures/ofdm-ber-equalizers.png` siguen presentes |
| T-02-11 | Information Disclosure | Caption nuevo en §4.8 con notación matemática | accept | Caption usa `$1/\text{SNR}$` siguiendo la convención del documento (Pattern "math inline within prose") — no expone información sensible |
| T-02-12 | DoS | MkDocs build fallido por sintaxis MD/HTML rota tras la inserción | mitigate | Pattern 1 de PATTERNS.md replica exactamente el bloque de la Figura 2 (línea 233–236), que MkDocs ya parsea correctamente; verify ejecuta grep sobre `markdown="1"` para confirmar atributo presente |
| T-02-13 | Repudiation | Commit mezclado con cambios fuera de scope | mitigate | Task 4 stage solo `index.md`; acceptance criterion verifica `git show --name-only HEAD` retorna exclusivamente `index.md` |
</threat_model>

<verification>
- `def mmse_equalizer(Y, h, N, SNR_dB):` presente exactamente una vez en `index.md`.
- `def ls_channel_estimate(Y, pilot_idx, X_pilot, N):` presente exactamente una vez.
- `figures/mmse-vs-zf-constellation.png` referenciada exactamente una vez (anteriormente cero — era huérfana).
- 0 referencias rotas a `figures/*.png` desde `index.md` (cada referencia tiene archivo en disco).
- 0 figuras huérfanas tracked sin referencia (excluyendo D-04 + D-02 explícitos).
- `git log -1` contiene `CORR-01`, `CORR-02`, `CORR-03`.
- `git show --name-only HEAD` solo lista `index.md`.
- MkDocs sigue parseando el archivo (el patrón `<figure markdown="span">` ya existe en otras 2 secciones del documento sin problemas).
</verification>

<success_criteria>
- [ ] Snippet MMSE alineado con `lab.ipynb` (MINOR-03).
- [ ] Snippet LS alineado con `lab.ipynb` (MINOR-04).
- [ ] `mmse-vs-zf-constellation.png` referenciada en §4.8 (MINOR-02 + D-03).
- [ ] 0 referencias rotas a figuras desde `index.md`.
- [ ] 0 figuras huérfanas (excepto las 5 explícitamente exceptuadas por D-02/D-04).
- [ ] Commit único con solo `index.md`, referenciando CORR-01, CORR-02 y CORR-03.
- [ ] Junto con Plans 02-01 y 02-02, los 4 success criteria de Fase 2 en ROADMAP están cumplidos.
</success_criteria>

<output>
Create `.planning/phases/02-correcci-n-de-contenido/02-03-SUMMARY.md` when done. Include in the summary the final `wc -l index.md` value and a 1-line confirmation that all checklist items of `01-AUDIT-FINDINGS.md` están resueltos.
</output>
