---
phase: 01-auditor-a-y-diagn-stico
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-figuras.md
autonomous: true
requirements:
  - CORR-02
tags:
  - audit
  - documentation
  - figures

must_haves:
  truths:
    - "Toda referencia a figura en index.md está cruzada contra los archivos reales en `figures/`"
    - "Cada discrepancia (figura rota o huérfana) está marcada explícitamente con línea y ruta"
    - "Los tres errores conocidos (`ofdm-ber-equalizers.png` × 2 refs, `ofdm-per-subcarrier-ber.png` × 1 ref, `mmse-vs-zf-constellation.png` huérfana) están todos registrados"
  artifacts:
    - path: ".planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-figuras.md"
      provides: "Fragmento Sección 2 del informe — referencias de figuras"
      contains: "## 2. Referencias de Figuras (Rotas / Huérfanas)"
  key_links:
    - from: "01-AUDIT-FRAGMENT-figuras.md"
      to: "figures/ directorio en disco"
      via: "cruce línea-de-referencia × archivo-existente"
      pattern: "figures/[a-z0-9-]+\\.png"
---

<objective>
Producir el inventario completo de referencias a figuras en `index.md` cruzado contra los archivos reales en `figures/`. Marcar cada referencia rota (archivo no existe) y cada figura huérfana (existe en disco pero no referenciada en el texto).

Purpose: Producir la Sección 2 del informe final de diagnóstico (D-04). Es la sección más mecánica pero la más visible — una figura rota se ve como `[imagen no disponible]` al renderizar MkDocs.
Output: `01-AUDIT-FRAGMENT-figuras.md` con tabla de referencias y entradas BLOCKER/MINOR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-auditor-a-y-diagn-stico/01-CONTEXT.md
@.planning/phases/01-auditor-a-y-diagn-stico/01-PATTERNS.md

<interfaces>
<!-- Inventario verificado de referencias (PATTERNS.md Patrón 2) — ESTA ES LA TABLA CANÓNICA -->

Referencias de figuras conocidas en index.md (12 referencias en 11 archivos distintos):

| Línea | Ruta referenciada                            | En disco | Estado            |
|-------|----------------------------------------------|----------|-------------------|
| 60    | figures/isi-problem.png                      | SÍ       | OK                |
| 234   | figures/ofdm-ifft-transmitter.png            | SÍ       | OK                |
| 308   | figures/ofdm-subcarriers.png                 | SÍ       | OK                |
| 405   | figures/cp-illustration.png                  | SÍ       | OK                |
| 739   | figures/zf-equalizer-effect.png              | SÍ       | OK                |
| 753   | figures/zf-equalizer-qam-comparison.png      | SÍ       | OK                |
| 782   | figures/zf-noise-amplification.png           | SÍ       | OK                |
| 814   | figures/ofdm-ber-equalizers.png              | NO       | **ROTA** (1ª ref) |
| 840   | figures/channel-estimation-ls.png            | SÍ       | OK                |
| 868   | figures/lte-resource-grid-pilots.png         | SÍ       | OK                |
| 953   | figures/ofdm-ber-equalizers.png              | NO       | **ROTA** (2ª ref) |
| 961   | figures/ofdm-per-subcarrier-ber.png          | NO       | **ROTA**          |

Archivos en `figures/` sin referenciar (huérfanos):
- `figures/mmse-vs-zf-constellation.png` — generada por `lab.ipynb` celda `81830cd0`

Severidad (D-02):
- Referencia rota (archivo no existe) → **BLOCKER** (figura no carga, visible al estudiante)
- Figura huérfana (existe pero no referenciada) → **MINOR** (no confunde; impacto sobre disco, no sobre lectura)

Formato de entrada (D-03):

```markdown
**[BLOCKER|MINOR]-NN**: descripción en una oración
- **Ubicación:** `index.md` línea XXX  (o `figures/` para huérfanas)
- **Texto actual:** "cita textual del `![](...)` o `<figure>` problemático"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar inventario contra disco y producir fragmento de figuras</name>
  <files>.planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-figuras.md</files>
  <read_first>
    - .planning/phases/01-auditor-a-y-diagn-stico/01-PATTERNS.md (Patrón 2 — tabla canónica de referencias de figuras)
    - .planning/phases/01-auditor-a-y-diagn-stico/01-CONTEXT.md (D-02, D-03, D-04 — severidad, formato, sección 2 del informe)
    - index.md líneas 50–70, 230–240, 305–315, 400–410 (verificar texto de referencia exacto para citas — Variantes A y B descritas en Patrón 1)
    - index.md líneas 735–760, 780–820, 835–870, 950–970 (verificar referencias a figuras ROTAS y figuras OK adyacentes para cita textual)
  </read_first>
  <action>
Crear `01-AUDIT-FRAGMENT-figuras.md` desde cero. Estructura:

```markdown
# Fragmento — Referencias de Figuras (Rotas / Huérfanas)

## 2. Referencias de Figuras (Rotas / Huérfanas)

### 2.1 Inventario completo de referencias

[Tabla idéntica a la del bloque `<interfaces>` arriba; columnas: Línea | Ruta referenciada | En disco | Estado]

### 2.2 Hallazgos

[Entradas BLOCKER-NN y MINOR-NN en formato D-03]

---
Total de hallazgos: [N blockers, M minors]
```

Verificar contra disco antes de copiar la tabla. Usar Bash para confirmar:

```bash
ls /home/researcher/Teaching/uni/2026/wireless-communication-systems/docs/sessions/03-ofdm-systems/figures/ | sort
```

Comparar la salida con la columna "Ruta referenciada" de la tabla canónica. Si descubre cualquier diferencia respecto a PATTERNS.md (archivo que ya existe en disco pero está marcado como NO, o viceversa), corregir la entrada y anotarlo. La tabla del fragmento DEBE reflejar la realidad del disco al momento de la auditoría, no la tabla del Patrón 2 ciegamente.

Registrar los siguientes hallazgos OBLIGATORIOS (D-02 BLOCKER para rotas, MINOR para huérfanas):

1. **BLOCKER-01**: Referencia rota `ofdm-ber-equalizers.png` línea 814 (1ª referencia). Citar textualmente la línea 814 de index.md.
2. **BLOCKER-02**: Referencia rota `ofdm-ber-equalizers.png` línea 953 (2ª referencia, misma figura). Citar textualmente la línea 953.
3. **BLOCKER-03**: Referencia rota `ofdm-per-subcarrier-ber.png` línea 961. Citar textualmente la línea 961.
4. **MINOR-NN**: Figura huérfana `figures/mmse-vs-zf-constellation.png` — existe en disco pero no está referenciada en index.md. Ubicación: `figures/mmse-vs-zf-constellation.png`. Texto actual: indicar "(sin referencia en index.md; generada por lab.ipynb celda `81830cd0`)".

Para cada referencia rota, el campo `Texto actual` debe contener la línea exacta de `index.md` — incluyendo el `<figure>...</figure>` completo si la referencia está dentro de uno (Variante A de Patrón 1), o el `![alt](ruta)` (Variante B). Usar Read con offset preciso para obtener la cita literal.

Per D-03, NO incluir propuesta de corrección. No modificar `index.md`.
  </action>
  <verify>
    <automated>set -e; F=.planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-figuras.md; grep -q "## 2. Referencias de Figuras" "$F" && grep -q "línea 814" "$F" && grep -q "línea 953" "$F" && grep -q "línea 961" "$F" && grep -q "mmse-vs-zf-constellation" "$F" && grep -Eq "^Total de hallazgos: [0-9]+ blockers, [0-9]+ minors" "$F" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `.planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-figuras.md` existe
    - Contiene el encabezado literal `## 2. Referencias de Figuras (Rotas / Huérfanas)`
    - Contiene una tabla con columnas `Línea`, `Ruta referenciada`, `En disco`, `Estado` y al menos 12 filas
    - Contiene al menos 3 entradas marcadas como `**BLOCKER-` que referencian respectivamente las líneas 814, 953 y 961 de index.md
    - Contiene al menos una entrada `**MINOR-` que menciona `mmse-vs-zf-constellation.png`
    - Para cada BLOCKER de referencia rota, el campo `**Texto actual:**` contiene una subcadena que coincide con el contenido real de la línea citada en index.md (verificable con `grep -F "<cita>" index.md`)
    - La línea final coincide con el patrón `Total de hallazgos: N blockers, M minors`
    - `git diff -- index.md figures/` está vacío al final del Task (read-only)
  </acceptance_criteria>
  <done>Sección 2 del informe (figuras) completa con tabla, 3 referencias rotas y al menos 1 huérfana, todas citadas textualmente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem (`figures/`) → fragmento | Listado del directorio confiable, archivos PNG son binarios pero no se ejecutan |
| index.md → fragmento | Lectura de archivo confiable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | index.md y archivos PNG | mitigate | Plan es read-only; verify del Task incluye `git diff -- index.md figures/` vacío |
| T-01-05 | Information Disclosure | `01-AUDIT-FRAGMENT-figuras.md` | accept | Archivo local en `.planning/`, sin secretos |
| T-01-06 | Tampering | inventario inexacto | mitigate | Verificación contra `ls figures/` real antes de copiar la tabla del Patrón 2 |
</threat_model>

<verification>
1. El archivo `01-AUDIT-FRAGMENT-figuras.md` existe con la tabla y los 4 hallazgos mínimos
2. `git diff -- index.md figures/` está vacío
3. Cada cita textual de BLOCKER aparece efectivamente en la línea citada de index.md
</verification>

<success_criteria>
- Tres referencias rotas marcadas como BLOCKER, con línea y cita textual
- Al menos una figura huérfana marcada como MINOR
- Tabla canónica verificada contra disco real al momento de la auditoría
- Fragmento listo para concatenar en `01-AUDIT-FINDINGS.md` por Plan 04
</success_criteria>

<output>
Create `.planning/phases/01-auditor-a-y-diagn-stico/01-02-SUMMARY.md` when done.
</output>
