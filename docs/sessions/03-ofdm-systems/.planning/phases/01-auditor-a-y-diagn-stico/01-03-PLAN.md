---
phase: 01-auditor-a-y-diagn-stico
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-codigo.md
  - .planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-notebook.md
autonomous: true
requirements:
  - CORR-03
  - LAB-01
tags:
  - audit
  - documentation
  - python
  - notebook

must_haves:
  truths:
    - "Cada snippet Python funcional de index.md está cruzado contra su celda equivalente en lab.ipynb"
    - "Las desalineaciones funcionales están marcadas con línea-en-index.md + celda-en-notebook + descripción funcional (D-07)"
    - "Existe registro de si lab.ipynb corre de punta a punta sin error, con número de celda de cualquier fallo"
  artifacts:
    - path: ".planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-codigo.md"
      provides: "Fragmento Sección 3 del informe — snippets desalineados"
      contains: "## 3. Snippets de Código Desalineados"
    - path: ".planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-notebook.md"
      provides: "Fragmento Sección 4 del informe — estado del notebook"
      contains: "## 4. Estado del Notebook (lab.ipynb)"
  key_links:
    - from: "01-AUDIT-FRAGMENT-codigo.md"
      to: "lab.ipynb celdas a5c7793d, 11d893ff, 3ee4d17a, da295e7a, 11e22143, 81830cd0, 23ad1479"
      via: "comparación funcional API + comportamiento"
      pattern: "celda `[a-z0-9-]+`"
---

<objective>
Producir DOS fragmentos del informe final:
1. **Sección 3** — diff conceptual de cada snippet Python en index.md vs su celda equivalente en lab.ipynb (D-05, D-07)
2. **Sección 4** — registro del estado de ejecución del notebook (corre limpio o cuál celda falla)

Purpose: Cerrar los dos criterios de éxito restantes de la Fase 1 (Roadmap §3 y §4). El snippet desalineado es el segundo error de mayor riesgo después de fórmulas; el notebook como ground truth debe verificarse ejecutable.
Output: Dos archivos fragmento listos para ensamblar en `01-AUDIT-FINDINGS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auditor-a-y-diagn-stico/01-CONTEXT.md
@.planning/phases/01-auditor-a-y-diagn-stico/01-PATTERNS.md

<interfaces>
<!-- Mapping canónico snippet ↔ celda (PATTERNS.md Patrón 3) -->

| Snippet en index.md (línea) | Celda en lab.ipynb         | Función                                          |
|-----------------------------|----------------------------|--------------------------------------------------|
| 574–580                     | `a5c7793d`, `cell-08-ex1-code` | `qpsk_map(bits)`                              |
| 618–623                     | `11d893ff`, `cell-08-ex1-code` | `ofdm_tx(X, N_CP)`                            |
| 648–658                     | `3ee4d17a`, `cell-13-ex3-code` | `apply_channel(x_signal, h)`                  |
| 681–685                     | `da295e7a`, `cell-11-ex2-code` | `ofdm_rx_no_channel(y_received, N, N_CP)`     |
| 806–808                     | `11e22143`                     | `zf_equalizer(Y, h, N)`                       |
| 806–808 (??? example)       | `81830cd0`                     | `mmse_equalizer(Y, h, N, SNR_dB)`             |
| 891–894                     | `23ad1479`                     | `ls_channel_estimate(Y, pilot_idx, X_pilot, N)` |

Criterio de inclusión (D-05, D-06):
Un snippet entra en la Sección 3 SOLO si:
1. La diferencia es **funcional** (API distinta, lógica diferente, módulo equivocado)
2. Existe celda ejecutable equivalente en lab.ipynb

NO se reportan: diferencias de nombres de variables, formato, comentarios, docstrings.

Formato Sección 3 (D-03 + D-07):

```markdown
**[BLOCKER|MINOR]-NN**: descripción funcional de la diferencia
- **Ubicación:** `index.md` línea XXX ↔ `lab.ipynb` celda `ID-celda`
- **Texto actual (index.md):** "firma o línea citada de index.md"
- **Texto actual (lab.ipynb):** "firma o línea citada de la celda"
```

Formato Sección 4 (registro de ejecución):

```markdown
## 4. Estado del Notebook (lab.ipynb)

**Método de ejecución:** [Run All con kernel <kernel>] | [celda por celda]
**Resultado global:** [LIMPIO | FALLO en celda <ID>]
**Hallazgos:**
- (si limpio) Sin errores. Todas las celdas se ejecutaron exitosamente.
- (si fallo) Celda `<ID>` falla con `<tipo de error>`. Mensaje: "<mensaje citado>".

**Figuras generadas vs disco:**
[Tabla de plt.savefig(...) vs presencia en figures/]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditar snippets de Python (Sección 3) — diff conceptual contra celdas del notebook</name>
  <files>.planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-codigo.md</files>
  <read_first>
    - .planning/phases/01-auditor-a-y-diagn-stico/01-PATTERNS.md (Patrón 3 — mapping canónico snippet ↔ celda)
    - .planning/phases/01-auditor-a-y-diagn-stico/01-CONTEXT.md (D-05, D-06, D-07 — criterios de inclusión y formato)
    - index.md líneas 570–700 (snippets §4 bloques 4.1–4.4: qpsk_map, ofdm_tx, apply_channel, ofdm_rx_no_channel)
    - index.md líneas 800–815 (snippet ZF y MMSE en `??? example`)
    - index.md líneas 885–900 (snippet LS estimate)
    - lab.ipynb celdas: `a5c7793d`, `11d893ff`, `3ee4d17a`, `da295e7a`, `11e22143`, `81830cd0`, `23ad1479` — leer cada celda completa usando Read sobre lab.ipynb (es JSON, buscar `"id": "<celda>"` y extraer `source`)
  </read_first>
  <action>
Crear `01-AUDIT-FRAGMENT-codigo.md` con estructura:

```markdown
# Fragmento — Snippets de Código Desalineados

## 3. Snippets de Código Desalineados

### 3.1 Inventario verificado snippet ↔ celda

[Tabla con columnas: Línea en index.md | Celda en notebook | Función | Estado (OK | FUNCIONAL-MISMATCH | DIDÁCTICO-OMITIDO)]

### 3.2 Hallazgos

[Entradas BLOCKER-NN y MINOR-NN para cada FUNCIONAL-MISMATCH]

---
Total de hallazgos: [N blockers, M minors]
```

Para cada uno de los 7 snippets del mapping en `<interfaces>`:

1. Leer la celda completa del notebook (extraer la firma exacta `def nombre(...)` y el cuerpo)
2. Leer las líneas correspondientes de index.md
3. Comparar **funcionalmente** según D-05:
   - **Reportar** (BLOCKER): nombre de función distinto, número/orden de argumentos distinto, retorno con shape/tipo distinto, módulo importado distinto, lógica que produce resultado distinto
   - **NO reportar**: nombres de variables locales diferentes, comentarios distintos, docstrings, formato, espaciado, orden de imports

4. Manejo del caso `ofdm_rx_no_channel` (línea 682 index.md vs celda `da295e7a`): PATTERNS.md Patrón 3 reporta que index.md usa `def ofdm_rx_no_channel(y_received, N, N_CP)` y la celda `da295e7a` usa `def ofdm_rx_no_channel(x_with_cp, N, N_CP)`. Verificar leyendo ambos:
   - Si el orden de argumentos es idéntico y solo cambia el nombre del primer parámetro → NO reportar (D-05: diferencias de nombres de variables no son funcionales)
   - Si el orden o cantidad cambia → reportar como BLOCKER

5. Aplicar D-06: si un snippet en index.md es ilustrativo/pseudocódigo sin celda ejecutable equivalente, marcarlo en la tabla como `DIDÁCTICO-OMITIDO` y NO incluirlo en la sección de hallazgos.

Para cada hallazgo, usar el formato definido en `<interfaces>` con AMBAS citas textuales (index.md y celda).

NO incluir propuesta de corrección (D-03). NO modificar index.md ni lab.ipynb.
  </action>
  <verify>
    <automated>set -e; F=.planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-codigo.md; grep -q "## 3. Snippets de Código Desalineados" "$F" && grep -q "qpsk_map\|ofdm_tx\|apply_channel\|ofdm_rx_no_channel\|zf_equalizer\|mmse_equalizer\|ls_channel_estimate" "$F" && grep -Eq "^Total de hallazgos: [0-9]+ blockers, [0-9]+ minors" "$F" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `.planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-codigo.md` existe
    - Contiene el encabezado literal `## 3. Snippets de Código Desalineados`
    - Contiene una sección `### 3.1` con tabla de los 7 snippets canónicos, cada uno con un Estado en `{OK, FUNCIONAL-MISMATCH, DIDÁCTICO-OMITIDO}`
    - Para cada fila marcada `FUNCIONAL-MISMATCH` en 3.1 existe exactamente una entrada correspondiente en `### 3.2 Hallazgos`
    - Cada entrada de 3.2 tiene los 4 campos: `[BLOCKER|MINOR]-NN`, `**Ubicación:**` con línea + celda, `**Texto actual (index.md):**` con cita, `**Texto actual (lab.ipynb):**` con cita
    - Ningún hallazgo se basa exclusivamente en diferencia de nombres de variables locales (D-05)
    - La línea final coincide con `Total de hallazgos: N blockers, M minors`
  </acceptance_criteria>
  <done>Sección 3 del informe completa con los 7 snippets evaluados y todos los mismatches funcionales registrados.</done>
</task>

<task type="auto">
  <name>Task 2: Ejecutar lab.ipynb de punta a punta y registrar estado en Sección 4</name>
  <files>.planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-notebook.md</files>
  <read_first>
    - .planning/phases/01-auditor-a-y-diagn-stico/01-CONTEXT.md (D-04 ítem 4 — sección 4 del informe; D-11 Claude's Discretion sobre kernel)
    - .planning/phases/01-auditor-a-y-diagn-stico/01-PATTERNS.md (Patrón 4 — estructura del notebook y figuras que escribe a disco)
    - lab.ipynb (al menos la celda `cell-03-setup` para conocer dependencias: NumPy, Matplotlib, SciPy)
  </read_first>
  <action>
Ejecutar `lab.ipynb` de punta a punta y registrar el resultado en `01-AUDIT-FRAGMENT-notebook.md`.

**Método de ejecución:** usar `jupyter nbconvert --to notebook --execute` para reproducibilidad — NO requiere interacción. Si `jupyter` no está disponible en PATH, intentar `python -m nbconvert`. Si ninguno está disponible, registrar el hecho como hallazgo y marcar el estado como `NO-VERIFICADO (entorno sin jupyter)`. Comando exacto a ejecutar:

```bash
cd /home/researcher/Teaching/uni/2026/wireless-communication-systems/docs/sessions/03-ofdm-systems
jupyter nbconvert --to notebook --execute lab.ipynb --output lab.executed.ipynb --ExecutePreprocessor.timeout=180 2>&1 | tee /tmp/lab-execute.log
```

El archivo `lab.executed.ipynb` es temporal y NO debe commitearse — añadirlo a `.gitignore` si todavía no está, o borrarlo al final del Task. La intención es preservar el ground truth (`lab.ipynb` sin cambios de outputs).

Crear `01-AUDIT-FRAGMENT-notebook.md` con esta estructura literal:

```markdown
# Fragmento — Estado del Notebook (lab.ipynb)

## 4. Estado del Notebook (lab.ipynb)

**Fecha de auditoría:** 2026-05-22
**Método de ejecución:** jupyter nbconvert --execute (kernel Python 3, timeout 180s por celda)
**Resultado global:** [LIMPIO | FALLO en celda `<ID>` | NO-VERIFICADO (entorno sin jupyter)]

### 4.1 Hallazgos de ejecución

[Si LIMPIO:]
- Sin errores. Todas las celdas se ejecutaron exitosamente de punta a punta.

[Si FALLO:]
**BLOCKER-NN**: La celda `<ID>` falla durante ejecución limpia
- **Ubicación:** `lab.ipynb` celda `<ID>`
- **Tipo de error:** `<NameError|ValueError|ImportError|...>`
- **Mensaje:** "<traceback citado, líneas relevantes>"

[Si NO-VERIFICADO:]
**MINOR-NN**: Notebook no verificable en este entorno (jupyter/nbconvert no instalado).
La verificación de Roadmap §1.4 queda PENDIENTE para Fase 2 en un entorno con kernel disponible.

### 4.2 Inventario de figuras generadas por el notebook vs disco

[Tabla con columnas: plt.savefig(...) en celda | Archivo generado | Existe en figures/ tras ejecución | Referenciada en index.md]

Filas obligatorias (de PATTERNS.md Patrón 4):
- `11e22143` → figures/zf-equalizer-effect.png
- `11e22143` → figures/zf-equalizer-qam-comparison.png
- `81830cd0` → figures/mmse-vs-zf-constellation.png
- `a602f4ca` / `cell-16-ex4-code` → figures/ofdm-ber-equalizers.png
- `eecd25a6` → figures/channel-estimation-pilots.png
- `cell-19-ex5-code` → figures/ofdm-per-subcarrier-ber.png, figures/ofdm-subcarriers.png, figures/cp-illustration.png
- `cell-08-ex1-code` → figures/ofdm-time-domain.png
- `cell-13-ex3-code` → figures/cp-effect-constellation.png
- `2fd4f44b` → figures/qpsk-decision-regions.png

### 4.3 Observación crítica

[Si tras ejecución limpia el archivo `figures/ofdm-ber-equalizers.png` aparece en disco (porque la celda `a602f4ca` lo regenera): registrar como observación —
"La ausencia previa de `ofdm-ber-equalizers.png` se debe a que el notebook no se había ejecutado desde la última reorganización. Tras `Run All`, el archivo se regenera. Esto NO invalida los BLOCKERs de Sección 2 (las referencias rotas existen mientras el notebook no se re-ejecute en CI)."

Si NO aparece tras ejecución, registrar como BLOCKER adicional.]

---
Total de hallazgos: [N blockers, M minors]
```

**Limpieza:** Tras completar el fragmento, eliminar `lab.executed.ipynb` y cualquier artifact temporal. Verificar que `git diff -- lab.ipynb` está vacío (el notebook ground truth NO debe modificarse).
  </action>
  <verify>
    <automated>set -e; F=.planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-notebook.md; test -f "$F" && grep -q "## 4. Estado del Notebook" "$F" && grep -Eq "Resultado global:\s*(LIMPIO|FALLO|NO-VERIFICADO)" "$F" && grep -Eq "^Total de hallazgos: [0-9]+ blockers, [0-9]+ minors" "$F" && [ ! -f /home/researcher/Teaching/uni/2026/wireless-communication-systems/docs/sessions/03-ofdm-systems/lab.executed.ipynb ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `.planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-notebook.md` existe
    - Contiene el encabezado literal `## 4. Estado del Notebook (lab.ipynb)`
    - Contiene la línea `**Resultado global:**` con uno de los tres valores: `LIMPIO`, `FALLO en celda \`<ID>\``, o `NO-VERIFICADO (entorno sin jupyter)`
    - Contiene sección `### 4.2 Inventario de figuras generadas por el notebook vs disco` con tabla de al menos 9 entradas savefig
    - El archivo `lab.executed.ipynb` NO existe en el directorio del proyecto al finalizar el Task
    - `git diff -- lab.ipynb` está vacío (notebook ground truth preservado)
    - La línea final coincide con el patrón `Total de hallazgos: N blockers, M minors`
  </acceptance_criteria>
  <done>Sección 4 del informe completa con resultado global de ejecución, hallazgos de fallo (si aplica), e inventario de figuras generadas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lab.ipynb (ejecutable) → kernel local | Ejecuta código Python con permisos del usuario actual |
| filesystem (figures/) → fragmento | Verificación de existencia post-ejecución |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | lab.ipynb (ground truth) | mitigate | Ejecución vía `nbconvert --output lab.executed.ipynb` preserva el original. Verify del Task chequea `git diff -- lab.ipynb` vacío. Cleanup borra `lab.executed.ipynb` al final |
| T-01-08 | Elevation of Privilege | código del notebook | accept | El notebook usa solo NumPy/Matplotlib/SciPy, dependencias estándar del curso. No instala paquetes; el plan no añade nuevas dependencias |
| T-01-09 | Denial of Service | celda con loop infinito | mitigate | `--ExecutePreprocessor.timeout=180` limita el tiempo por celda. Si una celda excede, el resultado se registra como FALLO y la auditoría continúa |
| T-01-10 | Information Disclosure | fragmentos en `.planning/` | accept | Documentación pedagógica local; sin secretos ni PII |

**Nota ASVS L1:** Este es un proyecto de documentación pedagógica. No hay autenticación, autorización, manejo de datos sensibles, ni superficie de red. La auditoría se limita a evitar tampering del ground truth (lab.ipynb) y a prevenir DoS por celdas infinitas.
</threat_model>

<verification>
1. Ambos archivos fragmento existen con sus encabezados de sección correctos
2. `git diff -- lab.ipynb index.md` está vacío
3. No quedan artefactos temporales (`lab.executed.ipynb` borrado)
</verification>

<success_criteria>
- Inventario verificado de los 7 snippets canónicos con estado por cada uno
- Todas las desalineaciones funcionales registradas con AMBAS citas (index.md y celda)
- Resultado de `Run All` del notebook documentado con número de celda de cualquier fallo
- Inventario de figuras generadas por el notebook vs disco completo
</success_criteria>

<output>
Create `.planning/phases/01-auditor-a-y-diagn-stico/01-03-SUMMARY.md` when done.
</output>
