---
phase: 01-auditor-a-y-diagn-stico
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-formulas.md
autonomous: true
requirements:
  - CORR-01
tags:
  - audit
  - documentation
  - ofdm
  - formulas

must_haves:
  truths:
    - "Existe un fragmento de auditoría con toda fórmula o enunciado falso encontrado en index.md, con sección y línea"
    - "Los errores conocidos de §2 línea 240 (normalización IFFT/FFT) y §6 líneas 1029–1037 (overhead CP) están explícitamente registrados o descartados con cita textual"
    - "Cada entrada usa el formato D-03: severidad + ubicación + texto actual citado"
  artifacts:
    - path: ".planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-formulas.md"
      provides: "Fragmento Sección 1 del informe — fórmulas y enunciados incorrectos"
      contains: "## 1. Fórmulas y Enunciados Incorrectos"
  key_links:
    - from: "01-AUDIT-FRAGMENT-formulas.md"
      to: "index.md líneas 27, 240, 263, 599, 677, 808, 836, 949, 1029–1037, 1063"
      via: "cita textual + número de línea por hallazgo"
      pattern: "(BLOCKER|MINOR)-[0-9]+"
---

<objective>
Auditar todas las fórmulas LaTeX y enunciados teóricos de `index.md` y registrar cada error o inconsistencia en `01-AUDIT-FRAGMENT-formulas.md`. Esta fase es **read-only sobre index.md**; el único archivo creado es el fragmento de auditoría.

Purpose: Producir la Sección 1 del informe final de diagnóstico (D-04). Es la sección de mayor riesgo pedagógico — una fórmula incorrecta lleva al estudiante a un resultado falso.
Output: `01-AUDIT-FRAGMENT-formulas.md` con todos los hallazgos de fórmulas y enunciados, listos para ensamblar en el informe final por el Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-auditor-a-y-diagn-stico/01-CONTEXT.md
@.planning/phases/01-auditor-a-y-diagn-stico/01-PATTERNS.md

<interfaces>
<!-- Formato de entrada en el informe (D-03 desde CONTEXT.md). Usar EXACTAMENTE este formato. -->

Cada entrada del fragmento debe seguir esta estructura literal:

```markdown
**[BLOCKER|MINOR]-NN**: descripción en una oración
- **Ubicación:** `index.md` línea XXX
- **Texto actual:** "cita textual del fragmento problemático"
```

NN se numera correlativo dentro del fragmento empezando en 01.

Severidad (D-02):
- **BLOCKER** — fórmula que produce resultado incorrecto si un estudiante la aplica, enunciado falso que se contradice con lo que demuestra el notebook
- **MINOR** — inconsistencia que no confunde (texto levemente desactualizado pero sin error factual, variable renombrada)

Estructura del fragmento (encabezado fijo):

```markdown
# Fragmento — Fórmulas y Enunciados Incorrectos

## 1. Fórmulas y Enunciados Incorrectos

[entradas BLOCKER-NN y MINOR-NN en orden de línea ascendente]

---
Total de hallazgos: [N blockers, M minors]
```

NO incluir propuestas de corrección (D-03). NO modificar `index.md`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditar fórmulas y enunciados de §4 (bloques 4.1–4.8) y §6 (eficiencia espectral y CP overhead)</name>
  <files>.planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-formulas.md</files>
  <read_first>
    - index.md líneas 500–700 (sección §4 bloques 4.1–4.8 — usar Read con offset=500, limit=200, luego offset=700, limit=200 si hace falta)
    - index.md líneas 1000–1080 (sección §6 — eficiencia espectral y CP overhead)
    - .planning/phases/01-auditor-a-y-diagn-stico/01-PATTERNS.md (Patrón 5 — convenciones de fórmulas LaTeX y errores conocidos)
    - .planning/phases/01-auditor-a-y-diagn-stico/01-CONTEXT.md (D-02, D-03, D-08 — severidad, formato, profundidad de §4 y §6)
  </read_first>
  <action>
Crear `01-AUDIT-FRAGMENT-formulas.md` con el encabezado fijo definido en `<interfaces>`. Auditar EXHAUSTIVAMENTE las secciones §4 y §6 de `index.md` (prioridad alta por D-08). Para cada fórmula o enunciado, registrar si tiene error, citando textualmente el fragmento problemático.

Cubrir AL MENOS estos puntos auditables identificados en PATTERNS.md Patrón 5:

1. Línea ~599 — definición IFFT (verificar normalización `1/sqrt(N)` vs `1/N`)
2. Línea ~677 — relación convolución circular / multiplicación DFT (verificar correspondencia con celda `da295e7a` del notebook)
3. Líneas ~1029–1037 — §6 fórmula de eficiencia espectral `eta_neta` con factor `N_CP / (N + N_CP)`. **Error conocido**: el término representa la *fracción de overhead*, no la *eficiencia*. La eficiencia temporal sería `N / (N + N_CP)`. Determinar si la fórmula publicada invierte el significado y registrarlo como BLOCKER si lo hace. Citar textualmente los `$$...$$` afectados.
4. Línea ~1063 — definición PAPR (verificar contra notebook si hay celda PAPR ejecutable; si no, marcar como didáctico-only y omitir per D-06)
5. Cualquier otro enunciado teórico en §4 que se contradiga con lo demostrado por las celdas del notebook (firmas, comportamiento) — verificar referencia cruzada con `lab.ipynb` celdas `a5c7793d`, `11d893ff`, `3ee4d17a`, `da295e7a`, `11e22143`, `81830cd0`, `23ad1479`.

Per D-03, NO incluir propuesta de corrección. Per D-08, esta sección recibe auditoría más profunda — leer la fórmula entera, no solo el término en disputa.
  </action>
  <verify>
    <automated>grep -c "^\*\*\(BLOCKER\|MINOR\)-[0-9]\+\*\*:" .planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-formulas.md | awk '{ if ($1 >= 2) print "OK ("$1" entries)"; else { print "FAIL — al menos 2 hallazgos esperados de §4+§6"; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `.planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-formulas.md` existe
    - Contiene literal el encabezado `## 1. Fórmulas y Enunciados Incorrectos`
    - Contiene al menos una entrada referenciando líneas 1029–1037 de index.md (§6 CP overhead — error conocido)
    - Cada entrada tiene los tres campos del formato D-03: identificador `[BLOCKER|MINOR]-NN`, línea `**Ubicación:** \`index.md\` línea XXX`, y `**Texto actual:** "..."` con cita textual entre comillas dobles
    - Las entradas están ordenadas por número de línea ascendente
    - Ninguna entrada incluye propuesta de corrección (sin texto del tipo "debería ser", "corregir a", "cambiar por")
  </acceptance_criteria>
  <done>Sección 1 del informe (fórmulas) completa con todos los hallazgos de §4 y §6 citados textualmente.</done>
</task>

<task type="auto">
  <name>Task 2: Auditar fórmulas y enunciados de §1, §2, §5 y §7 — solo errores de contenido</name>
  <files>.planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-formulas.md</files>
  <read_first>
    - .planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-formulas.md (estado actual tras Task 1)
    - index.md líneas 1–280 (Introducción §1 + §2 motivación multiportadora — incluye línea 240 normalización conocida)
    - index.md líneas 920–970 (§5 — Eb/N0 vs SNR, línea 949)
    - index.md líneas 1100–1326 (§7 Síntesis — 5 dimensiones; auditar SOLO errores factuales, no calidad narrativa)
    - .planning/phases/01-auditor-a-y-diagn-stico/01-CONTEXT.md (D-10 — §1, §2, §7 se auditan SOLO para errores de contenido)
  </read_first>
  <action>
Añadir al fragmento existente las entradas de §1, §2, §5 y §7. NO crear archivo nuevo: usar Edit para insertar nuevas entradas en orden de línea ascendente (consolidando con las de Task 1).

Cubrir AL MENOS:

1. Línea ~240 (§2) — **error conocido en PATTERNS.md Patrón 5**: posible inconsistencia de normalización entre el lado izquierdo (`1/N`) y el lado derecho (`1/sqrt(N)`) en la demostración de ortogonalidad. Determinar si es:
   - (a) BLOCKER — error de notación que invierte la relación IFFT/FFT
   - (b) MINOR — desarrollo pedagógico intencional con factores mezclados
   - (c) sin error
   Citar textualmente el bloque LaTeX completo de `$$...$$` y registrar como BLOCKER si no es defendible pedagógicamente.

2. Línea ~27 — convolución del canal `y[n] = sum h[l] x[n-l] + w[n]` (verificar índice de suma y signo)

3. Línea ~263 — condición de ortogonalidad (verificar consistencia con línea 240 si línea 240 tiene error)

4. Línea ~949 (§5) — relación Eb/N0 vs SNR. Verificar si el factor depende del orden de modulación (`log2(M)`) y si está aplicado correctamente.

5. §7 (líneas ~1100 en adelante) — auditar SOLO frases con contenido técnico falsificable (afirmaciones numéricas, relaciones fórmula-resultado). Per D-10, NO evaluar calidad narrativa, transiciones, ni "5 dimensiones bien integradas" — eso es Fase 3.

Mantener la numeración correlativa BLOCKER-NN / MINOR-NN continuando desde el último número usado en Task 1. Reordenar todas las entradas por línea ascendente al final.

Actualizar la línea final `Total de hallazgos: [N blockers, M minors]` con los conteos reales.
  </action>
  <verify>
    <automated>set -e; F=.planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-formulas.md; grep -q "línea 240" "$F" && grep -q "línea 1029\|línea 103[0-7]" "$F" && grep -Eq "^Total de hallazgos: [0-9]+ blockers, [0-9]+ minors" "$F" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `.planning/phases/01-auditor-a-y-diagn-stico/01-AUDIT-FRAGMENT-formulas.md` contiene al menos una entrada que referencia explícitamente la línea 240 de index.md (error conocido §2 normalización)
    - El archivo contiene al menos una entrada que referencia las líneas 1029–1037 de index.md (error conocido §6 CP overhead)
    - La línea final del archivo coincide con el patrón `Total de hallazgos: N blockers, M minors` donde N y M son enteros
    - Las entradas están ordenadas ascendentemente por número de línea (verificable visualmente: línea 27 < 240 < 599 < ...)
    - No hay duplicados (cada par línea+descripción aparece exactamente una vez)
    - Ninguna entrada de §7 evalúa "narrativa", "transición", "integración", "hilo conductor" o "claridad pedagógica" (D-10)
  </acceptance_criteria>
  <done>Fragmento de fórmulas cubre §1, §2, §4, §5, §6, §7 según las profundidades indicadas en D-08 y D-10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| index.md → fragmento de auditoría | Lectura de archivo confiable, escritura local en `.planning/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `01-AUDIT-FRAGMENT-formulas.md` | accept | Archivo local en `.planning/` sin secretos ni PII; proyecto de documentación pedagógica |
| T-01-02 | Tampering | index.md (auditado) | mitigate | Fase 1 es read-only sobre index.md; el plan no incluye Edit/Write sobre index.md. Verify command no modifica el documento auditado |
| T-01-03 | Repudiation | hallazgos sin trazabilidad | mitigate | Cada hallazgo cita línea exacta + texto literal (D-03), permitiendo verificación independiente |
</threat_model>

<verification>
1. El archivo `01-AUDIT-FRAGMENT-formulas.md` existe y tiene encabezado `## 1. Fórmulas y Enunciados Incorrectos`
2. Los dos errores conocidos (línea 240 §2, líneas 1029–1037 §6) están registrados como entradas
3. `git diff index.md` produce salida vacía (read-only sobre index.md)
</verification>

<success_criteria>
- Inventario completo y citado de todos los errores de fórmulas y enunciados en index.md
- Cero modificaciones a index.md (`git diff -- docs/sessions/03-ofdm-systems/index.md` está vacío)
- Formato D-03 respetado en todas las entradas
- Fragmento listo para que el Plan 04 lo concatene en `01-AUDIT-FINDINGS.md`
</success_criteria>

<output>
Create `.planning/phases/01-auditor-a-y-diagn-stico/01-01-SUMMARY.md` when done.
</output>
