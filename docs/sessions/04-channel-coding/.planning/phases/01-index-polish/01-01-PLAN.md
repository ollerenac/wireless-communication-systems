---
phase: 01-index-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/sessions/04-channel-coding/index.md
autonomous: true
requirements:
  - IDX-01

must_haves:
  truths:
    - "El cierre de §1 (El Límite de Shannon) contiene 'La pregunta natural es' anclado al último objeto matemático de la sección"
    - "El cierre de §2 (Codificación de Canal) contiene 'La pregunta natural es' anclado al último objeto matemático"
    - "El cierre de §3.1 (Grafo de Tanner) contiene 'La pregunta natural es' anclado a un objeto de la sección"
    - "El cierre de §3.2 (Belief Propagation) contiene 'La pregunta natural es' anclado a la LLR λ_v o μ"
    - "El cierre de §3.3 (LDPC en 5G NR) contiene 'La pregunta natural es' anclado a la estructura BG/lifting"
    - "El cierre de §4.1 (Polarización del Canal) contiene 'La pregunta natural es' anclado a Z(W) o G_N"
    - "El cierre de §4.2 (Decodificación SC) contiene 'La pregunta natural es' anclado al SCL o CA-Polar"
    - "El cierre de §4.3 (Polar en 5G NR) contiene 'La pregunta natural es' anclado a la tabla de canales"
    - "El cierre de §5 (Comparación LDPC vs Polar) contiene 'La pregunta natural es' anclado a la tabla de criterios"
  artifacts:
    - path: "docs/sessions/04-channel-coding/index.md"
      provides: "Narrative hooks at every sub-section close"
      contains: "La pregunta natural es"
  key_links:
    - from: "§3.2 close"
      to: "§3.3"
      via: "hook connecting BP algorithm to 5G NR implementation"
      pattern: "La pregunta natural es.*La respuesta es"
    - from: "§4.2 close"
      to: "§4.3"
      via: "hook connecting SC/SCL decoder to 5G NR usage"
      pattern: "La pregunta natural es.*La respuesta es"
---

<objective>
Añadir hooks narrativos "La pregunta natural es..." al cierre de TODAS las sub-secciones de index.md: §1, §2, §3.1, §3.2, §3.3, §4.1, §4.2, §4.3, y §5. Cada hook ancla al último objeto matemático introducido en la sección y construye un puente narrativo hacia el concepto de la sección siguiente.

Purpose: Llevar la calidad narrativa de index.md al nivel de sesión 03, donde 11 hooks "La pregunta natural es..." encadenan todas las sub-secciones de forma fluida, eliminando los saltos abruptos entre secciones del borrador actual.

Output: index.md con 9 hooks nuevos insertados al cierre de cada sub-sección, siguiendo el patrón exacto de sesión 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-index-polish/01-CONTEXT.md

<interfaces>
<!-- Patrón exacto de hooks de sesión 03 — extraído de ../03-ofdm-systems/index.md líneas 610-982 -->
<!-- El ejecutor DEBE leer al menos 3 instancias antes de escribir cualquier hook. -->

Instancia 1 (línea 610, al final de §4.1 "Mapper"):
"La pregunta natural es: `X` contiene $N$ símbolos complejos en el dominio de la frecuencia, pero el canal opera en tiempo — no existe forma de transmitir un vector de frecuencias directamente. ¿Cómo se convierten esos $N$ números complejos en una señal de tiempo muestreada lista para pasar por el canal? La respuesta es la IFFT, que genera las $N$ subportadoras ortogonales simultáneamente y les añade el prefijo cíclico."

Instancia 2 (línea 718, al final de §4.3 "FFT + Eliminar CP"):
"La pregunta natural es: la FFT ha separado las subportadoras y cada $Y[k] = H[k]\,X[k] + W[k]$ — el canal distorsionó cada subportadora de forma diferente. Sin corregir esa distorsión, el detector recibirá una nube dispersa en lugar de la constelación original. ¿Cómo invertir la ganancia de canal de cada subportadora independientemente? La respuesta es el ecualizador, que divide o compensa $Y[k]$ subportadora a subportadora."

Instancia 3 (línea 982, al final de §4.5 "Demapper"):
"Con el demapper, la cadena completa está cerrada: bits de entrada atraviesan el transmisor, el canal y el receptor, y los bits de salida pueden compararse con los originales. La pregunta natural es: ¿qué tan bien funciona el sistema? ¿Cuántos bits llegan incorrectos a medida que la SNR disminuye? La respuesta es la curva BER de §5, que mide exactamente el comportamiento de esta cadena para distintos niveles de energía por bit."

Patrón estructural: "[Objeto recién introducido en la sección] ya es [descripción de su logro], pero [limitación o pregunta abierta]. ¿[Pregunta retórica específica]? La respuesta es [concepto de la siguiente sección]."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Leer las referencias obligatorias y planificar el texto de cada hook</name>
  <files>docs/sessions/04-channel-coding/index.md</files>
  <read_first>
    - docs/sessions/04-channel-coding/index.md (leer completo — 503 líneas — para identificar el último objeto matemático de cada sub-sección)
    - docs/sessions/03-ofdm-systems/index.md líneas 600–990 (leer las 11 instancias de "La pregunta natural es" para calibrar tono y densidad)
    - docs/sessions/04-channel-coding/.planning/phases/01-index-polish/01-CONTEXT.md (D-01, D-02 — instrucciones exactas)
  </read_first>
  <action>
Leer index.md completo e identificar el último objeto matemático introducido antes del `---` de cierre de cada sub-sección. Registrar:

- §1 cierre: línea 67 (antes del `---`). Último objeto: límite $E_b/N_0 \geq \ln 2 = -1{,}59\ \text{dB}$ y la figura shannon-capacity.png. La figura describe puntos de operación por modulación y la brecha. El hook debe anclar al límite de Shannon como frontera y preguntar cómo cruzar esa brecha.
- §2 cierre: línea 93 (antes del `---`). Último objeto: ganancia de codificación $G_c \approx 8\ \text{dB}$. Hook ancla a $G_c$ y pregunta cómo lograr esa ganancia en la práctica — qué estructura tienen los códigos que se acercan al límite.
- §3.1 cierre: línea 123 (antes de `####`). Último objeto: propiedad de dispersidad del grafo y ciclos largos. Hook ancla a la dispersidad y pregunta cómo el decodificador la aprovecha para corregir errores.
- §3.2 cierre: línea 149 (antes del `---` y `#### 3.3`). Último objeto: curva waterfall y convergencia BP. Hook ancla a la curva waterfall / decisión $\hat{c}_v$ y pregunta cómo BP se implementa a escala real en un estándar como 5G NR.
- §3.3 cierre: línea 159 (antes del `---`). Último objeto: factor de lifting $Z$ y matrices de expansión. Hook ancla al lifting y pregunta qué familia de códigos ofrece las mismas garantías teóricas de forma determinista — sin búsqueda aleatoria de grafos.
- §4.1 cierre: línea 185 (antes de `#### 4.2`). Último objeto: parámetro de Bhattacharyya $Z(W_N^{(i)})$ y la fracción $C(W)$ de canales perfectos. Hook ancla a $Z(W_N^{(i)})$ y pregunta cómo el decodificador, dado que sabe cuáles son los canales buenos, extrae los bits de información de forma eficiente.
- §4.2 cierre: línea 196 (antes de `#### 4.3`). Último objeto: SCL con $L=8$ y CA-Polar. Hook ancla al CA-Polar y pregunta en qué canales físicos concretos de 5G NR se despliega este esquema y por qué no se usa LDPC en esos canales.
- §4.3 cierre: línea 209 (antes del `---`). Último objeto: tabla de canales de control (PBCH, PDCCH, PUCCH) con sus parámetros. Hook ancla a la diferencia de bloques cortos/largos y pregunta cuáles son los criterios cuantitativos para elegir uno u otro en un sistema real como 5G NR.
- §5 cierre: línea 243 (Paso 4 del ejemplo numérico, antes del `---`). Último objeto: throughput de 160 Mbit/s del ejemplo end-to-end. Hook ancla al resultado del ejemplo y pregunta cómo verificar empíricamente ese comportamiento — cómo se construye y mide en el laboratorio.

NO escribir nada todavía — esta tarea es de lectura y mapeado interno.
  </action>
  <verify>
    <automated>grep -c "La pregunta natural es" docs/sessions/04-channel-coding/index.md</automated>
  </verify>
  <done>El ejecutor ha leído index.md completo y los hooks de sesión 03, y tiene claro el objeto ancla y el concepto destino para cada uno de los 9 hooks a escribir.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Insertar los 9 hooks en index.md</name>
  <files>docs/sessions/04-channel-coding/index.md</files>
  <read_first>
    - docs/sessions/04-channel-coding/index.md (ya leído en Task 1 — no re-leer completo; usar el mapeado de posiciones de Task 1)
  </read_first>
  <action>
Usar la herramienta Edit para insertar el texto del hook al final de cada sub-sección, inmediatamente antes del separador `---` o del encabezado `####` que inicia la siguiente sub-sección. El texto debe ser un párrafo nuevo, sin línea en blanco antes del separador.

Formulaciones a usar (Claude decide la redacción exacta, pero debe seguir el patrón de sesión 03 con los objetos ancla identificados en Task 1):

**Hook §1** — insertar antes del `---` en línea ~67, después de la descripción de la figura shannon-capacity.png:
Anclar al límite $E_b/N_0 \geq \ln 2 = -1{,}59\ \text{dB}$. Preguntar: ¿cómo se construye un código que opera cerca de ese límite? La respuesta apunta a §2 (redundancia estructurada y ganancia de codificación).

**Hook §2** — insertar antes del `---` en línea ~93, después de la definición de $G_c$:
Anclar a $G_c \approx 8\ \text{dB}$ para LDPC $r_c=1/2$. Preguntar: ¿qué estructura interna tiene ese código para obtener esa ganancia? La respuesta apunta a §3 (grafos LDPC dispersos).

**Hook §3.1** — insertar antes del `#### 3.2`, después del párrafo sobre dispersidad y ciclos largos:
Anclar a la dispersidad del grafo de Tanner (ciclos largos). Preguntar: ¿cómo un decodificador explota esa estructura de grafo para propagar correcciones bit a bit? La respuesta apunta a §3.2 (belief propagation).

**Hook §3.2** — insertar antes del `#### 3.3`, después del párrafo sobre la curva waterfall:
Anclar a la decisión $\hat{c}_v$ y a la curva waterfall. Preguntar: ¿cómo se escala BP a los bloques de miles de bits que usa 5G NR? La respuesta apunta a §3.3 (grafos base y lifting).

**Hook §3.3** — insertar antes del `---` que separa §3 de §4, después del párrafo de lifting:
Anclar al factor de lifting $Z$ y las matrices expandidas. Preguntar: ¿existe una familia de códigos cuya capacity-achieving se demuestre matemáticamente, sin búsqueda aleatoria de buenos grafos? La respuesta apunta a §4 (códigos Polar).

**Hook §4.1** — insertar antes del `#### 4.2`, después del párrafo de $Z(W_N^{(i)})$ y la fracción $C(W)$:
Anclar al parámetro de Bhattacharyya $Z(W_N^{(i)})$ como selector de canales buenos. Preguntar: ¿cómo extrae el decodificador los bits de información de los canales buenos sin conocer los bits aún no decodificados? La respuesta apunta a §4.2 (cancelación sucesiva).

**Hook §4.2** — insertar antes del `#### 4.3`, después del párrafo sobre SCL $L=8$ y CA-Polar:
Anclar al CA-Polar (SCL + CRC). Preguntar: ¿en qué canales físicos concretos de 5G NR aparece este esquema y qué lo hace preferible a LDPC en esos casos? La respuesta apunta a §4.3 (tabla de canales de control).

**Hook §4.3** — insertar antes del `---` que separa §4 de §5, después de la tabla de bloques máximos:
Anclar a la diferencia de longitud máxima de bloque (1706 bits Polar vs 8448 bits LDPC). Preguntar: ¿cuáles son los criterios cuantitativos para elegir entre LDPC y Polar en un sistema real, y qué ganancia ofrece cada uno en su dominio? La respuesta apunta a §5 (comparación y tabla de criterios).

**Hook §5** — insertar antes del `---` que separa §5 de Síntesis, después del Paso 4 del ejemplo numérico (throughput 160 Mbit/s):
Anclar al resultado del ejemplo (throughput $R \approx 160$ Mbit/s, ganancia de codificación $\approx 7\ \text{dB}$). Preguntar: ¿cómo se verifica empíricamente que estos códigos alcanzan esa ganancia? La respuesta apunta a la sección de laboratorio.

Restricciones de formato:
- Párrafo plano, sin bullet points ni encabezados
- Comillas matemáticas con $...$ igual que sesión 03
- Longitud: 2-4 oraciones, misma densidad que instancias de sesión 03
- No usar bloques de código (```) dentro del hook
- Cada hook en una sola línea de párrafo (sin saltos de línea internos forzados)
  </action>
  <verify>
    <automated>grep -c "La pregunta natural es" docs/sessions/04-channel-coding/index.md</automated>
  </verify>
  <done>
    - grep devuelve exactamente 9
    - Cada hook aparece antes del separador `---` o `####` de su sección (verificar posición con grep -n "La pregunta natural es" index.md)
    - Cada hook contiene "La respuesta es" (grep -c "La respuesta es" index.md devuelve 9)
    - Ningún hook tiene triple backtick ni encabezado markdown interno
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editor → index.md | Modificaciones de texto plano; sin código ejecutable ni inputs externos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | Edición de index.md | accept | Solo edición de texto narrativo en archivo markdown; sin instalación de paquetes ni ejecución de código |
</threat_model>

<verification>
Al finalizar el plan:

```bash
# 1. Exactamente 9 hooks
grep -c "La pregunta natural es" docs/sessions/04-channel-coding/index.md
# Esperado: 9

# 2. Todos con "La respuesta es"
grep -c "La respuesta es" docs/sessions/04-channel-coding/index.md
# Esperado: 9

# 3. Posiciones de los hooks (deben estar distribuidas, no agrupadas)
grep -n "La pregunta natural es" docs/sessions/04-channel-coding/index.md

# 4. Sin triple backtick dentro de un hook
grep -A2 "La pregunta natural es" docs/sessions/04-channel-coding/index.md | grep -c '```'
# Esperado: 0
```
</verification>

<success_criteria>
- index.md contiene exactamente 9 instancias de "La pregunta natural es"
- Cada instancia contiene "La respuesta es" en la misma oración o la siguiente
- Las 9 instancias están distribuidas en posiciones correspondientes al cierre de §1, §2, §3.1, §3.2, §3.3, §4.1, §4.2, §4.3, §5
- El estilo es coherente con las instancias de sesión 03 (párrafo plano, objeto ancla matemático explícito, pregunta retórica, respuesta que apunta a la sección siguiente)
</success_criteria>

<output>
Crear `.planning/phases/01-index-polish/01-01-SUMMARY.md` al completar
</output>
