---
phase: 01-index-polish
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - docs/sessions/04-channel-coding/index.md
autonomous: true
requirements:
  - FIG-01
  - IDX-03

must_haves:
  truths:
    - "Las referencias `![alt](figures/shannon-capacity.png)` y `![alt](figures/waterfall-curves.png)` ya no existen en el archivo"
    - "Ambas figuras están en bloques `<figure markdown=\"span\">` con `<figcaption markdown=\"1\">` de al menos 2 líneas"
    - "Cada bloque `<figure>` contiene un comentario HTML `<!-- generada por celda N de lab.ipynb -->`"
    - "El diagrama mermaid de §3.1 permanece intacto tal como estaba"
    - "Debajo del mermaid existe un bloque `<figure>` placeholder para `figures/tanner-graph.png`"
    - "La afirmación '86% de la capacidad de Shannon' en la Introducción corresponde al resultado de sesión 03 Ejercicio 6(d)"
    - "La afirmación sobre BER pre-FEC en la Introducción dice $10^{-1}$, no $10^{-1.5}$"
  artifacts:
    - path: "docs/sessions/04-channel-coding/index.md"
      provides: "Figure blocks + factual intro corrections"
      contains: "<figure markdown=\"span\">"
  key_links:
    - from: "shannon-capacity.png figure block"
      to: "lab.ipynb celda 3"
      via: "comentario HTML <!-- generada por celda 3 de lab.ipynb -->"
      pattern: "generada por celda.*lab.ipynb"
    - from: "tanner-graph.png figure placeholder"
      to: "figures/tanner-graph.png"
      via: "path consistente con Fase 3"
      pattern: "figures/tanner-graph.png"
---

<objective>
Tres cambios en index.md: (1) convertir las 2 referencias planas `![alt](path)` a bloques `<figure markdown="span">` completos (D-04); (2) añadir un placeholder `<figure>` para `tanner-graph.png` debajo del mermaid en §3.1, manteniendo el mermaid intacto (D-05); (3) verificar y corregir las afirmaciones numéricas del párrafo de Introducción contra sesiones 02-03 (D-07).

Purpose: Cumplir el formato MkDocs-Material obligatorio para figuras, preparar el anchor del grafo de Tanner para Fase 3, y garantizar que las referencias cruzadas del intro son factuales y verificables.

Output: index.md con 3 bloques `<figure>` (2 reales + 1 placeholder), formato correcto en todos, e Introducción con afirmaciones numéricas corregidas si había errores.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-index-polish/01-CONTEXT.md

<interfaces>
<!-- Patrón exacto de figura de sesión 03 — extraído de ../03-ofdm-systems/index.md líneas 59-62 -->

FORMATO OBLIGATORIO (copiar estructura exacta):
```html
<figure markdown="span">
  ![alt text](figures/filename.png)
  <!-- generada por celda N de lab.ipynb -->
  <figcaption markdown="1">**Figura N.** Primera línea descriptiva con el contenido principal.
  Segunda línea con detalle técnico adicional o interpretación.
  </figcaption>
</figure>
```

Reglas:
- La imagen va en la primera línea interior con 2 espacios de indentación
- El comentario HTML va en la segunda línea interior
- El `<figcaption markdown="1">` abre en la tercera línea
- La primera línea del caption empieza con `**Figura N.**`
- Mínimo 2 líneas de descripción dentro del figcaption
- El `</figcaption>` cierra solo, con la etiqueta `</figure>` inmediatamente después

<!-- Celdas del notebook que generan las figuras — extraído de lab.ipynb -->

- Celda 3 (cell-3): genera la curva Shannon (eta vs EbN0, con límite -1.59 dB y punto 64-QAM 3/4)
  → usar "<!-- generada por celda 3 de lab.ipynb -->"
- No hay celda que genere waterfall-curves.png actualmente en el borrador del notebook
  → usar "<!-- generada por celda 13 de lab.ipynb -->" (celda 13 = simulate_ldpc_bler, produce la curva waterfall)
  → O alternativamente, si la celda no existe todavía: "<!-- será generada por lab.ipynb en la Fase 2 -->"
  → Usar el comentario que sea más honesto con el estado actual: "<!-- será generada por lab.ipynb — Fase 2 -->"

<!-- Afirmaciones numéricas del intro a verificar — extraídas de index.md línea 23 -->

TEXTO ACTUAL (línea 23):
"En la Sesión 03, el Ejercicio 6 calculó que un sistema OFDM operaba al 86% de la capacidad de Shannon en condiciones realistas. En la Sesión 02, el selector de MCS usó un umbral de BER pre-FEC de $10^{-1.5}$ — una BER que un detector sin código nunca aceptaría — dando por sentado que el código LDPC de 5G NR la transformaba en $<10^{-5}$."

VERIFICACIÓN FACTUAL:
- "86% de la capacidad de Shannon": CORRECTO — sesión 03, línea 1369: "Fracción: $15,4/17,8 \approx \mathbf{86\%}$"
- "BER pre-FEC de $10^{-1.5}$": INCORRECTO — sesión 02 dice "$10^{-1}$ o superior" (línea 267). El valor $10^{-1.5}$ (~0.032) no aparece en sesión 02. Corregir a $10^{-1}$.
- "transformaba en $<10^{-5}$": CORRECTO — sesión 02 línea 333 dice "de $10^{-1}$ (pre-decodificación) a $10^{-5}$ (post-decodificación)"

CORRECCIÓN REQUERIDA: Cambiar $10^{-1.5}$ por $10^{-1}$ en el párrafo de Introducción.

<!-- Referencias de figura actuales en index.md — para localizar exactamente dónde editar -->

Línea 63: ![Capacidad de Shannon y puntos de operación](figures/shannon-capacity.png)
  → Contexto: dentro de §1 (El Límite de Shannon), después de "La figura siguiente muestra..."

Línea 215: ![Curvas waterfall LDPC y Polar vs BPSK sin código](figures/waterfall-curves.png)
  → Contexto: dentro de §5 (Comparación LDPC vs Polar), al inicio de la sección

Mermaid: líneas 108-119 (```mermaid ... ```)
  → Seguido de texto en cursiva (línea 121): "*Grafo de Tanner para el código (7,4)..."
  → El placeholder de figura va DESPUÉS de ese texto en cursiva, antes del párrafo "La dispersidad del grafo..."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convertir referencias planas a bloques `<figure>` (D-04)</name>
  <files>docs/sessions/04-channel-coding/index.md</files>
  <read_first>
    - docs/sessions/04-channel-coding/index.md líneas 59-68 (contexto figura shannon-capacity)
    - docs/sessions/04-channel-coding/index.md líneas 211-218 (contexto figura waterfall-curves)
    - docs/sessions/03-ofdm-systems/index.md líneas 59-62 (patrón de figura de referencia)
    - docs/sessions/04-channel-coding/.planning/phases/01-index-polish/01-CONTEXT.md sección D-04
  </read_first>
  <action>
Reemplazar las dos referencias planas con bloques `<figure markdown="span">` completos.

**Figura 1 — shannon-capacity.png** (línea 63 actual):
Reemplazar `![Capacidad de Shannon y puntos de operación](figures/shannon-capacity.png)` con:

```html
<figure markdown="span">
  ![Capacidad de Shannon y puntos de operación](figures/shannon-capacity.png)
  <!-- generada por celda 3 de lab.ipynb -->
  <figcaption markdown="1">**Figura 1.** Capacidad $C/B$ (bit/s/Hz) como función del SNR (dB). La curva negra es la frontera de Shannon $C/B = \log_2(1+\text{SNR})$; los puntos de colores muestran los puntos de operación de BPSK, QPSK, 16-QAM, 64-QAM y 256-QAM a BER = $10^{-3}$ (valores del Ejercicio 5 de la Sesión 02).
  Las flechas horizontales indican la ganancia de codificación disponible: la reducción de Eb/N0 que permitiría un buen código operando a la misma tasa espectral. El límite absoluto de $E_b/N_0 = -1{,}59$ dB aparece como línea vertical roja.
  </figcaption>
</figure>
```

**Figura 2 — waterfall-curves.png** (línea 215 actual):
Reemplazar `![Curvas waterfall LDPC y Polar vs BPSK sin código](figures/waterfall-curves.png)` con:

```html
<figure markdown="span">
  ![Curvas waterfall LDPC y Polar vs BPSK sin código](figures/waterfall-curves.png)
  <!-- será generada por lab.ipynb — Fase 2 -->
  <figcaption markdown="1">**Figura 2.** Curvas de BER (*waterfall*) en función de $E_b/N_0$ para BPSK sin código (negro), LDPC con tasas $r_c = 1/2$, $2/3$, $3/4$ (azul) y Polar con tasas equivalentes (naranja), todas sobre canal AWGN.
  La característica "curva en cascada" de LDPC y Polar contrasta con la caída suave de BPSK sin código: por encima del umbral de decodificación, la BER cae precipitosamente varias décadas en pocos dB, alcanzando la ganancia de codificación neta de 4–8 dB respecto a BPSK sin código a BER $= 10^{-5}$.
  </figcaption>
</figure>
```

Nota: eliminar el párrafo descriptivo que actualmente sigue a la referencia plana de waterfall-curves.png (líneas 216-218 aprox., "El panel muestra..."), ya que esa descripción pasa a ser el caption de la figura. Si el párrafo descriptivo es sustancialmente diferente del caption propuesto, conservar el párrafo descriptivo después del bloque `<figure>` y abreviar el caption a 2 líneas mínimas.
  </action>
  <verify>
    <automated>grep -c '<figure markdown="span">' docs/sessions/04-channel-coding/index.md</automated>
  </verify>
  <acceptance_criteria>
    - grep devuelve al menos 2 (las dos figuras principales)
    - "grep '!\[' docs/sessions/04-channel-coding/index.md | grep -v '^\s*<!--'" lista solo referencias dentro de bloques `<figure>`, no referencias planas sueltas
    - "grep 'generada por celda 3' docs/sessions/04-channel-coding/index.md" devuelve 1 coincidencia
    - "grep 'Fase 2' docs/sessions/04-channel-coding/index.md" devuelve al menos 1 coincidencia (waterfall placeholder)
    - Cada bloque `<figure>` contiene `<figcaption markdown="1">` con al menos 2 líneas antes de `</figcaption>`
    - "grep -c '\*\*Figura [0-9]\.\*\*' docs/sessions/04-channel-coding/index.md" devuelve al menos 2
  </acceptance_criteria>
  <done>Las dos referencias planas `![alt](path)` han sido reemplazadas por bloques `<figure markdown="span">` completos con comentario HTML y figcaption de al menos 2 líneas.</done>
</task>

<task type="auto">
  <name>Task 2: Añadir placeholder `<figure>` para tanner-graph.png y corregir intro factual (D-05, D-07)</name>
  <files>docs/sessions/04-channel-coding/index.md</files>
  <read_first>
    - docs/sessions/04-channel-coding/index.md líneas 108-130 (bloque mermaid y texto que le sigue en §3.1)
    - docs/sessions/04-channel-coding/index.md líneas 21-26 (párrafo de Introducción con afirmaciones a corregir)
    - docs/sessions/04-channel-coding/.planning/phases/01-index-polish/01-CONTEXT.md secciones D-05 y D-07
  </read_first>
  <action>
**D-05 — Placeholder tanner-graph.png:**

Localizar el texto en cursiva "*Grafo de Tanner para el código (7,4) de Hamming...*" (línea ~121) que sigue al bloque mermaid. Inmediatamente DESPUÉS de ese texto en cursiva, y ANTES del párrafo "La dispersidad del grafo es...", insertar el bloque placeholder:

```html
<figure markdown="span">
  ![Grafo de Tanner del código LDPC](figures/tanner-graph.png)
  <!-- será generada por lab.ipynb — Fase 3 -->
  <figcaption markdown="1">**Figura 3.** Grafo bipartito de Tanner para un código LDPC representativo. Los nodos de variable $v_j$ (círculos) representan los bits del codeword; los nodos de verificación $c_i$ (cuadrados) representan las ecuaciones de paridad $H_{ij}=1$ de la matriz $\mathbf{H}$.
  La dispersidad del grafo — pocos unos en $\mathbf{H}$, aristas escasas — garantiza ciclos largos y convergencia rápida del decodificador belief propagation. Esta figura se generará en la Fase 3 del laboratorio.
  </figcaption>
</figure>
```

El diagrama mermaid (líneas 108-119) NO se modifica — permanece exactamente como está.

**D-07 — Corrección factual del intro:**

Localizar la oración en la Introducción (línea ~23): "el selector de MCS usó un umbral de BER pre-FEC de $10^{-1.5}$"

Cambiar $10^{-1.5}$ por $10^{-1}$ en esa oración. La oración corregida debe leer: "el selector de MCS usó un umbral de BER pre-FEC de $10^{-1}$"

Verificar que "86% de la capacidad de Shannon" permanece sin cambios (es factualmente correcto según sesión 03 Ejercicio 6(d)).

No realizar ninguna otra modificación al párrafo de Introducción.
  </action>
  <verify>
    <automated>grep -c '<figure markdown="span">' docs/sessions/04-channel-coding/index.md</automated>
  </verify>
  <acceptance_criteria>
    - grep devuelve 3 (shannon-capacity + waterfall-curves + tanner-graph placeholder)
    - "grep 'tanner-graph.png' docs/sessions/04-channel-coding/index.md" devuelve al menos 1 coincidencia dentro de un bloque `<figure>`
    - "grep 'mermaid' docs/sessions/04-channel-coding/index.md" sigue devolviendo 1 coincidencia (el mermaid original no fue eliminado)
    - "grep '10^{-1\.5}' docs/sessions/04-channel-coding/index.md" devuelve 0 (la afirmación incorrecta fue eliminada)
    - "grep '10^{-1}' docs/sessions/04-channel-coding/index.md" devuelve al menos 1 (la corrección está presente en el intro)
    - "grep '86%' docs/sessions/04-channel-coding/index.md" sigue devolviendo 1 (el valor correcto permanece)
    - El placeholder tanner-graph.png contiene "Fase 3" en el comentario HTML
  </acceptance_criteria>
  <done>
    - 3 bloques `<figure markdown="span">` presentes en el archivo
    - El mermaid de §3.1 permanece intacto
    - El placeholder tanner-graph.png está debajo del mermaid con caption de 2+ líneas
    - La intro dice $10^{-1}$ (no $10^{-1.5}$) y sigue diciendo 86%
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editor → index.md | Modificaciones de texto plano y HTML en Markdown; sin código ejecutable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | Edición de index.md | accept | Solo edición de texto y HTML en Markdown; sin instalación de paquetes ni ejecución de código externo |
</threat_model>

<verification>
Al finalizar el plan:

```bash
# 1. Total de bloques <figure>
grep -c '<figure markdown="span">' docs/sessions/04-channel-coding/index.md
# Esperado: 3

# 2. Sin referencias planas sueltas (fuera de figure blocks)
grep -n "^\!\[" docs/sessions/04-channel-coding/index.md
# Esperado: 0 líneas que comiencen con ![ (todas deben estar dentro de <figure>)

# 3. Mermaid intacto
grep -c "mermaid" docs/sessions/04-channel-coding/index.md
# Esperado: ≥1

# 4. Corrección factual intro
grep "10^{-1.5}" docs/sessions/04-channel-coding/index.md
# Esperado: sin salida (cero coincidencias)

grep "10^{-1}" docs/sessions/04-channel-coding/index.md | head -3
# Esperado: al menos 1 línea en la Introducción

# 5. 86% sigue presente
grep "86%" docs/sessions/04-channel-coding/index.md
# Esperado: ≥1

# 6. Comentarios HTML correctos
grep "generada por celda 3" docs/sessions/04-channel-coding/index.md
grep "Fase 2" docs/sessions/04-channel-coding/index.md
grep "Fase 3" docs/sessions/04-channel-coding/index.md
# Cada uno: ≥1 coincidencia

# 7. Figcaptions con al menos 2 líneas (verificación manual requerida):
grep -A5 '<figcaption markdown="1">' docs/sessions/04-channel-coding/index.md
```
</verification>

<success_criteria>
- Exactamente 3 bloques `<figure markdown="span">` en index.md
- Cero referencias planas `![alt](path)` fuera de un bloque `<figure>`
- El bloque mermaid de §3.1 está intacto (grep "mermaid" devuelve ≥1)
- El placeholder para tanner-graph.png aparece después del mermaid, con caption de 2+ líneas y comentario "Fase 3"
- La Introducción dice $10^{-1}$ (no $10^{-1.5}$) y sigue diciendo 86%
- Cada bloque `<figure>` contiene `**Figura N.**` en el figcaption
</success_criteria>

<output>
Crear `.planning/phases/01-index-polish/01-03-SUMMARY.md` al completar
</output>
