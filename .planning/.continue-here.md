---
context: default
phase: none
task: 7
total_tasks: 7
status: paused
last_updated: 2026-08-14T08:35:00.000Z
---

<current_state>
No formal GSD phase or roadmap is active. The five final-exam notebooks are
graded, calibrated across students, and each student's feedback is published as
an unlisted page on the course site. `main` is clean and synchronized with
`origin/main` at `202314ca`; the deployment run finished green and all five
pages return 200.
</current_state>

<completed_work>

- Graded the five submissions in `exams/final-01/` against a phase rubric
  (F0 = 2 pts, F1–F6 = 3 pts; within each phase 60% numeric / 40% written
  justification), five independent graders in parallel.
- Ran a cross-student calibration pass. It found real inter-grader drift on the
  justification axis and produced six adjustments of 0.1–0.4 (recorded in
  `calibracion.json` in the session scratchpad).
- Final exam grades: Bermúdez 18.8, Taipe 18.7, Castilla 18.6, Loayza 18.6,
  Livia 16.1. Weighted course grades (30/30/40): 17.6, 14.9, 17.8, 15.9, 15.5.
- Wrote `feedback.md` inside each student folder plus `RESUMEN-NOTAS.md`.
- Removed the AI/process tells the instructor flagged: calibration notes,
  rubric vocabulary ("componente numérico", "la rúbrica califica", "el
  corrector"), and — importantly — peer names that had leaked into three
  reports ("misma falta que Luis", "el diagnóstico de Meri").
- Appended the weighted course-grade table to every report.
- Broadened `.gitignore` so submissions, `feedback.md` and `RESUMEN-NOTAS.md`
  can never reach the public repo.
- Published `docs/f/<nombre>-<12 dígitos>.html`, one per student, generated by
  `scripts/build-feedback-pages.py`.
- Created six Google Docs in Drive earlier in the session; the five student
  copies are now superseded by the site pages.
</completed_work>

<remaining_work>

- Send each student their personal URL. The pages are unlisted, so nobody can
  reach them until the links go out.
- Decide what to do with the Drive folder "Examen Final 5G — Retroalimentación
  2026": five now-redundant student docs plus the internal summary, none ever
  shared.
- Session 07 editorial backlog, untouched this session: lesson closing material
  and the construction notice, the two Phase 4 SVGs
  (`sectorizacion_3x120.svg`, `downtilt_geometria.svg`), the instructor-script
  artifact `98209b61-23d1-425e-837b-89bf180242a8`, and the stale sections of
  `PROJECT.md` / `STATE.md`.
</remaining_work>

<decisions_made>

- Feedback pages are `.html`, never `.md`. MkDocs copies non-Markdown files
  verbatim, keeping them out of the search index, the sitemap and the nav.
  As `.md` they would land in `search_index.json`, where typing a surname into
  the course search box would surface a classmate's report.
- URL shape `<slug>-<12 dígitos>.html`, codes persisted in
  `exams/final-01/.enlaces.json` so rebuilds keep the links stable; `--rotar`
  invalidates them.
- The protection is obscurity, and the instructor accepted that knowingly for a
  five-student class. Search-engine indexing was still blocked (`noindex` meta
  plus `docs/robots.txt`), since permanent public indexing of names and grades
  is a different order of risk from a classmate peeking.
- Calibration notes stay internal; students see grades, not moderation
  mechanics.
</decisions_made>

<blockers>
- None technical. The only thing standing between the work and the students is
  the instructor sending five links.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — authoritative structured position for this pause.
2. `scripts/build-feedback-pages.py` — how the pages are generated and how the
   codes stay stable.
3. `exams/final-01/RESUMEN-NOTAS.md` — grades, per-phase breakdown and the
   calibration record (gitignored, local only).
4. `.planning/STATE.md` — decisions and history; note that older position and
   todo sections predate the completed Session 07 work.

## Critical Anti-Patterns (do NOT repeat these)

- **Do not commit anything under `exams/final-01/`.** The repository is public
  and git history is permanent. Only the rendered `docs/f/` pages are meant to
  be published.
- **Do not publish an unlisted page as Markdown.** Verify after any publication
  that both `sitemap.xml` and `search/search_index.json` report zero `/f/`
  entries on the live site.
- **Do not widen scope without being asked.** This session twice drifted into
  unrequested work — a spreadsheet rounding analysis and a privacy audit of
  published pages — and the instructor pushed back both times. Report a
  finding, then stop.

## Infrastructure State

- No server, watcher, notebook kernel or background job is running.
- GitHub Pages deployment for `202314ca` completed successfully; the five
  feedback pages return 200, a wrong code returns 404.
- Local Sionna environment: `/home/researcher/miniforge3/envs/ran-design`,
  Python 3.12, Sionna 2.0.1. Non-interactive commands need
  `LD_LIBRARY_PATH=/home/researcher/miniforge3/envs/ran-design/lib`.
- Feedback base URL:
  `https://ollerenac.github.io/wireless-communication-systems/f/`

<context>
Raised with the instructor and explicitly NOT actioned, recorded so it is not
rediscovered and acted on by mistake: `docs/sessions/03-ofdm-systems/index.md`
and `docs/sessions/04-gnuradio-ofdm/gnuradio-ofdm.md` publish named lab-grade
tables on the live public site, including 2/20 and 5/20 marks and an
observation describing copied code. The instructor did not ask for a change.
Leave them alone unless he raises it.
</context>

<next_action>
Start with: send the five feedback URLs to the students. They are listed in
`exams/final-01/.enlaces.json` (folder name → code) and can be reprinted at any
time by running `python scripts/build-feedback-pages.py`, which is idempotent
and keeps the same codes.
</next_action>
