---
context: default
task: n/a (quick-task study/build workflow, no active phase)
status: paused
last_updated: 2026-08-11T22:05:00Z
---

# Handoff — 2026-08-11 (pausa tras Fase 6 + sistema de mapas Colab)

## Estado

Sesión 07 **dictable de punta a punta** (Fases 0–6 en lección + notebook +
guion). Todo pusheado (`7fc0cb7`), working tree limpio, sitio publicado.

Fase 6 medida: R2 74% y R3 78.6% REPRUEBAN (clímax pedagógico), σ=14.4 vs 8,
SE=4.75 vs 2.0, tilt bisturí inefectivo (3er anti-libro), veredicto 5✓/2✗.

Sistema de mapas: alumnos corren en **Colab** (link en la lección) con una
perilla `ESCENA`; el curso publica mapas como `escenas/<nombre>.zip` en el
sitio. Dos mapas: `san_isidro` (el del encargo) y `san-isidro-01` (nuevo,
Z-up verificado). Guía Blender = herramienta del instructor (§7 = publicar).

## Pendiente

1. **Cierre de la lección**: objetivos↔contenido, resumen, ejercicios.
2. **4 figuras SVG** del instructor (placeholders §4.1/§4.3).
3. **Smoke test Colab** (usuario, en curso): sesiones→terminar todas,
   incógnito, T4, Ejecutar todo; éxito = celda [1] + "mapa descargado".
4. Opcional: parametrizar `REQ["area_km2"]` y `SITIOS` si san-isidro-01
   será escenario de Fases 4–6 (hoy solo Fases 0–3 le calzan sin cambios).

## Anti-patrones vigentes

| Patrón | Prevención |
|---|---|
| np.float64 → mi.Point3f | `float()` en todo argumento a Sionna |
| Comparar calidades de muestreo | mapas solo vs misma calidad (49%≠79% mismo plan) |
| Commits sin push | hoy: 0 pendientes ✓ |
| Colab sirve copias viejas | caché por-URL + VM persistente: incógnito + Administrar sesiones |
| Escena acostada (Y-up) | chequeo bbox SIEMPRE antes de publicar (guía §6/§7) |

## Infraestructura

- Env `ran-design`, `LD_LIBRARY_PATH=$CONDA_PREFIX/lib` en shells no interactivos.
- Ejecución: `cd docs/sessions/07-network-design && python -m nbconvert --to notebook --execute --inplace design.ipynb` (~25 min por el mapa 10⁶; background).
- Guion: https://claude.ai/code/artifact/98209b61-23d1-425e-837b-89bf180242a8 — en sesión nueva WebFetch + republicar con `url:`.
- Reglas editoriales en STATE.md: tuteo; "cálculo" no "cuenta"; "la publicidad" no "folleto"; ningún símbolo entra a fórmula sin nombre (glosario de respaldo en la lección).
- Cadena del notebook: `REQ→ESPECTRO→COBERTURA→CAPACIDAD→PLAN→DETALLE→FINAL` + perilla `ESCENA` en la celda de preparación.

## Next action

Cierre de la lección (resumen + ejercicios), o la prioridad que fije el
instructor. Verificar antes el resultado del smoke test de Colab.
